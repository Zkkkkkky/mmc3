# M03/M04 参考版页签控件枚举（2026-09-16）

## 采集口径

- 参考程序：`references/legacy_modifier/SRW2_patched.exe` 的隔离副本；SHA-256 `FF1C1C5799FDB8F34DF355D761B382C6946CDADC6810772862947E892E8B4C27`。
- 成功载入样本：`references/legacy_modifier/默认配置文件/测试.nes` 与同名 `.cdl` 的隔离副本；NES SHA-256 `A7656C6ECE9B463DCC26C8C62F280BDAADE55DC39F0A120B8CFFDEB54E8CFC2F`。
- 探针通过真实点击扫描易语言 `CPageControl` 的页签标题带，以可见控件树判断页面是否实际切换。参考区全程只读。

## 结果

| 页面 | 控件证据 | 结论 |
|---|---|---|
| 战场地图 | `controls/B04_主窗口_实际页签_页签A.json` | 与既有 B02 地图页控件树一致。 |
| 初始配置 | `controls/B04_主窗口_实际页签_页签B_x97.json` | 成功枚举图标地址下拉 `140/160/180`、机体图标组 `200`、关卡选择 `110`；截图显示三组图库。当前所选关卡没有可独立枚举的部署行控件。 |
| 商店事件 | `controls/B04_主窗口_实际页签_页签C_x193.json` | 截图显示原始记录 `15 02 FF 01`、`1C 1B FF 00` 及地图事件标记；记录列表/地图为自绘区域，控件树只暴露页容器和关卡选择，未取得单字段控件 ID。 |

三张页签截图的 MD5 各不相同，证明页签切换已生效。M03 的只读图标路由控件已补齐第一手 ID；M03 部署字段及 M04 记录字段仍没有参考版单变量保存差分，不能解除 D3 写入门禁。

`probe-attempts.json` 记录了隔离环境补齐前的失败样本。它们均在参考版载入阶段报同一运行时错误，没有产生字段修改或参考区写入。
