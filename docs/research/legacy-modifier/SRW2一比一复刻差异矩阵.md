# SRW2 参考修改器一比一复刻差异矩阵

> 参考目标：`references/legacy_modifier/SRW2_patched.exe`
>
> 目标 SHA-256：`FF1C1C5799FDB8F34DF355D761B382C6946CDADC6810772862947E892E8B4C27`
>
> 本表只评价用户可观察的业务行为。旧程序的授权、联网、硬件锁和原地覆盖参考 ROM 不属于复刻范围；新修改器继续使用派生 ROM 和事务回滚保护。

## 完成判据

“界面存在”不等于“功能完成”。每个可写功能必须同时满足：

1. 在参考 EXE 中完成一次可重复操作并记录输入、前置状态和结果；
2. 对 ROM 副本只改一个字段，保存后定位全部变化字节；
3. 用参考 EXE 重开并确认显示值和联动结果；
4. 新修改器对同一基线执行同一操作，比较输出字节和重开显示；
5. 覆盖取消、切换记录、非法输入、撤销和容量边界；
6. 有自动化测试，并在适用时通过 Mesen 实机验证。

状态含义：

- **已对齐**：满足上述判据；
- **部分**：仅一部分字段或流程已验证；
- **外壳**：只有窗口、控件或只读展示；
- **缺失**：参考程序存在，当前产品没有对应能力；
- **待差分**：已确认参考控件，但写入格式或联动尚未做单变量实验。

## 总览

| 区域 | 参考程序能力 | 当前状态 | 主要差距 / 下一条验收证据 |
|---|---|---|---|
| 启动、打开、保存 | 启动页；打开、保存、退出；自动备份 | 部分 | 外部入口、旧版备份命名和关闭提示仍需实测；新产品刻意禁止覆盖参考输入 |
| 主窗口地图 | 图块库、左右笔刷、地图属性、标题预览 | 部分 | 已修正标题字模；缩放、容量、草稿及规划直达可用。图库绑定、图块属性和写入仍需黄金对照 |
| 初始配置 | 三组图标库；敌军/我方/NPC 部署 | 部分 | 三组图标默认显示为两行八个；具名列表、地图/表格联动、非模态编辑、数量上限可用。第一关确为空初始表，出击由界面事件安排；机体ID到真实图标的绑定仍待查证 |
| 商店事件 | 关卡商店/坐标事件编辑 | 部分 | 实际记录、添加入口、地图定位与空表语义已接通；旧版保存布局黄金对照待补 |
| 机体数据库 | 数值、成长、金钱、经验、技能、变形、地形、图片、碎片、图标、配色、武器跳转 | 部分 | 已补基础金钱/经验、完整特技字节、保留高位的地形编辑、完整16字节常驻显示、配色与图库事务编辑，并按真实主体脚本合成128×128预览；主体脚本编辑、图像上传、碎片合成、图标绑定和变形仍待完成 |
| 人物数据库 | 双头像、配色、名称、双方 BGM、修正值、精神、台词、击破不消失 | 部分 | 已补精神值/成长、五项修正、24精神及全局消耗、击落不消失、可变长属性/7字节头像原码、头像配色/图库/32×32上传，处理共用记录与固定池容量；人物台词字段绑定和旧版黄金对照仍待补齐 |
| 武器数据库 | 名称、特技、射程、命中、距离修正、地形、双方动画、使用机体、代码与测试 | 部分 | 已补特技、距离方式、6字节属性/名称Token及双方真实动画脚本的等长参数编辑；已有反查与双向跳转。动画播放、模式和新增脚本未闭环 |
| 战斗对话 | 24 段对话块；按列表/内容查看和编辑 | 部分 | 已按当前ROM真实文本组、编号和随机分支读取/编辑，控制Token与别名无损保留；只用原容量，未实现文本搬移及参考保存黄金对照 |
| 其他修改 1 | 系统文字、成长方式、等级/经验表、射程命中修正 | 部分 | 99项升级阈值/累计/还需、4×16距离补正、221条系统文字及201—253成长指针槽位已接通；成长实际14条独立记录，214—253共用零记录；仍需旧版逐字段输出黄金对照 |
| 其他修改 2 | 道具名/价格/说明；F0–FE 商店、四件商品、店员与七段对话 | 部分 | 24项道具名称/价格/说明、有效F0—F4商店商品/店员/七段系统对话已可写。F4只有1商品，F5—FE为无效商店指针而禁写；不新增商品或搬移文字 |
| 文字库 | 16×16 网格、单字写入、字体批量替换、清页、代码/地址 | 部分 | 已修正三条4×12竖条反色编码，固定字模点阵写入、按页字体替换/清页/.dcfont导入导出、取消与撤销可用；旧版写回黄金对照、自动新增编码及全字体协议仍待完成 |
| 地图动画 | 动画、规律、调用三页；语义指令、添加、名称和代码编辑 | 部分 | 已解析实际指针、指令和三类规律，允许已验证等长参数及安全调用编辑；背景改写、新增/搬移、名称修改、可视拼图和播放验证未完成 |
| 文字转换 | 文字与 Token 双向转换 | 部分 | 已有双向工具；仍需与旧版码表对未知字、控制码做全量黄金对照 |
| 剧情事件 | 关卡、行动、劝降、地图、剧情对话、胜利文字；三类关卡事件 | 部分 | 关卡设置已按正确Bank读取96组4274条指令，支持等长参数和对话摘要；标题拼图、独立行动表、胜利关卡索引、接上接下与变长脚本仍缺失 |
| 导出机体 | 按旧版命名导出效果、机体、碎片、图标位图 | 缺失 | 当前 `.dcunit` 是新格式，不能冒充旧版导出；目录、五文件命名、尺寸及确定性 BMP 编码器已完成，组合像素规则仍待黄金样本 |
| 导出头像 | 导出正面/背面头像位图 | 部分 | 已按真实头像记录导出 `[背面].bmp`、`[正面].bmp`，4×4图块、旧版素材四色、32×32/24-bit/BGR/bottom-up及目录命名均已接通；仍缺参考程序动态导出文件的逐字节黄金对照 |
| 属性计算器 | 敌我人物/机体/武器/等级装配后计算 | 部分 | 已读取人物属性原码/精神/成长/补正、机体属性与特技、武器属性并使用非空默认记录；当前公式仍只使用部分控件，需对命中、伤害、倍率、地形等建立旧版黄金样本 |
| 存档修改器 | 打开、选槽、读取、两组单位表、写入内存、保存文件 | 外壳 | 当前只显示文件大小与哈希，没有槽位、表结构、校验和或保存流程 |
| 其他全局参数 | 双击公式、伤害公式、命中公式、11 个道具值、12 个初始人物/机体选择框 | 部分 | 已从 ROM 读取并事务写回全部可见字段；仍需参考 EXE 保存副本的逐字段黄金对照后升为“已对齐” |

## 已确认的参考窗口细节

2026-09-11 用户截图反馈的定位、实现、写入边界和自动验证分别见 [机体与地图](截图反馈_机体地图.md)、[人物与武器](截图反馈_人物武器.md)、[文字、关卡、成长与商店](截图反馈_文字事件.md)、[动画与规律](截图反馈_动画.md)。本轮结果尚不替代参考 EXE 单变量保存的黄金对照，因此相关行仍标记“部分”。界面实拍证据位于 `output/verification/screenshot-feedback/`。

同日新增的“完整ROM数据读取”是排查遗漏的只读入口：它索引255条机体、199条人物、254条武器、100张地图、1258条文字、1177条动画，以及行动事件与分Bank关卡脚本合计6911条，并允许从记录跳到覆盖全部1,310,736字节的HEX视图。它证明原始内容已进入界面，不代表未知字节已经获得可写语义。

### 数据库

- 机体页包含机体/碎片上传、清除、导图偏移、调换图库、压缩、两类拼图与显示、图标上传/更改、两组三色、机体类型、三个地址、地形、变形，以及完整属性和两格武器。
- 人物页包含正反头像及图库/颜色、名称和战斗名、双方战斗音乐、精神值/成长、五项修正、“击破不消失”、24 种精神及消耗、五类攻击台词、六类防御台词和变形台词。
- 武器页包含名称、特技、射程、命中、距离修正、空陆海、双方武器动画、使用机体、代码编辑、测试和模式。
- “其他修改2”包含道具表、`F0–FE` 商店，每店四件商品、店员、对话和七段公共对话。

### 独立工具

- 文字库只有“确定”，没有统一的“取消”按钮。
- 文字转换窗口只有两种转换动作，没有“确定/取消”。
- 属性计算器只有“开始计算”，不会写 ROM。
- 存档修改器具有独立的“写入存档”和“保存存档文件”两个阶段。
- “其他”窗口默认双击公式为 `70, 90, 20`；伤害公式为 `13, 10, 10, 1, 1`；命中公式为 `70`；道具值为 `1,1,1 / 5,3,1 / 3,3,25 / 25,50`；初始机体为两列各六格。

### 已接入的全局表证据

- 距离命中补正位于文件偏移 `0xB4D8`，是 4×16 个无符号字节；运行时代码按“方式×16 + 距离−1”索引。
- 累计经验位于 `0xB530`，是等级 1—99 共 99 个小端 `u16`；第 50 项为 `15200`，第 98 项为 `64350`，第 99 项为 `65535` 上限值。
- 初始阵容位于 `0x3965D`，为 6 组人物/机体 ID，后随 `$FF` 哨兵。
- 双击公式三项各有两份运行时代码操作数，偏移分别为 `(0x78109,0x7815A)`、`(0x78127,0x78178)`、`(0x78138,0x78189)`；写入必须同步镜像。
- 伤害公式五项位于 `0x780E4,0x780C5,0x780EB,0x9999,0x99A0`；第 3、5 项是除数，拒绝 0。命中阈值位于 `0xA44E`。
- 11 项道具效果操作数位于 `0x140CF` 至 `0x14115` 的已确认离散位置。上述切片在 `新DC.nes`、兼容基线与 464K 推荐输出中一致。
- 上述公式和道具效果的 23 个可编辑立即数均有周边指令签名；签名已在三份兼容 ROM 交叉验证，并在载入及每次写入前校验。可编辑操作数自身不参与签名，避免合法改值后无法重开。
- 24 项道具名称的指针表位于 `0xCD5A`，文本池为 `[0xCD8A,0xCE42)` 共 184 字节，每项以 `$FF` 结束；修改时重编码全部名称、重排文本池并同步全部指针。
- 24 项道具价格位于 `0x15723`，格式为小端 `u16`。参考窗口显示值是 ROM 原值的 10 倍，因此 UI 只接受 0—99990 的 10 倍数。

### 已确认的旧版 BMP 导出协议

- 主菜单“导出机体”按 ID `1..N` 批量导出；机体右键入口只导出当前项；头像入口只导出当前人物。首个交互均是选择根目录。
- 目录为 `<十进制ID>：<名称>`。机体固定生成 `[效果]`、`[机体]`、`[碎片]`、`[图标1]`、`[图标2]` 五张图；头像固定生成 `[背面]`、`[正面]` 两张图。
- 机体三张主体图为 128×128，图标为 16×16，头像为 32×32；全部是 `BITMAPINFOHEADER`、24-bit `BI_RGB`、BGR、bottom-up、无 alpha 的 Windows BMP。
- 参考图标由 2×2 共四块 8×8 tile 组成。当前候选预览已修正为四图块2×2，但机体ID绑定和参考BMP的像素黄金对照尚未完成，不能宣称旧版导出已兼容。
- 头像记录的正面位置在当前图库 `$C0—$FF` 内按16图块选择，背景位置在偶数2 KiB图库 `$80—$FF` 内按16图块选择；菜单导出以当前工程CHR按4×4顺序生成固定的 `[背面].bmp`、`[正面].bmp`。文件名和BMP结构已匹配静态证据，动态黄金文件仍待采集。
- `src/fc_editor/legacy_bitmap.py` 已实现上述 24-bit、BGR、bottom-up 和行补齐规则，并锁定 16/32/128 像素输出长度；在取得参考程序真实导出黄金文件前，它只代表协议编码器完成，不代表整套图片内容已对齐。

## 逆向与实现规则

2026-09-11 字库纠错证据：旧“直接连续144位”的实现虽读取了ROM，却不是正确字形。`CAC6`（一）在 `0x72C7C` 的18字节为 `FF FF FF 8F FF FF / FF FF FF 0F FF FF / FF FF FF 0F FF FF`；按三条4×12竖条、每字节高/低nibble对应相邻两行、0为前景解码，恰好得到第6行11点横线。`C800`（啊）与参考截图可视吻合，地图“伏击之战”亦已核对。实现及固定槽位/行保留字节测试见 `src/fc_editor/codecs/dc_font.py`、`tests/test_feedback_font_and_performance.py`；完整本轮验收见 [视频反馈优化验收](视频反馈优化验收.md)。这不是参考EXE单字段保存的黄金对照替代品。

- 目标是行为和数据兼容，不宣称恢复原易语言工程源码。
- 优先使用窗体序列化元数据、GBK 字符串交叉引用和代码调用点确定功能边界；任何地址仍需对当前 ROM 实测。
- 一次只改变一个输入；保存到临时副本，不改 `references/`。
- 未知命令必须以原始字节无损往返，不能因选择列表默认值而被重写。
- 对参考程序自身的不安全行为采用明确、可记录的兼容差异；不能为了视觉相同而取消派生输出、范围校验或事务回滚。
- 每补齐一个功能，同步更新本表和 `docs/DC修改器使用说明.md`，并附对应测试或验证产物路径。

## 当前优先级

1. 修复所有“改了但会丢”的草稿与事务缺陷。
2. 为数据库六页建立真正的结构化记录模型，先补齐可由单变量差分证明的字段。
3. 解析文字库、地图动画、剧情和战斗对话，禁止继续使用硬编码示例冒充 ROM 数据。
4. 复原旧版两类 BMP 导出、存档生命周期和全局表。
5. 执行完整黄金对照、构建和模拟器验证后，才将对应行改为“已对齐”。
