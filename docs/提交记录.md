# 提交记录

## 2026-09-10 08:38:12 +08:00 — 扩展资源池并完善修改器

- 分支：`expanded-modifier`
- 基线提交：`10a91dc88133cb998d81de0c95afd736f6baa63f`
- 改了什么：
  - 将原 CHR 旧位置副本和 Bank `$60` 释放为托管空间，使可分配资源池由 200 KiB 增至 464 KiB，同时保留旧基线兼容。
  - 完善修改器的资源导入、工程回放、保护区校验，以及机体、剧情、事件、音乐等编辑入口。
  - 全部页面改为旧版 SRW2 修改器式紧凑布局，并补充 13 张截图的图文使用说明和可重复截图工具。
  - 更新派生 ROM、IPS、单文件 EXE、映射表、容量/构建/验证报告与 Mesen 验证产物。
- 怎么改的：
  - 把双音频调度跳板写入原 Bank `$18` 的已验证空洞，清零旧 CHR 位置副本，以 `$40-$60/$65-$7D` 作为确定性分配区；`$61-$64/$7E-$7F` 保持专用或受保护。
  - 在 profile、工程模型、资源图和构建校验中登记资源 ID、对齐、地址、哈希及重叠保护，并将分配记录写入 `.dcmod`。
  - 隐藏现代外层导航和大标题，按菜单、顶部页签、左侧记录、右侧编辑及地图全景重新组织界面。
- 影响范围：
  - `tools/build_dc_expanded_dual_audio.py`
  - `tools/fc_editor/`
  - `tools/dc_modifier/`
  - `analysis/`
  - `FC模拟器/`
  - `build/`、`patches/`
  - `DC修改器使用说明.md`、`交接文档.md`
- 验证：
  - `.\.venv\Scripts\python.exe -m unittest discover -s tools -p "test_*.py"`：66 项通过。
  - `.\build_modifier_exe.ps1`：成功；EXE 自检退出码 0。
  - 最终 EXE 12 个功能页逐页视觉检查通过，13 张说明截图生成成功且链接有效。
  - `.\验证交接包.ps1`：2226 个文件全部通过。

## 2026-09-10 09:05:08 +08:00 — 添加图文版修改器使用说明

- 分支：`expanded-modifier`
- 基线提交：`7d7112b242aa95e2f2eb80f8869450d6eddb4ddf`
- 改了什么：
  - 新增 14 页 A4 图文版修改器使用说明，完整收录 12 个主章节和 13 张界面截图。
  - PDF 提供封面、纸面目录、页眉页码及 24 个可点击侧栏书签。
  - 新增可重复执行的 PDF 构建脚本，后续可从 Markdown 源说明重新生成交付文件。
- 怎么改的：
  - 使用 ReportLab 解析标题、段落、列表、表格、代码块、提示框和图片，并按中文操作手册样式统一排版。
  - 嵌入思源黑体和黑体中文字形，自动缩放截图，控制章节分页并为标题建立正确的 PDF 书签目标。
- 影响范围：
  - `output/pdf/DC修改器使用说明.pdf`
  - `tools/build_dc_modifier_manual_pdf.py`
- 验证：
  - 使用工作区 Python 运行 `tools/build_dc_modifier_manual_pdf.py`：成功生成 PDF。
  - `pdfinfo`：A4、14 页、未加密，标题和主题元数据正确。
  - pypdf 结构检查：12 个主章节、13 张图片、24 个书签均存在，书签分别指向正确页面，中文字体已嵌入。
  - `pdftoppm -png -r 120`：14 页全部成功渲染；逐页及独立视觉复核未发现裁切、重叠、乱码或孤立标题。

## 2026-09-10 11:19:40 +08:00 — 完善自动扩容并归档 SRW2 修改器界面

- 分支：`expanded-modifier`
- 基线提交：`c976e9f4fa931637aa44ea2ed651112b5b75e77e`
- 改了什么：
  - 完善地图、剧情、机体等资源的自动扩容、重定位、工程回放和保护 ROM 完整性校验，并补充覆盖容量、链接、运行时调度和异常阻断的测试。
  - 更新修改器界面、使用说明、单文件 EXE、PDF、说明截图及自动规划验证产物。
  - 新增 12 张第三方 `SRW2_patched.exe` 顶层操作界面截图，作为后续修改器设计与功能比对参考。
- 怎么改的：
  - 将扩容规划拆分为通用链接、地图、剧情和机体模块，扩展相关编解码器、工程模型、验证服务与修改器页面。
  - 为扩容后的目录、指针表、银行分配、保护区签名和故事记录终止符增加确定性检查与回归用例。
  - 使用兼容的 `DC_kuorong.nes` 逐项打开第三方修改器界面并保存截图，数据库界面使用独立窗口重新截取。
- 影响范围：
  - `tools/fc_editor/`
  - `tools/dc_modifier/`
  - `tools/test_*.py`
  - `analysis/SRW2修改器界面截图/`
  - `build/auto-plan-verification/`
  - `FC模拟器/`、`docs/`、`output/pdf/`、`验证工具/`
- 验证：
  - 本次提交工作流未额外运行测试。
  - 已核对 Git 工作区状态、差异统计、未跟踪文件清单，以及 12 张 PNG 截图和自动规划验证产物均存在。

## 2026-09-10 12:13:37 +08:00 — 补充 SRW2 复刻动线并更新验证产物

- 分支：`expanded-modifier`
- 基线提交：`93a459634bbe02bba2df42624fff3a195a4aaad3`
- 改了什么：
  - 新增第三方 SRW2 修改器的操作动线与一比一复刻规格，覆盖启动、ROM 会话、主页面、数据工具、窗口规则、实施顺序及待实测清单。
  - 改善 Mesen 冒烟脚本在文件输出 API 不可用时的兼容处理，并修正剧情配额耗尽提示文字。
  - 更新修改器 EXE、校验值、PDF 使用说明、自动规划构建报告及 Mesen 验证状态产物。
- 怎么改的：
  - 依据实际操作和已归档截图，把已验证行为与仍需二进制差分确认的推断分开记录，并建立四层界面状态模型。
  - 在 Lua 脚本中先检查 `io` 与 `io.open` 是否存在，再尝试创建日志文件。
  - 将剧情配额提示从“长度发生变化”改为更准确的“被修改”，并同步生成相关交付与验证文件。
- 影响范围：
  - `analysis/SRW2修改器操作动线与复刻规格.md`
  - `analysis/dc_mesen_smoke.lua`
  - `tools/fc_editor/expansion.py`
  - `FC模拟器/`、`build/auto-plan-verification/`、`output/pdf/`、`验证工具/`
- 验证：
  - 本次提交工作流未额外运行测试。
  - 已核对 Git 分支、上游、完整差异统计、文本差异、生成文件和新增复刻文档内容。

## 2026-09-10 14:29:33 +08:00 — 重构 SRW2 修改器界面与操作动线

- 分支：`expanded-modifier`
- 基线提交：`d4bb0eca4366160a3a15c86549c5f4a8e809a782`
- 改了什么：
  - 按参考 SRW2 修改器重构启动器、空白主窗口、地图三页、数据库六页、剧情六页及旧版工具窗口，并将新增能力收纳到“扩展功能”和“工程”菜单。
  - 地图改为全景适应显示；数据库、剧情和高级编辑窗口采用确定/取消整窗事务，补齐合法草稿自动提交、非法草稿导航阻断及撤销/重做保护。
  - 更新 3.0 图文说明、界面截图、14 页 PDF、单文件 EXE、校验文件及全仓库 SHA-256 清单。
- 怎么改的：
  - 新增 `legacy_windows.py` 和 `legacy_tools.py` 复刻参考窗口结构；重组 `app.py` 与 `map_page.py`，加入启动器生命周期、独立窗口路由、共享关卡上下文和无滚动地图画布。
  - 为地图、剧情、事件和劝降页面建立统一草稿契约与导航守卫；只开放已核对的 ROM 字段，未验证的字体、动画、存档和全局参数写入继续保持只读。
  - 扩展截图与 PDF 生成脚本，并重新构建带完整 Qt 依赖的 Windows 可执行文件。
- 影响范围：
  - `tools/dc_modifier/`
  - `tools/test_dc_modifier_ui.py`、`tools/test_legacy_*_ui.py`
  - `DC修改器使用说明.md`、`docs/images/dc_modifier/`
  - `output/pdf/DC修改器使用说明.pdf`
  - `FC模拟器/新DC篇完整修改器.exe`、`文件清单_SHA256.csv`
- 验证：
  - `.\.venv\Scripts\python.exe -m unittest discover -s tools -p "test_*.py" -v -f`：181 项全部通过。
  - `.\build_modifier_exe.ps1`：成功；生成 EXE SHA-256 为 `3788C79C60DAB30DFFF3065DBB50E56DA450F6DF330C8207C62B390F4AFD8DB5`。
  - Windows 实机验证通过启动器、ROM 载入、地图三页、数据库、剧情、文字库、扩展容量规划及关闭退出动线，未发现 Qt DLL 错误或后台残留进程。
  - PDF：A4、14 页，逐页渲染检查未发现裁切、重叠或乱码。
  - `.\验证交接包.ps1`：2277 个文件全部通过。

## 2026-09-10 15:32:09 +08:00 — 精简修改器启动页并更新交付产物

- 分支：`expanded-modifier`
- 基线提交：`04c3bae25a9190ee9a42c94c4c4b353bb5747fee`
- 改了什么：
  - 将修改器启动页精简为红色“作者 断月残心”和唯一的“进入修改器”按钮，移除四个旧外部入口及其提示文案。
  - 同步更新启动页截图、图文使用说明、14 页 PDF、单文件 EXE、校验值和全仓库 SHA-256 清单。
  - 收录用户提供的旧启动页截图，作为本次界面变更的对照参考。
- 怎么改的：
  - 从 `LauncherWindow` 删除旧说明和 `QGridLayout` 外链按钮，为作者标签和进入按钮设置稳定对象名，并保留原有窗口生命周期和进入主编辑器逻辑。
  - 新增启动页契约测试，严格校验作者文字、红色样式及按钮列表，随后重新生成文档截图并构建 PDF 与 Windows EXE。
- 影响范围：
  - `tools/dc_modifier/app.py`、`tools/test_dc_modifier_ui.py`
  - `DC修改器使用说明.md`、`docs/images/dc_modifier/00-launcher.png`
  - `output/pdf/DC修改器使用说明.pdf`
  - `FC模拟器/新DC篇完整修改器.exe`、`文件清单_SHA256.csv`
  - `.codex-remote-attachments/`
- 验证：
  - 启动页与关闭生命周期的 2 项定向测试通过。
  - `.\.venv\Scripts\python.exe -m unittest discover -s tools -p "test_*.py"`：182 项全部通过。
  - `.\build_modifier_exe.ps1`：成功；EXE SHA-256 为 `66C12E236E6030604D7C0AD1B8FC85A534D07CFF57DB08E058296D1538F4BA29`。
  - Windows 实机检查确认启动页仅有作者文字与一个进入按钮；点击后主窗口正常打开，关闭后无残留修改器窗口。
  - PDF：A4、14 页，逐页渲染检查未发现裁切、重叠或乱码。
  - `.\验证交接包.ps1`：2277 个文件全部通过。

## 2026-09-10 16:01:36 +08:00 — 整理项目结构并完善 README

- 分支：`expanded-modifier`
- 基线提交：`dc04e87d5d45d567758970ad4106b6692dd6fcba`
- 改了什么：
  - 将旧修改器快照迁入 `references/legacy_modifier/`，将淘汰方案归档到 `docs/archive/`，并把设计方案与提交记录收进 `docs/`。
  - 删除 1 份与兼容基线逐字节相同的 ROM 副本，以及 Codex 附件缓存和 FCEUX 本机状态文件；补充相应忽略规则。
  - 重写根目录 `README.md`，补齐快速开始、能力、ROM 身份、技术架构、目录树、开发命令、文档索引和已知限制。
  - 新增 `requirements-dev.txt`、`docs/README.md` 与 `references/README.md`，并同步使用说明、交接文档和仓库约束。
- 怎么改的：
  - 保留 `tools/`、`analysis/`、`build/` 和 `FC模拟器/` 等现有导入与构建契约，仅整理根目录中的历史输入、文档与本机状态。
  - 更新旧路径引用、交接清单排除项和忽略规则，并从 Markdown 源重新生成 PDF 使用说明与 SHA-256 文件清单。
- 影响范围：
  - `README.md`、`requirements-dev.txt`、`.gitignore`
  - `docs/`、`references/legacy_modifier/`
  - `DC修改器使用说明.md`、`交接文档.md`、`AGENTS.md`
  - `output/pdf/DC修改器使用说明.pdf`、`文件清单_SHA256.csv`
- 验证：
  - 迁移的旧修改器 120 个文件、历史方案 7 个文件及 2 份文档均与原 Git blob 逐字节一致。
  - `.\.venv\Scripts\python.exe -m unittest discover -s tools -p "test_*.py"`：182 项全部通过。
  - `.\.venv\Scripts\python.exe tools\run_dc_modifier.py --self-test`：通过。
  - PDF：A4、15 页，全部页面渲染检查未发现裁切、重叠、空白页或乱码。
  - `.\验证交接包.ps1`：2279 个文件全部通过。
