# M00—M02 待人工完成事项

> 更新日期：2026-09-16 ｜ 当前状态：H-01—H-05 证据已完成实现侧审计，用户本轮确认 H-06 已签收；M00—M02 按遗留项记录为有条件通过。本清单是交接入口，不替代 `docs/需求拆分/M00_黄金对照流水线.md`、`M01_启动器与主窗口.md`、`M02_战场地图.md` 第 7 节的完整验收清单；需求判定以最新 `docs/SRW2修改器1比1复刻需求文档.md` 为准。

本清单记录截至 M02 曾要求**可操作交互式 Windows 桌面的人**执行或观察的事项及当前完成状态。用户本人可操作并交付录屏/文件，也可让能访问同一桌面的验收者或 agent 操作；自动差分、档案归档、回归测试和问题定位由实现侧继续完成。用户已确认 H-06 签收，录屏或离屏单测仍不能自动视为“已对齐”。B1 初始配置/商店事件录屏、C1 音频验收和 C2 最新需求基准已经确认，不在本次清单中重复要求。

## 操作前共通约束

- 参考 ROM、参考 EXE 与 `references/` 全程只读。每个会写入的用例从基线 ROM 复制**独立工作副本**。2026-09-16 用户确认旧修改器不兼容新底板，M02 后续不再要求旧修改器对照；M00/M01 既有参考证据口径不变。
- 留存基线文件名及 SHA-256、操作录屏或关键截图、修改前/后的工作副本、保存目标与目录文件列表。可放在 `output/verification/` 下按模块分目录；若通过会话提供文件，注明对应事项编号与所用 ROM。对外分享优先使用差分报告/IPS，不公开分发 ROM。
- 发生报错或行为不一致时保留原始失败证据，不覆盖原用例，也不要填写“通过”。实现侧会先复核差异并返工，然后再安排复测。

## 按优先级执行

| 编号 | 模块 | 现场操作 | 必留证据 / 判定点 | 状态 |
|---|---|---|---|---|
| H-01 | M00 黄金流水线 | 在可见参考版窗口完成单字段保存与全新进程重开；新的复测须换用未占用的 case_id | `cold_start_03/case.json` 已确认两个不同 PID、70→71、预期外偏移为空、耗时 12.312 秒；全量 verify 为 62/62 pass | 证据通过；用户已签收 |
| H-02 | M01 启动/打开/菜单 | 分别在参考版与新修改器核对主窗口动线；新修改器先检查启动器仅署名 `断月残心`+进入按钮、点击后启动器销毁、未载入态三空页签及仅文件/帮助菜单；`Ctrl+O` 打开工作 ROM 后核对标题全角冒号+绝对路径、数据菜单与 B03 的 14 个参考命令及快捷键 | 参考版与新版均已覆盖文件 3+数据 10+帮助 1；新版文件菜单稳定显示打开/保存/退出及快捷键。原生命令 ID 由 B03 探针证据复核 | 证据通过；用户已签收 |
| H-03 | M01 两级提交/保存/备份 | 用隔离工作 ROM 的任一可编辑字段做“更改→确定→关主窗口而不保存→重开”和“更改→确定→保存→全新进程重开”两条路径；参考版与新修改器各做一遍。参考版保存前后目录列表还需在探针环境、常规桌面各观察一次，以解决 NEG-3/C-5 自动备份证据冲突 | 已覆盖两版保存重开及不保存重开；新版严格路径为改图→应用到项目→不 Ctrl+S 关闭→重开恢复。新版连续 `.bak` 唯一；参考版常规桌面与探针均无自动备份 | 证据通过；用户已签收 |
| H-04 | M02 地形/尺寸/容量 | 仅在新修改器用独立工作副本录左/右两套笔刷、中键吸取和 X/Y 坐标；改一格并导出；分别测试高度/宽度 1、32 边界和 RLE 容量超限提示/拒绝 | 已完成新修改器现场录制：右键单格出现 `326 / 324 B` 超限且未应用，左键恢复；中键吸取不改字节；坐标 `(7,18)` 单格修改得到 `322 / 324 B` 并导出。边界 1/32 已展示；全新进程重开导出 ROM 后仍显示 `322 / 324 B`、尺寸 `26 × 27` 且无未保存修改 | 新版证据完成；用户已签收 |
| H-05 | M02 图块属性/只读/超集 | 仅在新修改器抽查图库 A 的颜色表 1 色号、防御、海属性及空/陆/海移动补正各一字段，图库 B—G 各抽一字段；检查图库 H 与公用颜色表 2/3 的显示及只读性，并确认“编辑图块属性”超集动线 | 图库 A 六类字段均由新修改器界面保存为独立 1 字节样本；图库 B 的真实 UI 导出确认颜色改动同时补写缺失的位图 5 海标志，并另存严格单字段样本；B—G 最小样本均由同一编码器生成、反解并核对记录偏移。D/G 可编辑窗口、H 只读及公用颜色表 2/3 只读已有录屏 | 证据通过；用户已签收 |
| H-06 | M00/M01/M02 用户签核 | 实现侧完成上述证据审计、差异返工和模块第 7 节逐项核对后，由用户确认每个模块的验收结果与日期 | 用户在本轮会话确认“已经签收完毕”；三个模块签核已记入 `docs/需求拆分/README.md` 第 6 节，未完成门禁保留 | 已完成（2026-09-16，三个模块有条件通过） |

H-01 证据位于 `output/build/legacy-diff-audit/cases/legacy_live/M17/hit_threshold/cold_start_03/case.json`，失败尝试 `cold_start_02/error.json` 仍保留。采集器不覆盖既有 case_id；复测时以 `tools/golden_pipeline/cases/M17-hit-threshold-cold-start-03.json` 为配方，另设新 case_id。实现侧现已完成 18/18 模块的字段分母审计与动态证据门禁，安全分母合计 6842；用户签收与明确排除项仍按各模块清单处理。

H-02—H-05 证据已归档并按用户确认登记签收。后续补证仍按 `docs/需求拆分/M01_启动器与主窗口.md`、`M02_战场地图.md` 第 5 节进行，补齐后再决定是否从“部分实现”升级为“已对齐”。
