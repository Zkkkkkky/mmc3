# M00—M02 待人工完成事项

> 更新日期：2026-09-15 ｜ 当前状态：以下现场证据与用户签核均未完成。本清单是交接入口，不替代 `docs/需求拆分/M00_黄金对照流水线.md`、`M01_启动器与主窗口.md`、`M02_战场地图.md` 第 7 节的完整验收清单；需求判定以最新 `docs/SRW2修改器1比1复刻需求文档.md` 为准。

本清单仅列截至 M02 尚需**可操作交互式 Windows 桌面的人**执行或观察的事项。用户本人可操作并交付录屏/文件，也可让能访问同一桌面的验收者或 agent 操作；自动差分、档案归档、回归测试和问题定位由实现侧继续完成。只有用户最终确认后，才能填写模块验收签核，不能把录屏或离屏单测自动视为“已对齐”。B1 初始配置/商店事件录屏、C1 音频验收和 C2 最新需求基准已经确认，不在本次待办中重复要求。

## 操作前共通约束

- 参考 ROM、参考 EXE 与 `references/` 全程只读。每个会写入的用例从同一基线 ROM 复制**独立工作副本**，参考版与新修改器分别使用各自副本；不要用某一版保存过的 ROM 作为另一版的基线。
- 留存基线文件名及 SHA-256、操作录屏或关键截图、修改前/后的工作副本、保存目标与目录文件列表。可放在 `output/verification/` 下按模块分目录；若通过会话提供文件，注明对应事项编号与所用 ROM。对外分享优先使用差分报告/IPS，不公开分发 ROM。
- 发生报错或行为不一致时保留原始失败证据，不覆盖原用例，也不要填写“通过”。实现侧会先复核差异并返工，然后再安排复测。

## 按优先级执行

| 编号 | 模块 | 现场操作 | 必留证据 / 判定点 | 状态 |
|---|---|---|---|---|
| H-01 | M00 黄金流水线 | 在**可见参考版窗口**的交互式 Windows 桌面终端，从仓库根目录运行下方 `cold_start_02` 命令；若驱动未找到窗口，把失败画面与 `error.json` 一并交回实现侧 | 成功时留 `case.json`（两个不同 PID、请求值、完整偏移集、剔除归一化、全新进程重开值、全程 ≤30 秒）；失败时留 `error.json`。旧 `cold_start_01` 只有窗口等待失败，不能算闭环通过 | 待做 |
| H-02 | M01 启动/打开/菜单 | 分别在参考版与新修改器核对主窗口动线；新修改器先检查启动器仅署名 `断月残心`+进入按钮、点击后启动器销毁、未载入态三空页签及仅文件/帮助菜单；`Ctrl+O` 打开工作 ROM 后核对标题全角冒号+绝对路径、数据菜单与 B03 的 14 个参考命令及快捷键 | 连续录屏、打开对话框/载入后截图、菜单逐项记录。逻辑 ID 可由实现侧按 `output/verification/legacy-ui-probe/controls/B03_完整菜单树.json` 复核，静态录屏不单独证明原生命令 ID | 待做 |
| H-03 | M01 两级提交/保存/备份 | 用隔离工作 ROM 的任一可编辑字段做“更改→确定→关主窗口而不保存→重开”和“更改→确定→保存→全新进程重开”两条路径；参考版与新修改器各做一遍。参考版保存前后目录列表还需在探针环境、常规桌面各观察一次，以解决 NEG-3/C-5 自动备份证据冲突 | 各路径的请求值/重开值、前后工作副本和目录列表、录屏。**D1 例外**：新修改器首次 `Ctrl+S` 选择派生 ROM、以后保存同一派生文件并建唯一 `.bak`，不得为模拟参考版直接保存而覆盖载入基线；参考版备份观察只用于核实参考行为 | 待做 |
| H-04 | M02 地形/尺寸/容量 | 两版使用同 SHA-256 基线的独立副本、同一关卡；录左/右两套笔刷、吸取和 X/Y 坐标，再各改一格地形并保存；分别改高度与宽度，测试 1、32 边界和 RLE 容量超限时的实际提示/拒绝行为 | 同关卡/格坐标/原值/请求值、画面和前后 ROM 副本。实现侧做单格与宽高字段级差分、保存重开及容量边界比较；不能用不同 ROM 的 D5 静态截图判定逐格一致 | 待做 |
| H-05 | M02 图块属性/只读/超集 | 在参考版与新修改器各抽查图库 A 的颜色表 1 色号、防御、海属性及空/陆/海移动补正各一字段，图库 B—G 各抽一字段；检查图库 H 与公用颜色表 2/3 的显示及只读性。核对画布右键超集菜单不破坏左键行为，以及“编辑图块属性”按钮的参考版无行为/新版超集动线 | 每例单字段操作录屏、原值/请求值、保存重开值和前后工作副本；H/公用表的只读截图或录屏。实现侧核对 A—G 84 字节记录、`graphic_selector` 不变和预期外偏移为空 | 待做 |
| H-06 | M00/M01/M02 用户签核 | 实现侧完成上述证据审计、差异返工和模块第 7 节逐项核对后，由用户确认每个模块的验收结果与日期 | 三个模块第 7 节与 `docs/需求拆分/README.md` 第 6 节的签核记录；未满足 L1/L2/L3 与 M00 门禁时继续标“部分实现”，不先填通过 | 待做 |

H-01 命令（开发依赖需有 `pywinauto`；默认只用 `output/build/legacy-diff-audit/` 的隔离参考 EXE 与 `audit.nes`）：

```powershell
.\.venv\Scripts\python.exe tools\report_golden_coverage.py collect --case-config tools\golden_pipeline\cases\M17-hit-threshold-cold-start.json
```

正常输出落在 `output/build/legacy-diff-audit/cases/legacy_live/M17/hit_threshold/cold_start_02/`。采集器不覆盖既有 case_id；失败后实现侧先读 `error.json`，修复驱动时用新的 case_id 复测。H-01 通过后仍须由实现侧复核 61 个既有档案、G1/G2 分母口径和参考目录零改动，不能把“61 个 verify pass”当成这次真实冷启动现场通过。

收到 H-02—H-05 证据后，实现侧按 `docs/需求拆分/M01_启动器与主窗口.md`、`M02_战场地图.md` 第 5 节做逐字段黄金档案，再更新模块第 8 节、主需求第 11 章和 `docs/开发记录.md`；H-06 始终由用户决定。
