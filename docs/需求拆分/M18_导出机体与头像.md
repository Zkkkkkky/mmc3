# M18 导出机体与头像

> 模块编号：M18 ｜ 状态：未启动（B3 样本已就绪） ｜ 优先级：P2 ｜ 依赖：P0 黄金对照流水线（像素比对工具，G6）；D2 已决策｜ 主文档章节：4.16 / 5.8 / 11.17 / 9.2（D2）
> 决策依赖：D2 已于 2026-09-15 归档：采纳双入口方案——“数据→导出头像”保留但置灰并标注“参考版不可触发”，Ctrl+L 不触发增强导出；当前已有头像 BMP 导出作为超集功能迁至“扩展功能”独立入口

## 1. 范围界定

本模块实现参考版"导出机体"批量导出与"导出头像"两项功能：

- **做**：导出机体菜单动线（数据→导出机体 Ctrl+F，命令 20017）与系统通用保存对话框（REQ-EXP-001）；按五 BMP 协议批量导出全部机体（REQ-EXP-002）；产物与参考版逐像素一致（REQ-EXP-003，G6 验收）；参考版导出头像项按 D2 置灰（F-097）；已有头像两图 BMP 导出作为超集功能迁至“扩展功能”（F-S18）。
- **不做**：按推测协议实现导出头像导出行为（C 选项禁止）；机体组合像素规则未与 B3 样本完成 G6 比对前不宣称对齐；不改动超集 .dcunit 机体包（F-S08，依 D1 保留）。
- **边界**：导出读取的是当前内存 ROM 缓冲中的图形数据（与参考版一致）；全程不写 ROM、不弹确定/取消——与两级提交模型（6.1）无关。
- **超集口径**：当前产品 .dcunit 包导入/导出（F-S08）与头像 BMP 导出（F-S18）均为超集/新协议形态——依 D1 保留、不构成 1:1 偏差；依 D2，头像 BMP 导出改从“扩展功能”进入。参考版机体旧协议导出必须另行实现，不得以 .dcunit 替代宣称完成。

当前产品现状（8.1 #15“缺失”/#16“部分”）：已有通用确定性 BMP 编码器，菜单当前仍接 .dcunit，尚无参考版 001—255 批量导出路径，组合像素规则待与 B3 样本完成 G6；F-097 的对齐菜单项待置灰，已有增强导出重分类为 F-S18 并待迁移菜单入口。B3 参考版机体导出全集已于 2026-09-15 收到并完成协议审计，本模块可在 P0 像素比对工具就绪后直接启动。

## 2. 功能点清单（继承主文档 11.17）

抄录自主文档 11.17（共 2 项，与 11.17 表行数一致）：

| 编号 | 功能点 | 当前状态 | 本期目标 | 验收方式 |
|---|---|---|---|---|
| F-096 | 旧版导出机体（五 BMP 协议） | 部分实现 | 接入 001—255 批量路径并通过 B3 样本 G6 | 导出产物与参考版逐像素一致（G6）；目录/文件名协议一致 |
| F-097 | 导出头像（参考版对齐菜单项） | 部分实现 | 数据菜单项置灰并标注；Ctrl+L 不触发增强导出 | 菜单行为与 D2 决策一致 |

超集功能 F-S18：已有正面/背面 BMP 导出迁至“扩展功能”，保留确定性导出回归，不纳入参考版 G6 像素比对分母。

## 3. 参考版界面与行为规格

**入口动线**（controls/B03_完整菜单树.json 实证）：

- 导出机体：数据菜单「导出机体&(P)」，快捷键 Ctrl+F（命令 20017）。
- 导出头像：数据菜单「导出头像&(L)」，快捷键 Ctrl+L（命令 20018）。

**导出机体操作动线**：数据→导出机体（Ctrl+F）→ 弹出系统通用保存对话框（`#32770`，保存&S/取消）→ 选定根目录 → 参考版批量导出 ID 001—255（不含 000）的全部机体。另据差异矩阵（E4）：机体列表右键入口仅导出当前项、头像入口仅导出当前人物【推断待验证：右键/头像入口在探针轮次未复现，需黄金对照补证】。

**导出头像否定证据 NEG-5**：菜单项触发后无任何窗口创建（探针多轮触发+40/90/300s 轮询不可复现）——参考版该功能不可触发；处理方式见 D2。

**保存对话框**（D11，force-show 方案捕获）：51 控件系统通用 `#32770` 保存对话框（保存&S/取消）——参考版未提供自定义控件，全部为系统对话框控件（文件名编辑框、目录树、保存类型下拉等均为 Windows 系统控件，复刻版直接接系统对话框，无需自绘复刻）。对话框默认文件名/默认目录参数【待黄金对照补证——force-show 捕获的是空参数形态】。

**导出协议**（B3 参考版实际导出全集，E3 级；目录/命名/BMP 头已全量核验，像素组装仍须按 G6 与当前实现比对）：

| 项 | 值 |
|---|---|
| 根目录 | `导出的机体/` |
| ID 范围 | `001`—`255`，三位十进制补零，不含 `000`；共 255 个目录 |
| 目录命名 | `<三位十进制ID>：<名称>`（全角冒号）；ID 246—255 名称为空但保留冒号 |
| 机体五图 | 每目录恰好 5 张：`<名称>[效果].bmp`、`<名称>[机体].bmp`、`<名称>[碎片].bmp`、`<名称>[图标1].bmp`、`<名称>[图标2].bmp` |
| 数量与尺寸 | 共 1275 张；效果/机体/碎片各 255 张、128×128、49206 字节；图标1/图标2各 255 张、16×16、822 字节 |
| 文件格式 | 全部为 `BM`、40 字节 `BITMAPINFOHEADER`、24-bit BI_RGB/BGR、正高度 bottom-up；像素偏移 54，planes=1，保留及可选头字段为 0 |
| 颜色口径 | `[机体]`/`[碎片]`/`[图标2]` 全集固定 4 色 `#000000/#3933FF/#63CF63/#DCFFFF`；`[图标1]` 每张 4 色（全集 7 色）；非空 `[效果]` 每张 5—7 色（全集 38 色）；效果/机体/碎片各有 129 张全黑图 |
| 头像两图（F-S18） | `[背面]`、`[正面]`，32×32；不属于 B3 机体样本和参考版 G6 分母 |
| 命名敏感点 | 目录名全角冒号「：」；文件名方括号为半角；空名称时文件名从 `[` 开始 |

**需求条目**：REQ-EXP-001（菜单与快捷键动线一致；保存对话框为系统通用对话框）、REQ-EXP-002（落盘目录与文件名按上表协议；BMP 编码参数一致）、REQ-EXP-003（导出产物与参考版导出产物**逐像素一致**，G6）、REQ-EXP-004（导出头像在参考版不可触发——见 NEG-5 与 D2）。

**当前产品现状**：F-096 部分实现——通用确定性 BMP 编码器已完成，其 128×128 全黑输出已与 B3 样本整文件 SHA-256 一致（`B337D5ED1680E7F48814506D5925B8C0D60BAB222A9E9ABD62F5677740070539`）；但菜单当前接 .dcunit（超集 F-S08），参考版批量目录路径与非黑像素组合规则尚未接入/验证。F-097 的数据菜单项尚未按 D2 置灰；已有头像 BMP 导出已重分类为超集 F-S18，待迁至“扩展功能”入口。

**证据路径**（前三项在 `output/verification/legacy-ui-probe/`；B3 为 Git 历史对象）：

- screenshots/C06_导出机体.png、D11_导出机体_保存对话框.png（force-show 方案捕获的保存对话框，51 控件）
- controls/C06_导出机体.json、controls/B03_完整菜单树.json（菜单入口与命令 ID）
- summary.md（NEG-5 否定证据记录）
- B3 机体导出全集：Git 提交 `8766898` 的 `output/verification/legacy-user-evidence-2026-09-14/legacy-unit-export-samples.zip`（ZIP SHA-256 `230E55274B647D0E903C6B2753EE9BD54580F188FD8B3F0DC74F57F8FB0E2C39`；当前检出版本未保留二进制）

## 4. 数据与写入规格

- **只读导出**：本模块不写 ROM——全部产物为磁盘 BMP 文件；无确定/取消（菜单命令直接触发保存对话框）。
- **BMP 编码参数**（B3 动态样本 E3 级 confirmed）：24-bit BI_RGB、BGR 字节序、正高度 bottom-up 行序；尺寸、文件长度及其余头字段按协议表。编码器字节一致性仍由 G6 运行时比对验收。
- **像素组装规则**：机体战斗图 128x128 由 8x8/7x9/9x7/10x6 拼图协议组装（4.5 机体拼图尺寸按钮口径，5.8）；拼图代码为十六进制串（如 `F3 F8 00 01 …`）——**当前实现尚未与 B3 黄金样本完成 G6，仍为 candidate**，不得宣称对齐。
- **拼图尺寸口径**：8x8（标准）、7x9/9x7（窄高/宽矮变体）、10x6（宽扁变体）四尺寸对应参考版机体编辑的拼图尺寸按钮；导出与 4.5 机体编辑共享同一拼装实现，不得另造第二套口径。
- **头像数据源**：超集 F-S18 从人物头像数据只读导出 32x32 点阵；由于参考版导出不可触发（NEG-5），该超集产物仅做自身格式与确定性回归，不宣称与参考版像素协议对齐。
- **导出头像（D2）**：参考版不可触发（NEG-5）；对齐菜单项按已决口径置灰并标注，Ctrl+L 不触发增强导出；当前产品既有头像 BMP 导出属新协议（F-S18），作为超集功能迁至“扩展功能”。
- **命名细节**（B3 confirmed）：根目录固定 `导出的机体`；目录 ID 为三位十进制补零；目录名使用全角冒号；ID 246—255 的空名称不得裁掉冒号；文件名前缀重复目录中的名称，空名称时直接以 `[` 开头。
- **图标语义**：[图标1]/[图标2]（16x16）为机体图标两种形态；B3 只能确认两者像素产物不同，具体帧/用途仍待参考版运行时验证。[效果]/[机体]/[碎片]（128x128）为战斗图三形态。

## 5. 黄金对照采集清单

> 本模块对照形态为**产物像素比对**（G6：导出产物与参考版逐像素一致）。若对照过程中参考版发生 ROM 保存，仍须剔除归一化偏移 0x4A101/0x4A102/0x4AE2A/0x4AE2B/0x79538/0x79539 的 C9 02→D8 47 副作用（REQ-NEG-002）。

| # | 采集对象 | 参考版操作 | 预期差分语义 | 备注 |
|---|---|---|---|---|
| 1 | 参考版批量导出产物全集 | **已完成（B3）** | 255 个目录、五图×255=1275 张 | F-096 黄金样本基准已就绪 |
| 2 | 单机体五图逐像素比对 | 任选 3 机体，五图分别与复刻版比对 | 逐像素一致（G6） | 编码参数+组合像素规则验证 |
| 3 | 目录/文件名协议核对 | **已完成（B3 全量 1275 条）** | `导出的机体/<三位ID>：<名称>/<名称>[类型].bmp`；无例外 | REQ-EXP-002 confirmed |
| 4 | BMP 文件头逐字节比对 | **已完成（B3 全量 1275 张）** | 24-bit BI_RGB/BGR/正高度 bottom-up，头字段与长度均符合协议 | 编码器参数基准 confirmed；当前编码器仍待 G6 |
| 5 | 头像两图可选补证 | 仅当后续发现参考版真实触发条件时采集 | [背面]/[正面] 32x32 逐像素对照；未发现时不计入 G6 分母 | F-097；NEG-5；D2 |
| 6 | 导出头像菜单项行为记录 | 点击导出头像(Ctrl+L) | 无任何窗口创建（NEG-5 复核） | D2 决策证据；12.B2 用户配合 5 分钟 |
| 7 | 右键单机导出入口验证 | 机体列表右键→导出当前项【推断待验证】 | 行为与差异矩阵记录一致 | E4 口径补证 |
| 8 | 保存对话框形态核对 | Ctrl+F 后截图 | 系统 `#32770` 对话框（同 D11） | REQ-EXP-001 |
| 9 | .dcunit 与旧协议共存验证 | 旧协议导出后执行 .dcunit 导出 | 两路径互不干扰，产物各自正确 | 超集并存验证 |
| 10 | 导出 ID 范围确认 | **已完成（B3）** | 001—255，不含 000，共 255 个目录 | 协议口径 confirmed |
| 11 | 保存对话框取消路径 | Ctrl+F 后在对话框点取消 | 无文件落盘；ROM 无变化（本模块不写 ROM） | REQ-EXP-001 边界行为 |

## 6. 实现注意事项与禁区

1. **D2 已决策**：“数据→导出头像”项置灰且 Ctrl+L 不触发增强导出；已有头像 BMP 导出迁至“扩展功能”独立入口；不得按推测协议将超集行为冒充为参考版对齐行为。
2. **G6 一票否决**：像素不一致即未完成——不得以"视觉相似"替代逐像素比对；比对工具纳入 P0 流水线。
3. **协议证据纪律**：B3 已确认目录命名、文件名、数量、尺寸和 BMP 头；像素组装与当前编码器输出未通过 G6 前，UI 仍不得宣称“旧版协议已支持”。
4. **超集并存**：.dcunit 机体包导入/导出（F-S08）保留且默认可见（D1）；旧版协议导出为独立菜单动线（Ctrl+F 同一入口接旧协议），不得移除或改名参考版菜单项（REQ-NFR-SUPER-001）。
5. **全角冒号**：目录名 `：` 为全角——编码/文件系统层面逐字复刻（Windows 文件名允许全角冒号，半角冒号非法，注意区分）。
6. **快捷键**：Ctrl+F（导出机体 20017）按菜单树与命令 ID 实证复刻；Ctrl+L 对应的参考导出头像项因 D2 置灰，不分配给超集导出。
7. **衔接点**：`src/dc_modifier/` 既有 .dcunit 导出与头像 BMP 导出（超集/新协议）——旧协议实现为新增路径，不动超集入口；像素组装复用 4.5 机体拼图口径（8x8/7x9/9x7/10x6）。
8. **回归联动**：本模块不写 ROM；完成后与其他模块一样触发 M17 模板区回归重跑。
9. **产物基准库**：参考版导出产物全集存档至 output/build/legacy-diff-audit/（G6 比对基准库）；基准库一旦建立不得覆盖，只追加。
10. **ID 范围口径**：批量导出固定为 ID 001—255（255 项），不得导出 000；即使名称为空也必须创建目录和五图。ROM 内部 0x100 项与导出编号的映射须显式处理，不能把内部索引 0 直接输出为 000。
11. **批量规模**：全量固定 255 项、每项五图落盘，须顺序导出、及时释放句柄（一次性全载入内存的形态禁止）；单个机体导出失败不得中断整批，以失败清单记录收尾【参考版失败行为待实测确认】。

## 7. 用户验收清单（按模块验收）

| # | 验收步骤 | 预期结果 | 通过 | 验收日期 |
|---|---|---|---|---|
| 1 | 数据→导出机体(Ctrl+F) | 弹出系统保存对话框（同 D11 截图） | | |
| 2 | 选定目录执行导出 | 生成全部机体目录树，目录/文件名符合协议 | | |
| 3 | 抽 3 机体五图与参考版产物比对 | 逐像素一致（G6） | | |
| 4 | BMP 文件头抽查 | 24-bit BI_RGB/BGR/bottom-up 参数一致 | | |
| 5 | 导出头像双入口状态 | 数据菜单对齐项置灰并标注；扩展功能中的增强 BMP 导出可用；Ctrl+L 不触发增强导出 | | |
| 6 | .dcunit 机体包功能回归 | 超集功能保留可用（F-S08） | | |
| 7 | D2 决策归档状态确认 | 已归档为“对齐入口置灰+增强入口保留” | | |
| 8 | 保存对话框点取消 | 无文件落盘，程序状态无变化 | | |

## 8. 状态跟踪

| 日期 | 事件（实现/自验/用户验收/返工） | 记录 |
|---|---|---|
| 2026-09-14 | 模块文档创建（任务 #10） | 依据主文档 v1.1 4.16/5.8/9.2/11.17 + 探针 C06/D11 证据 + NEG-5 |
| 2026-09-15 | D2 决策归档 | 数据菜单对齐项置灰；已有头像 BMP 导出迁至扩展功能；Ctrl+L 不触发增强导出 |
| 2026-09-15 | B3 样本接收与协议审计 | 从 Git 提交 8766898 读取用户指定 ZIP；确认 001—255、255×5=1275 张、完整命名协议、全部 BMP 头与颜色集合；现有编码器的 128×128 全黑输出整文件匹配，非黑像素 G6 留待实现侧 |
| 2026-09-14 | 自查修订（任务 #10 完成标准五项自查） | 补协议命名敏感点/拼图尺寸口径/头像数据源/取消路径/批量规模约束；F 编号 2 项与 11.17 一致；34 条证据路径核验通过 |
