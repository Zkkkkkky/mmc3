# M04 商店事件

> 模块编号：M04 ｜ 状态：未启动 ｜ 优先级：P1 ｜ 依赖：M00（黄金对照流水线）；D3 已决策；用户 B1 录屏已完成 ｜ 主文档章节：4.2（商店事件页）/ 5.9 / 11.3
> 决策依赖：D3 已于 2026-09-15 归档：采纳 A+B（控件级枚举+逐字段黄金对照）；补证完成前只读、不得写入

## 1. 范围界定

本模块覆盖主窗口页签 2"商店事件"（4.2 REQ-MW-014）：按关卡加载商店/坐标事件记录（左侧列表）、记录选择与编辑（含商店与事件两类记录的编辑对话框）、右侧地图坐标定位联动、空表语义、保存动线。

不做：数据库"其他修改2"页签的商店面板（F0—F4 商店商品/店员/三对话编辑、七段店员公共对话——M10，F-055~F-057）；关卡事件指令编辑（M14 剧情事件）；劝降规则表（M14 剧情事件页劝降页签范畴）；部署记录编辑（M03）。

**D3 已决策（2026-09-15）**：主窗口页签 1/2 控件树为容器级转储，内部控件语义未逐项确认（REQ-MW-014；8.1 #3/#4）；探针 TCM 索引切换对易语言 CPageControl 不完全生效，`B04_主窗口_页签1/2.png` 两文件 MD5 相同（均呈现战场地图页 63 控件），商店事件页签**无第一手控件枚举**。依用户拍板采纳 **A+B**：先扩展探针做控件级枚举，再直接黄金对照逐字段差分定位；B1 录屏已补齐可见界面与操作路径，剩余控件 ID 和 ROM 映射仍由实现侧完成；黄金闭环前按 C 只读，不得写入。

超集功能口径（依 D1 决策保留、不构成 1:1 偏差、不参与本模块 1:1 验收）：当前产品已接通的实际记录加载、添加入口、地图定位与空表语义（11.3 F-014 备注所列，属部分实现的先行能力）；F5—FE 无效目录的禁写保护与原因显示（当前产品校验逻辑，保留）。

## 2. 功能点清单（继承主文档 11.3 第五行）

抄录自主文档 11.3（共 1 项，与该表行数一致）：

| 编号 | 功能点 | 当前状态 | 本期目标 | 验收方式 |
|---|---|---|---|---|
| F-014 | 商店/坐标事件编辑 | 部分实现 | B1 已完成；补齐删除、旧版保存布局及重开结果的黄金对照 | 单字段修改→保存→差分仅落该关触发器/记录区→参考版重开显示一致 |

## 3. 参考版界面与行为规格

**证据现状（B1 已补齐可见交互，控件及字节级证据仍待完成）**：

- B1 用户录屏 `output/verification/legacy-user-evidence-2026-09-14/initial-config-and-shop-events.mp4` 已确认“商店事件”页签、原始记录列表、关卡列表、地图右键菜单、“商店设置”对话框，以及多次新增/确定后的列表与地图反馈；审计见同目录 `initial-config-and-shop-events-audit.md`；
- 控件树 B04 节为容器级转储：`output/verification/legacy-ui-probe/controls/controls_summary.md` B04_主窗口_页签2 节与页签 1 转储**完全相同**（63 控件，均为战场地图页内容）——商店事件页签内部控件未逐项枚举（探针已知限制，summary.md §6）；
- E4 级动线证据：`docs/research/legacy-modifier/SRW2修改器操作动线与复刻规格.md` §5.3；参考截图 `references/legacy_modifier/screenshots/03_商店事件.png`；
- E5 级截图反馈：`docs/research/legacy-modifier/截图反馈_文字事件.md` §商店与七段对话（静态结构一致性验证）。

**参考版已知动线**（E4，动线规格 §5.3）：

```text
选择关卡
  → 切换"商店事件"
  → 左侧加载该关商店/事件记录
  → 在右侧地图右键"添加地图事件"
  → 在"商店设置"中选择限定人物、商店/事件类型及对应值并确定
  → 左侧原始记录与右侧地图标记同步更新
  → 文件 → 保存
```

- 空表语义：第 1 关左侧为空属于该关没有对应记录，不是加载失败；空记录显示白色空列表，读取错误显示明确提示（同源原则，动线规格 §5.3）。

**记录编辑对话框**（B1 可见行为已确认）：标题为“商店设置”，包含限定人物、商店选择、地图事件、商店/事件单选及确定/取消；录屏已展示新增和确定，未实际展示删除、取消、保存与重开。控件 ID、字段到 ROM 字节映射仍须按 D3 做控件级枚举和黄金对照，不能只凭录屏直接开放写入。

**与 M10（数据库“其他修改2”商店面板）的边界**：

| 维度 | 本模块（M04） | M10 |
|---|---|---|
| 编辑对象 | 关卡级绑定：哪关哪家店/坐标触发记录 | 商店记录本体：F0—F4 四道具/店员/三对话 |
| 数据落点 | 地图触发器区（Bank $0A/0x987E） | 商店目录指针表 0x15753 指向的记录区 |
| 参考版入口 | 主窗口页签 2 | 数据库窗口其他修改2 页 |
| F 编号 | F-014 | F-055~F-057 |

七段店员公共对话使用系统文字 48—54（顺序及首段示例：进入商店“哈罗：欢迎光临道具商店”），其文本编辑归 M10；本模块仅引用其编号语义：

| 系统文字编号 | 段名 | 语义 |
|---|---|---|
| 48 | 进入商店 | 入店问候 |
| 49 | 准备购买 | 进入选购 |
| 50 | 金钱不够 | 资金不足提示 |
| 51 | 数量超限 | 库存/数量边界提示 |
| 52 | 是否购买 | 确认询问 |
| 53 | 购买之后 | 成交提示 |
| 54 | 离开商店 | 离店告别 |

## 4. 数据与写入规格

**主文档 5.9（劝降/地图触发器/商店事件）**：

| 实体 | 偏移/结构 | 约束 | 状态 |
|---|---|---|---|
| 地图触发器 | Bank $0A；指针表 0x987E；托管区 [0x9ED4,0xA000)（300 字节） | 每关坐标触发器；原始区 [0x9946,0x9950) | candidate |
| 商店事件记录 | 记录 F0—F4 有效 | 商店设置四道具+店员+三对话（4.8，本体编辑在 M10） | candidate |
| 商店目录指针表 | 0x15753；F0 指向 0x15773，八字节：`02 0F 30 04 0C 04 0E 0F`（店员 02 / 窗口 0F / 系统对话起始编号 $30 / 商品数 4 / 四个从零计数道具 ID） | F0—F3 各 4 商品、F4 仅 1 商品（保留）；F5—FE 指针全为 $987E | confirmed（静态结构+截图一致性，E5） |
| 无效目录 F5—FE | 指针均指向 0x987E（地图触发指针表） | 不能作为商品记录解释，禁止写入 | candidate（当前产品校验逻辑） |

写入纪律：该页签记录的参考版保存布局未做单变量差分前，**编辑/新增/删除一律按 D3 保守方案处理（只读展示）**；托管区 300 字节为容量红线。

## 5. 黄金对照采集清单

| # | 采集对象 | 参考版操作 | 预期差分语义 | 备注 |
|---|---|---|---|---|
| 1 | 关卡商店记录·单字段（商店选择换绑） | 选 1 条商店记录改绑定商店→保存 | 差分仅落该关触发器记录 | B1 已完成；字段映射待黄金对照 |
| 2 | 坐标事件记录·单字段（坐标 x/y） | 选 1 条事件记录改坐标→保存 | 差分仅落该条记录 | 地图定位联动验证 |
| 3 | 记录新增 | 依 B1 录屏所示路径新增 1 条→保存 | 记录追加+可能的计数变化 | 容量边界观察（托管区 300 字节） |
| 4 | 记录删除 | 删除 1 条→保存 | 记录移除 | |
| 5 | 空表关卡加载（第 1 关） | 切换到第 1 关（无修改）→保存 | 差分仅归一化偏移 | 空表语义基准 |
| 6 | 商店与事件单选切换 | 切换记录类型（无修改）→保存 | 差分仅归一化偏移 | 验证类型切换是否有落盘 |
| 7 | 关卡切换联动 | 切换关卡（无修改）→保存 | 差分仅归一化偏移 | |
| 8 | 关卡商店记录·限定人物字段 | 依 B1 录屏所示路径改限定人物→保存 | 差分仅落该记录对应字节 | B1 已完成；字段映射待黄金对照 |
| 9 | 商店记录换绑前后比对 | 同一关卡换绑另一家店→保存→参考版重开 | 重开后列表/地图定位一致 | 与 M10 商店记录联动 |
| 10 | 坐标越界输入 | 输入地图宽高外坐标→保存 | 拒绝写入或按参考版边界行为 | 行为观察用例 |

全部用例差分先剔除归一化偏移 0x4A101/0x4A102/0x4AE2A/0x4AE2B/0x79538/0x79539 的 C9 02→D8 47 副作用再判定。

## 6. 实现注意事项与禁区

1. **禁区：D3 补证完成前写入该页签记录**——依已决定的 A+B 路径完成控件枚举与逐字段差分前，页签只读展示（REQ-MW-014、9.3）；
2. **禁区：F5—FE 无效商店禁写**——指针指向地图触发指针表 0x987E，写入将破坏其他数据；UI 显示实际原因（当前产品已实现，保留）；
3. **禁区：托管区越界**——[0x9ED4,0xA000) 共 300 字节容量红线，新增记录前检查剩余空间；
4. **禁区：把空表当读取失败**——第 1 关无记录属源数据实际状态（出击由界面事件安排），显示空列表而非报错；
5. 与 M10 边界：商店记录本体（四道具/店员/三对话/七段公共对话）在数据库"其他修改2"页编辑（F-055~F-057）；本模块只做关卡级绑定（哪关哪家店/坐标触发）；系统文字页与商店页物理偏移冲突保护为超集已有行为，保留；
6. 衔接点：`src/dc_modifier/map_page.py`（商店事件页/地图定位）、地图触发器 codec；不臆造 F5—FE 目录内容。

## 7. 用户验收清单（按模块验收）

| # | 验收步骤 | 预期结果 | 通过 | 验收日期 |
|---|---|---|---|---|
| 0 | **B1 用户录屏已提供并审计**：商店事件页签、右键菜单、商店设置弹窗与新增/确定流程 | 用户补证已归档；未展示的删除/取消/保存重开转入实现侧黄金对照，不再要求补录 | 通过（用户补证） | 2026-09-15 |
| 1 | 切换到有商店/事件记录的关卡，比对左侧记录列表 | 记录条目与参考版一致 | | |
| 2 | 第 1 关（空表） | 白色空列表 + 无报错 | | |
| 3 | 选择 1 条记录 | 右侧地图定位到事件坐标（联动一致） | | |
| 4 | 依录屏改 1 条记录单字段→保存→双方差分 | 差分区间一致 | | |
| 5 | 新增/删除记录各 1 例 | 行为与参考版一致；托管区容量边界一致 | | |
| 6 | F5—FE 选择 | 禁写 + 显示实际原因（指向地图触发指针表） | | |

## 8. 状态跟踪

| 日期 | 事件（实现/自验/用户验收/返工） | 记录 |
|---|---|---|
| 2026-09-14 | 模块文档创建（任务 10 拆分） | 依据主文档 v1.2 第 4.2/5.9/11.3 章；D3 待决策；F-014 单点继承 |
| 2026-09-15 | D3 决策归档 | 采纳 A+B 补证路径；补证完成前保持只读 |
| 2026-09-15 | B1 用户录屏审计完成 | 可见界面和新增/确定路径已补齐；删除/取消/保存重开转入 P1 黄金对照，用户侧不再阻塞 |
