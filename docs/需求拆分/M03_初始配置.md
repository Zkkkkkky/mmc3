# M03 初始配置

> 模块编号：M03 ｜ 状态：未启动 ｜ 优先级：P1 ｜ 依赖：M00；D3 已回答（用户 B1 录屏已提供） ｜ 主文档章节：4.2（初始配置页）/ 5.6 / 11.3
> D3 结论（2026-09-14）：用户真实操作录屏确认界面动线；后续按录屏操作并以单变量保存差分确认 ROM 写入布局，不再等待额外人工录屏。

## 1. 范围界定

本模块覆盖主窗口页签 1"初始配置"：三组图标 CHR 路由下拉与图标条预览、共享关卡选择联动、敌军/客军/我方部署记录编辑（读取/编辑/新增/删除）、部署对象与地图图标联动。

不做：商店事件页签（M04）；初始阵容 12 字段编辑（"其他"窗口，M17 模板区——本模块读取其结果推导我方槽位）；关卡事件指令本身（M14 剧情事件）。

**D3 已回答**：主窗口页签 1/2 的自动控件树仍不完整，但用户真实录屏已确认初始配置页的右键菜单、配置设置对话框与编辑动线。界面补证不再阻塞；ROM 写入仍须按录屏路径做单变量保存差分，通过前不得宣称对齐。

超集功能口径（依 D1 决策保留、不构成 1:1 偏差、不参与本模块 1:1 验收）：地图对象右键紧凑"配置设置"（切换阵营/改坐标/驾驶员/机体/等级/行动/队伍槽，F-S01）；部署对象悬停属性显示（驾驶员/五项补正/机体属性/成长推导/武器/行动名称）；空地右键新增三类配置、左键拖动改坐标；"展开部署明细列表"全表批量调整（说明§2、矩阵）。以上按第 11 章实际状态登记为超集或部分实现。

## 2. 功能点清单（继承主文档 11.3 前四行）

| 编号 | 功能点 | 当前状态 | 本期目标 | 验收方式 |
|---|---|---|---|---|
| F-010 | 三组图标库预览 | 已实现（未验证） | 补黄金对照（CHR 路由联动无写入用例） | 截图比对 + 差分审计 |
| F-011 | 敌军/客军部署编辑 | 已实现（未验证） | 部署记录单字段黄金对照 | 差分仅落部署记录区间 |
| F-012 | 我方槽位重建 | 已实现（未验证） | 初始阵容联动验证（依赖 M17 模板区） | 与参考版同基线比对 |
| F-013 | 部署对象新增/删除 | 部分实现 | 黄金保存对照补齐（前置：B1 录屏 + D3） | 增删用例差分 |

## 3. 参考版界面与行为规格

**证据现状（证据不足，如实标注）**：

- 2026-09-14 用户提供参考版真实操作录屏，已确认地图部署对象的右键菜单、配置设置对话框及部署编辑动线；证据归档于 `output/verification/legacy-user-evidence-2026-09-14/`；
- 控件树 B04 节为容器级转储，内部控件未逐项确认语义（REQ-MW-014）；
- `截图/B04_主窗口_页签1.png` 与页签 2 两文件 MD5 相同，均呈现商店事件页——探针受易语言 CPageControl 索引切换限制拿不到该页签单独呈现（探针 summary.md §6 失败项）；
- 操作动线规格（E4）：该页显示三个图标地址下拉框和三组 16×16 风格机体图标图集，与右侧地图共同构成初始部署编辑器；对象放置、阵营切换、人物绑定及删除手势需进一步 ROM 差异实验【推断待验证】；
- 主窗口战场地图页控件树含"机体图标组"（Group 200：图标地址 1/2/3 标签 610/620/630 + 地址下拉 140/160/180）——三组 CHR 路由关联控件（控件树 B02 节）。

**参考版已知行为**（E4 级）：

```text
选择关卡
  → 切换"初始配置"
  → 显示 3 组机体图标页及各自地址
  → 在地图/配置对象上完成初始单位设置（具体触发方式待实测）
  → 文件 → 保存
```

第一关等空配置表确实由关卡事件生成单位（说明§2 现状佐证空表语义）；空表显示白色空列表而非读取失败。

## 4. 数据与写入规格

**关卡部署（主文档 5.6，confirmed 部分）**：

| 实体 | 规格 | 状态 |
|---|---|---|
| 敌军部署上限 | 18 条/关 | confirmed |
| 客军部署上限 | 3 条/关 | confirmed |
| 我方部署上限 | 11 条/关 | confirmed |
| 敌/客军实体 | 6 字节（x, y, pilot, unit, level, flags） | confirmed |
| 我方部署 | 4 字节（x, y, roster_index, flags）——第三项为运行时队伍槽，非机体 ID | confirmed |
| 坐标约束 | 须在当前地图宽高内 | confirmed |
| 我方槽位推导 | 从六组初始阵容（0x3965D，M17 模板区）+ 此前关卡加入/离队/换人/换机事件重建 | confirmed（实现侧已遍历 73 我方出击位） |

**当前实现侧已遍历规模**（矩阵）：32 表共 140 敌军、10 客军、73 我方出击位；机体记录 `+2` 为四图块地图小图标首图块编号，高两位选择当前关卡路由三槽位之一；三组 CHR 路由随关卡变化。

| 示例关卡 | 三组 CHR 路由 | 说明 |
|---|---|---|
| 第 1 关 | $34 / $35 / $36 | 路由三槽位 |
| 第 4/5 关 | $34 / $35 / $3A | 路由随关卡变化 |

写入纪律：部署记录的参考版写入格式未做单变量差分前，**新增/删除/编辑一律按 D3 保守方案处理（只读展示）**。

## 5. 黄金对照采集清单

| # | 采集对象 | 参考版操作 | 预期差分语义 | 备注 |
|---|---|---|---|---|
| 1 | 敌军部署·单字段（如 level） | 依 B1 录屏所示操作路径改 1 字段→保存 | 差分仅落该 6 字节记录 | 前置：B1 + D3 |
| 2 | 敌军部署·坐标 x/y | 改坐标→保存 | 同上（坐标在宽高内） | |
| 3 | 客军部署·单字段 | 同 #1 | 同上 | |
| 4 | 我方部署·单字段（4 字节记录） | 同 #1 | 差分仅落该 4 字节记录 | roster_index 语义验证 |
| 5 | 部署对象新增 | 依 B1 录屏新增 1 条→保存 | 记录追加 + 可能的容量计数变化 | 容量上限行为观察 |
| 6 | 部署对象删除 | 删除 1 条→保存 | 记录移除 | |
| 7 | CHR 路由切换 | 切换图标地址下拉（无部署修改）→保存 | 差分仅归一化偏移（路由为运行时推导则无写入） | 验证路由是否落盘 |
| 8 | 关卡切换联动 | 切换关卡（无修改）→保存 | 差分仅归一化偏移 | |
| 9 | 敌军上限边界（第 18 条） | 依 B1 录屏改第 18 条→保存 | 差分仅落该 6 字节记录 | 敌方 18 条容量边界 |
| 10 | 我方上限边界（第 11 条） | 同上（我方记录） | 差分仅落该 4 字节记录 | 我方 11 条容量边界 |

全部用例差分先剔除归一化偏移 0x4A101/0x4A102/0x4AE2A/0x4AE2B/0x79538/0x79539 的 C9 02→D8 47 副作用再判定。

## 6. 实现注意事项与禁区

1. **禁区：黄金保存差分完成前不得宣称写入对齐**——D3 动线已由录屏回答，但部署记录仍须完成单变量差分（REQ-MW-014）；
2. **禁区：超出部署上限写入**——敌方 18/客军 3/我方 11 条上限（5.6）；
3. **禁区：坐标越界**——须在当前地图宽高内；
4. **禁区：把空表当读取失败**——空配置表由关卡事件生成单位，显示空列表语义（动线规格 5.3 同源原则）；
5. 我方部署第三项是运行时队伍槽（非机体 ID），不得当机体 ID 写入；
6. 固定部署槽占满时的新增：显示当前/所需字节并给容量规划入口（超集行为，保留）；
7. 衔接点：`src/dc_modifier/map_page.py`（初始配置页/部署明细/配置设置弹窗）、M17 模板区初始阵容联动；
8. 超集行为清单（依 D1 保留，不参与 1:1 验收）：

| 超集行为 | 说明 |
|---|---|
| 地图对象右键紧凑“配置设置” | 切换阵营/改坐标/驾驶员/机体/等级/行动/队伍槽 |
| 部署对象悬停属性显示 | 驾驶员/五项补正/机体属性/成长推导/武器/行动名称 |
| 空地右键新增/左键拖动 | 新增三类配置、拖动改坐标 |
| 展开部署明细列表 | 全表批量调整 |

## 7. 用户验收清单（按模块验收）

| # | 验收步骤 | 预期结果 | 通过 | 验收日期 |
|---|---|---|---|---|
| 0 | **前置：提供初始配置页签真实操作录屏/截图（B1，主文档 12.B1）**——真人展示如何在参考版增删/编辑部署记录 | 已归档至 `output/verification/legacy-user-evidence-2026-09-14/`；D3 获得真实动线依据 | ✓ | 2026-09-14 |
| 1 | 切换页签 1，比对三组图标库与关卡路由 | 三组 CHR 路由/图标条随关卡切换一致 | | |
| 2 | 比对敌军/客军部署列表与参考版同关卡 | 逐条记录一致（含行动名称显示） | | |
| 3 | 比对我方槽位重建结果 | 与参考版同关卡开场阵容一致 | | |
| 4 | 依 B1 操作路径改 1 条部署记录→保存→双方差分 | 差分区间一致 | | |
| 5 | 新增/删除部署对象各 1 例 | 行为与参考版一致；容量边界一致 | | |
| 6 | 空配置关卡（如第 1 关） | 空列表 + 明确"由关卡事件生成"语义 | | |
| 7 | 部署上限边界（敌 18/客 3/我 11） | 依 B1 操作在边界条目上改写，行为与参考版一致 | | |

## 8. 状态跟踪

| 日期 | 事件（实现/自验/用户验收/返工） | 记录 |
|---|---|---|
| 2026-09-14 | 模块文档创建（任务 10 拆分） | 依据主文档 v1.2 第 4.2/5.6/11.3 章；创建时 D3 尚未回答 |
| 2026-09-14 | 用户提供 B1 真实操作录屏 | 已归档压缩录屏与接触表；确认部署右键菜单和配置设置对话框，具体 ROM 写入仍待单变量差分 |
