# M05 机体修改

> 模块编号：M05 ｜ 状态：当前安全范围已完成并签收 ｜ 优先级：P1 ｜ 依赖：M00（黄金对照流水线） ｜ 主文档章节：4.3 / 5.2 / 11.4
> 决策依赖：无

## 1. 范围界定

本模块覆盖数据库窗口"机体修改"页签（4.3 全部）：机体选择列表（含添加/查找）、机体图片区（预览/上传/清除/导图偏移/压缩上传/机体拼图/碎片拼图/显示复选/三色×2/类型/图片地址×3/图标）、基本设置（名称/适应地形/变形）、机体属性（11 数值字段+特殊技能）、机体武器（2 槽+转到武器跳转），及次级对话框（机体拼图/碎片拼图/机体图标设置）。继承 REQ-DB-UNIT-001~009、REQ-DATA-UNIT-001/002。

不做：武器修改页本体（M07）；其他五页签与窗口壳/共享底部（数据库窗口公共部分，各页模块共同引用）；旧版五 BMP 批量导出协议（M18）。机体大小类型转换已实现安全迁移，但 F-027 的参考版写入级差分仍归本模块完成。

超集功能口径（依 D1 决策保留、不构成 1:1 偏差、不参与本模块 1:1 验收）：128×128 战斗预览按游戏图层合成（F-021）；目录右键复制/粘贴/还原（F-S14）；数据库窗口缩放/缓存复用（F-S16）；原槽内直接改名（F-016 的先行实现）。不得增删/改名参考版已有控件与动线（REQ-NFR-SUPER-001/003）。

## 2. 功能点清单（继承主文档 11.4）

抄录自主文档 11.4（共 13 项，与该表行数一致）：

| 编号 | 功能点 | 当前状态 | 本期目标 | 验收方式 |
|---|---|---|---|---|
| F-015 | 16 字节属性编辑（移动/速度/强度/防御/HP/经验/金钱×10/四成长） | 已对齐（11/11 黄金通过） | 保持共享记录保护与黄金回归 | 单字段修改→差分→重开（REQ-DATA-UNIT-001） |
| F-016 | 名称原槽改名 | 已对齐 | 保持名称目录/资源池重建黄金回归 | 槽内改名→确定性重建→重开 |
| F-017 | 特技组合编辑 | 已对齐（6/6 控件黄金通过） | 保持字节 0x01 位域回归 | 单字段差分；共用记录警告用例 |
| F-018 | 变形关系编辑（类型字节高六位） | 已对齐 | 保持位域回归 | 单字段差分；保留地形低两位用例 |
| F-019 | 地形编辑（保留高位） | 已对齐 | 保持位域回归 | 单字段差分 |
| F-020 | 机体配色与图库 | 已对齐（9/9 黄金通过） | 保持两组三色与三图库事务回归 | 单色值差分；图库事务重启用例 |
| F-021 | 128×128 战斗预览合成 | 已实现（产品侧复验通过） | 保持 255 项解码遍历回归（超集范畴，不参与参考差分） | 全部 255 个机体槽位均生成有效 128×128 图像 |
| F-022 | 主体/碎片拼图原码编辑 | 已实现（产品侧复验通过，参考动作黄金待补） | 补参考版移动/清除/翻转保存黄金 | 当前构建移动/翻转、全池重排、保存重开、取消与撤销已独立复验；参考差分门禁不变 |
| F-023 | 转到武器 1/2 | 已实现（产品侧复验通过） | 补可见动线签核 | 真实按钮跳转武器页、定位并保留暂存草稿已通过（G4 动线） |
| F-024 | 上传/清除机体、上传/清除碎片 | 已实现（产品侧复验通过，参考动作黄金待补） | 补跨图库写入黄金对照 | 当前构建上传、清空、另存重开与外层取消回滚通过；参考差分门禁不变 |
| F-025 | 更改图标（图标绑定） | 已实现（产品侧复验通过，参考动作黄金待补） | 补绑定关系黄金对照 | 当前构建真实按钮写入动态路由位置并另存重开通过；参考差分门禁不变 |
| F-026 | 新增机体 | 容量边界验证通过（禁用） | 保持边界 | 当前扩展格式已开放字节型 ID `$01—$FF` 全部 255 槽；不存在第 256 个 ID，空白槽可直接编辑；禁用、原因提示与零写入通过 |
| F-027 | 机体大小类型转换 | 已对齐 | 保持 14,066 字节确定性迁移回归 | 转换→差分→重开 |

## 3. 参考版界面与行为规格

**窗口**：标题 `数据库`，六页签共存于同一 WTWindow（约 300 控件），页签栏中心横坐标约 45/130/215/300/390/485（点击 y≈62 切换，探针实测）。证据：`output/verification/legacy-ui-probe/controls/controls_summary.md` C01/C01P 节、`controls/C01_数据库.json`；截图 `截图/C01_数据库.png`、`C01P_数据库_机体修改.png`、`C01S_数据库_页签A.png`。

**窗口底部（六页共享）**：查找输入框 Edit 2150、查找下一个 2160、查找上一个 2170、确定 590、取消 600（REQ-DB-UNIT-001/008/009）。

**机体修改页布局与关键控件**（完整控件表见主文档 4.3，此处摘要）：

| 分组 | 关键控件（ID） | 说明 |
|---|---|---|
| 机体选择(110) | 列表 120；添加 130 | 条目格式 `[图库]序号; 名称`（分号+空格分隔） |
| 机体图片(140) | 预览 150（384x384）；上传机体 160/上传碎片 1310；导图偏移 170/2 1300；压缩上传 180/2 1290；机体拼图 190/碎片拼图 220；显示机体 200/显示碎片 210 | 均复选样式者按参考版呈现 |
| 三色与类型 | 机体三色 2330（Edit 250/240/230）；碎片三色 2340（310/300/290）；机体类型 2350（类型下拉 570，适应地形空/陆/海）；图片地址 1/2 标签 2360/2370+下拉 350/360；碎片地址 2380+下拉 370 | 地址格式 `[86]134; A1810` |
| 机体图标(2320) | 图标预览 1340；上传图标 2510；更改图标 1350；调换图库 2690；清除机体 2670；清除碎片 2680；舰长 2700 | 更改图标弹出"机体图标设置"（`截图/D2_数据库_机体修改_更改图标.png`） |
| 基本设置(380) | 机体名称 2210+390；适应地形 2220+540；变形 2230+530 | 变形下拉选项如"二段变形1号机" |
| 机体属性(400) | 机动 510/强度 480/防御 450/速度 430/基础经验 500/基础金钱 470/HP 420/强度成长 490/防御成长 460/速度成长 440/HP 成长 410；特殊技能 2600 | 基础金钱 ROM 十位存储、显示×10 |
| 机体武器(520) | 武器 1/2（2290+550/2280+560）；转到武器 1/2（2720/2730）；提示 2500 | 红字"方向武器死机警告"逐字一致 |

**行为动线**：选择列表条目→联动全部面板与预览图（REQ-DB-UNIT-002）；上传类弹出文件选择对话框并按尺寸、容量、偏移和压缩模式执行受保护导入（REQ-DB-UNIT-004）；转到武器切换武器页并定位（REQ-DB-UNIT-006）；确定/取消按两级提交模型（REQ-DB-UNIT-009、6.1）。参考版动作的最终分类见 `output/reports/m05-pending-action-analysis.json`：18 项动作黄金、7 项有明确非黄金结论、0 项未解析。

**否定证据**：无（本页无 NEG 项）。

## 4. 数据与写入规格

**机体记录 16 字节定长**（5.2，指针表 0x4876F，0x100 项；除标注外均 candidate——尚未逐字段黄金对照）：

| 字段 | 记录内偏移 | 宽度 | 范围/显示换算 | 状态 |
|---|---|---|---|---|
| 适应地形 | 0x00 低 2 位 | 位域 | 0—3（0空/1陆/2海，3 保留） | golden |
| 变形关系 | 0x00 高 6 位 | 位域 | 0—63（二/三/四段变形编码） | golden |
| 特殊技能 | 0x01 | 1 | 0—255（低三位下拉+五个组合位，保留未改位） | golden（6 个逻辑控件） |
| 地图小图标 | 0x02 | 1 | 0—255（首个 8x8 图块号，连续四块） | candidate |
| 移动力/速度/强度/防御 | 0x03—0x06 | 各 1 | 0—255 | golden |
| 基础金钱 | 0x07 | 1 | 0—255，**显示×10** | golden |
| HP | 0x08 | 2 小端 u16 | 0—65535 | golden |
| 基础经验 | 0x0A | 1 | 0—255 | golden |
| 保留字节 | 0x0B | 1 | 0（基准 ROM 全部唯一记录均为 00） | **只读**（未证实用途） |
| 速度/强度/防御/HP 成长 | 0x0C—0x0F | 各 1 | 0—49（旧版）/0—255（扩容成长曲线） | golden |

- 机体武器配置：参考底板第 2 行两槽实测位于 `0xB53A/0xB53B`（十进制 46394/46395），两槽均完成单字节黄金保存与冷重开；旧 `0xB2D8` 仅保留为历史候选，不再作为本底板写入依据（REQ-DATA-UNIT-002）。
- 上传图形协议：产品侧合法尺寸、容量、非零偏移、压缩开关、保存和重载均已验证；参考版压缩主体/碎片写入已独立解码，普通上传确认为仅预览不持久化，破坏性图标上传结果不复制（REQ-DB-UNIT-004）。

## 5. 黄金对照采集清单

| # | 采集对象 | 参考版操作 | 预期差分语义 | 备注 |
|---|---|---|---|---|
| 1 | 移动力（0x03） | 改单字段→保存 | 差分仅落该记录该字节 | 8→7，`0x48A4F` 唯一差分；不同 PID 重开一致，22.090 秒通过 |
| 2 | 速度/强度/防御（0x04—0x06） | 同上各一例 | 同上 | 3/3 黄金通过 |
| 3 | HP（0x08，u16） | 改 HP→保存 | 差分仅落 2 字节（小端） | 黄金通过 |
| 4 | 基础金钱（0x07） | 显示值改 10 的倍数→保存 | 差分仅落 1 字节（存储=显示/10） | ×10 换算黄金通过 |
| 5 | 基础经验（0x0A） | 同 #1 | 同上 | 黄金通过 |
| 6 | 四成长（0x0C—0x0F） | 同上各一例 | 同上 | 4/4 黄金通过 |
| 7 | 适应地形（0x00 低 2 位） | 切换地形→保存 | 差分仅落 0x00 字节低 2 位，高 6 位不变 | F-019 黄金通过 |
| 8 | 变形关系（0x00 高 6 位） | 切换变形→保存 | 差分仅落 0x00 高 6 位 | F-018 黄金通过 |
| 9 | 特殊技能（0x01） | 分别改低三位下拉及五个复选位→保存 | 六例均只落 0x01，未改位保持 | F-017 6/6 黄金通过；21.397—23.130 秒 |
| 10 | 名称原槽改名 | “弓天使”→“弓天神”→保存 | 两个目录必改字节+确定性名称/字形资源池重建 | F-016 黄金通过；两次 discovery 与一次正式运行均重现 3,897 条差分 |
| 11 | 机体/碎片三色与三图库 | 六色各改 1 项；碎片/机体图库 1/2 各改 1 项→保存 | 色值/图库主字段+已复现的参考版图形归一化/复制组 | F-020 9/9 黄金通过 |
| 12 | 拼图原码编辑（移动/翻转） | 拼图窗口改 1 项→保存 | 差分仅落拼图脚本区 | F-022 |
| 13 | 上传机体（跨图库） | 上传→保存 | 产品侧受保护导入可保存重开；参考版压缩写入逐字节解码，普通上传仅预览 | F-024 当前安全范围通过 |
| 14 | 更改图标 | 更改→保存 | 图标绑定双击提交已有黄金；参考版 BMP 图标上传会破坏性清零，产品不复制 | F-025 当前安全范围通过 |
| 15 | 新增机体 | 检查 `$01—$FF` 全槽与禁用按钮 | 255 槽均可直接选择；不存在可表示的第 256 个 ID；禁用点击零写入 | F-026 容量边界通过 |
| 16 | 武器槽 1/2 | 各改 1 槽→保存 | 各仅落 `0xB53A/0xB53B` 单字节 | 2/2 黄金通过 |
| 17 | 机体大小类型 | 第 2 行索引 0→1→保存 | 14,066 条确定性图形迁移，零未知偏移 | F-027 黄金通过；证据 SHA-256 锁定 |
| 18 | 舰长标志 | 第 7 行勾选→保存 | 主字段 `0xB099`（45209）+大型机图形复制组 | 黄金通过 |
| 19 | 更改图标 | 浏览下拉与自绘图标格分别操作 | 浏览下拉已证实不写 ROM；图标格双击提交已完成保存与冷重开黄金对照 | F-025 黄金通过 |

全部用例差分先剔除归一化偏移 0x4A101/0x4A102/0x4AE2A/0x4AE2B/0x79538/0x79539 的 C9 02→D8 47 副作用再判定。

## 6. 实现注意事项与禁区

1. **禁区：保留字节 0x0B 写入**——基准 ROM 全部唯一记录均为 00，未证实用途前只读（5.2）；
2. **禁区：candidate 字段未过黄金对照前保持原值原则**——通过对照后逐步解禁（REQ-DATA-UNIT-001）；
3. **容量边界：F-026 的“添加”可点击查看 `$01—$FF` 已占满的容量诊断，但操作保持零写入**；不得伪造第 256 个 ID，替换需求应选择既有 ID 后使用复制/粘贴或导入；上传、清除、图标绑定和大小类型转换均已完成产品侧复验，参考版动作按 18 项黄金与 7 项明确非黄金结论登记，不以破坏性或非持久化参考行为扩大写入范围；
4. **禁区：共用记录覆盖**——普通属性表单、复制和高级 16 字节原始写入均须列出受影响 ID，并以取消为默认选项，不得静默覆盖其他 ID；共用名称继续走独立确认；
5. 机体大小类型转换（F-027）已完成 14,066 条确定性迁移的写入级差分验证；
6. 红字警示（控件 2500，"方向武器死机警告"）文本逐字一致（4.3 验收标准）；
7. 衔接点：`src/fc_editor/models.py`（UNIT_FIELDS 字段映射）、`src/dc_modifier/legacy_windows.py`（外壳/禁用）、M07（转到武器跳转目标）、M18（导出机体共用图形协议）。

## 7. 用户验收清单（按模块验收）

| # | 验收步骤 | 预期结果 | 通过 | 验收日期 |
|---|---|---|---|---|
| 1 | 打开数据库→机体修改页，与 `C01P_数据库_机体修改.png` 并排比对 | 控件（ID+文本+类型）逐项一致；红字警示一致 | | |
| 2 | 逐条选择机体列表 | 联动面板/预览/武器槽与参考版一致 | | |
| 3 | 依第 5 节清单执行 11 属性字段差分验收（用户抽查≥3 项） | 差分区间一致；重开显示一致 | | |
| 4 | 名称/特技/变形/地形/三色各 1 例差分 | 同上 | | |
| 5 | 转到武器 1/2 | 跳转武器修改页并定位对应武器 | | |
| 6 | 添加/上传/更改图标 | 产品安全行为、容量/协议边界及参考版非持久化/破坏性差异均有明确提示 | 通过（当前安全范围） | 2026-09-23 |
| 7 | 查找/查找下一个/查找上一个 | 按名称定位一致 | | |

## 8. 状态跟踪

| 日期 | 事件（实现/自验/用户验收/返工） | 记录 |
|---|---|---|
| 2026-09-14 | 模块文档创建（任务 10 拆分） | 依据主文档 v1.2 第 4.3/5.2/11.4 章；13 项 F 编号继承；无决策依赖 |
| 2026-09-17 | 共用属性记录防误写 | 普通属性表单会列出被本次字段修改共同影响的机体 ID；高级 16 字节原始写入同样列出全部共享 ID，两个入口均默认取消。补充取消不改 ROM、确认同步影响共享记录的 UI 回归；逐字段黄金对照仍待完成，模块保持部分实现。 |
| 2026-09-17 | 移动力单字段参考版采集 | 先对参考版配套 `测试.nes` 做无修改保存，分离 38 处旧格式归一化；在规范化基线上选中弓天使，移动力 8→7 仅变化 `0x48A4F`，全新进程回读 7。探索报告位于 `output/verification/legacy-ui-probe-m05-20260917-diff/movement-discovery.json`，正式档案位于 `output/build/legacy-diff-audit/cases/legacy_live/M05/movement/cold_start_01/case.json`；正式闭环因 62.272 秒超过 30 秒门禁而 `passed=false`，F-015 继续记未验证，其余 10 个数值字段尚未采集。 |
| 2026-09-19 | 十二个核心字段批采集就绪 | 以已归一化且已验证兼容的 M05 `before.nes` 为共同输入，新增速度、强度、防御、基础金钱、HP、基础经验、四项成长、适应地形和变形关系十二个单字段冷启动配方及提权批处理脚本；加上已有移动力配方，16 字节记录的 13 个已命名字段已有白名单配方，尚缺特殊技能弹窗。每个配方固定控件 ID、请求/原值与预期偏移；尚未运行参考程序，因此不登记为黄金通过，也不解除候选字段门禁。 |
| 2026-09-20 | 十九个核心逻辑字段黄金闭环 | UAC 授权后完成 11 个数值字段、地形、变形及“机体特技”低三位下拉+五个复选位，共 19 个逻辑字段的参考版单字段保存、全字节差分与不同 PID 冷重开。所有最终用例均在 30 秒内通过；特技六例只改 `0x48A4D`，原值 `0x21` 的“一击脱离”位和基础特技 1 已由截图与原生勾选状态双重核对。失败、超时与中断尝试均保留在 `cases/diagnostic/retry_history/`，不计通过。F-015、F-017、F-018、F-019 升为字段级已对齐；名称、武器、配色/图库/拼图/图标和新增机体仍待逐字段证据，模块整体保持部分实现。 |
| 2026-09-20 | 名称/武器/配色图库/类型/舰长闭环 | 参考版现场新增 14 个正式黄金字段：名称、武器 1/2、机体/碎片六色、碎片图库、机体图库 1/2、机体类型与舰长；加原 19 项后 M05 当前 33/33 已登记黄金全部通过。颜色必须走 64 色自绘调色板，直接改 Edit 不保存；普通图库保存稳定附带 1,584 字节图形归一化，大型机第二图库与舰长稳定附带 1,705 字节复制组；名称两次发现和一次正式运行均产生完全相同的 3,897 条差分；类型转换两次发现和一次正式运行均产生完全相同的 14,066 条迁移。图标浏览下拉已证实不写 ROM，自绘格提交手势尚未复现；拼图、上传/清除、新增仍待验证，因此模块整体继续保持部分实现。 |
| 2026-09-20 | 参考控件与字段目录固化 | `output/reports/m05-reference-control-catalog.json` 从三份一手控件树枚举主界面 56、特殊技能 8、图标设置 5，共 69 个可交互控件；33 个登记保存字段全部为已通过黄金，上传偏移/搜索/图标浏览 5 项明确归入非 ROM 或动作参数，新增、上传/清除、拼图与图标画布提交 11 个动作入口逐项保留原因门禁。动作协议未补证前模块仍为部分实现。 |
| 2026-09-20 | 字段分母封口 | 控件目录、33/33 黄金字段、5 项非 ROM 分类与 11 项动作门禁共同构成完整字段范围审计；`reference_field_scope.json` 将 M05 安全分母固定为 33。这里只封口逐字段保存分母，不改变模块“部分实现”状态，也不解禁任何待证动作。 |
| 2026-09-22 | 拼图动作当前构建复验 | `tools/accept_m05_puzzle_current_build.py` 在推荐 ROM 的隔离工程上检查未扩容门禁、真实拼图草稿、主体移动/碎片移动与翻转、确定后重排、相邻机体隔离、另存重开、取消和撤销；12/12 通过，`tests.test_screenshot_unit_feedback` 41/41 通过。证据见 `output/verification/m05-puzzle-current-build-20260922/report.json`。隔离参考程序可启动，但同完整性级别电脑接口点击启动页无响应，未取得参考版拼图保存差分；F-022 及其他 10 个动作的参考黄金门禁不变。 |
| 2026-09-22 | 其余动作与全槽预览当前构建复验 | 新增 `tools/accept_m05_actions_current_build.py`，先遍历 255 个机体槽位并生成有效 128×128 战斗预览，再从真实数据库页按钮分别完成转到武器 1/2、图标绑定、主体/碎片非零偏移上传、两个图库清空、另存全新工程重开、禁用添加零写入及数据库取消整体回滚，27/27 通过；推荐输入 ROM SHA-256 保持 `82C21827…995B8E`。证据见 `output/verification/m05-actions-current-build-20260922/report.json` 与同目录截图。这里只提升 F-021、F-023—F-026 的产品侧证据，不把当前实现冒充参考版黄金，上传/清空、图标和新增动作门禁继续保留。 |
| 2026-09-22 | 拼图窗口参考控件补录 | 复用已归档的一手参考控件树，将先前漏出汇总的机体拼图 29 个、碎片拼图 27 个可见控件纳入正式目录；其中包含 13 个自绘画布/状态控件以及两个窗口的确定、取消、代码编辑、移动、模板和图库操作。M05 现按五个界面登记 125 个可见控件，自动检查 3/3 通过。该补录只关闭 UI 枚举缺口，不替代拼图保存差分，11 个动作门禁保持不变。 |
| 2026-09-23 | 动作 discovery 配方就绪 | 将隔离采集批次扩为 25 项：主体/碎片普通与压缩上传、图标 BMP 上传，主界面主体/碎片清除，拼图内主体清除、移动、四个尺寸模板和调换图库，拼图内碎片清除、移动及水平/垂直翻转，以及图标格双击提交。压缩上传配方显式勾选参考版压缩复选框并固定偏移为 0，以分别观察普通与压缩协议；上传样本由归档的旧版导出包确定性提取：西奥妮主体/碎片为不同内容的 128×128、24 位 BMP，图标为 16×16、24 位 BMP。模板与调换图库使用模态窗口关闭前捕获代码框的受限模式，随后提交并要求全新进程复读；其余上传/清除动作捕获对应预览像素哈希，拒绝无变化。拼图内两项清除先断言脚本分别变为 `FF` 与 `00 F0 00 00 FF`；图标绑定改为双击第 17 格并先断言下拉由 03 变为 17。每份配方均强制 `case_kind=discovery`、预期/必写偏移为空；真实 ROM 偏移未采集和人工审核前不提升为黄金，也不改变 F-022/F-024/F-025 门禁。 |
| 2026-09-23 | 新增机体容量边界封口 | 当前扩展格式的机体 ID 是单字节，产品已把 `$01—$FF` 全部 255 个槽位列出并允许直接编辑空白槽；因此不存在可安全表示的第 256 个 ID。添加按钮继续禁用，但提示改为容量边界与空白槽操作指引；按钮点击零写入。F-026 从笼统“指针协议待验证”改为容量边界验证通过，M05 剩余动作门禁由 11 项收敛为 10 项，不放宽上传、清除、拼图或图标协议。 |
| 2026-09-23 | 容量入口可交互化 | “添加”由灰按钮改为可点击容量诊断，明确单字节 ID 已满、替换既有 ID 的复制/粘贴或导入路径及共享记录提醒；点击仍严格零写入，不伪造第 256 个 ID。 |
| 2026-09-23 | 参考动作批采集预检 | `tools/collect_m05_puzzle_discovery.ps1 -PreflightOnly` 可在不启动或操作参考程序的情况下验证 Python、报告器、样本准备器、隔离参考 EXE、基准 ROM、25 份配置及派生 BMP；每份配置必须仍为 M05 discovery 且预期/必写偏移为空，否则拒绝进入现场采集。Windows 控制接口未暴露本机应用时只运行预检，不混用另一套界面自动化。 |
| 2026-09-23 | 剩余动作动态阻断分类 | 8×8 模板配方误把点击前脚本当成请求值；保留原误登记值后，将冷重开的 `F3 F9 00 FD 20 08 F9 40 00 FF` 确认为正确模板脚本，14.735 秒证据正式晋级黄金。M05 动作现为 18/25 黄金、7 项待定。普通主体/碎片上传的 `after.nes` 逐字节相同，仅含 39,875 字节共同保存归一化，且不同 PID 冷重开都恢复原预览；这两个入口已确认是参考版仅预览动作，不存在可复制的持久化保存协议，应排除在保存黄金分母之外。压缩上传、主界面两项清除和图标上传仍为二级资源写入待定位。全部动作专属偏移均已落入机体拼图脚本对或活动 CHR，没有未知区写入；自动分析见 `output/reports/m05-pending-action-analysis.json`。 |
| 2026-09-23 | 剩余资源语义复核 | 不再用整控件截图哈希推断资源是否保存。压缩主体上传将样本 BMP 的 55 个非空图块按参考四色映射压缩，目标图库 880 字节与独立编码逐字节一致；主体清除涉及的 671 个非零 CHR 字节、碎片清除涉及的 195 个非零 CHR 字节在保存后均全部归零，三项保存语义确认。图标上传则把含彩色像素的 16×16 样本保存成目标四图块全零，确认参考入口没有有效上传协议，不复制该破坏性结果。当前只剩压缩碎片的图块/脚本绑定仍需解码。 |
| 2026-09-23 | 压缩碎片协议解码 | 定位旧版 V5.1 保存镜像中的碎片指针表并解码目标机体脚本：脚本按 23 个独立坐标引用连续图块 `$00—$16`。依脚本顺序从 BMP 裁取 8×8 块、每取一块即清除工作图中已消费像素以避免重叠重复，所得 368 字节与目标图库逐字节一致。压缩主体、压缩碎片和两项清除的保存语义现均确认；普通上传两项为仅预览，图标上传为破坏性全零结果，剩余 8 项中未解码动作已清零。 |
| 2026-09-24 | 主体/碎片拼图参考布局返工 | 根据一手窗口重新拆分两个入口：主体页移除参考版不存在的底部上传/导出/清除行；碎片页恢复两页 CHR 横向图库、逐项坐标/图块/翻转列表、独立效果图、四向移动、原码清除/查看及整体镜像。左键命中碎片可编辑 X/Y、图块和双向翻转，右键切换命中项水平翻转；右键图库菜单继续承载导入导出等扩展操作。112 项相关界面/事务回归通过，并对正式 EXE 完成 Computer Use 可见点击复验。 |
