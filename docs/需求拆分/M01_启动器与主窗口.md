# M01 启动器与主窗口

> 模块编号：M01 ｜ 状态：部分实现（2026-09-19 ROM 优先启动动线已实现并通过离屏自验，待用户复验；旧版打开/保存签收记录保留为历史证据） ｜ 优先级：P1（打开/保存两级提交签核） ｜ 依赖：M00（黄金对照流水线） ｜ 主文档章节：4.1 / 4.2 / 6.1 / 6.2 / 11.1
> 决策依赖：无（NEG-3/C-5 备份证据冲突的复测属证据补证，不构成决策项）

## 1. 范围界定

本模块覆盖：启动器窗口（作者信息+单一“打开ROM”按钮，不参考截图）；启动页拖入单个 `.nes`、把 `.nes` 拖到 EXE 图标上的启动参数路径；主窗口壳层（菜单栏 14 项、内部未载入/载入两态、打开/保存动线、两级提交保存模型）；三页签工作区容器与共享关卡选择列表（页签内容分别归 M02/M03/M04）；标题栏 ROM 路径追加规则。正常启动不再把用户带入空主窗口。

不做：三页签内部功能（M02/M03/M04）；数据库等"数据"菜单子窗口（M05—M18）；"导出机体/导出头像"动线细节（M18）。

超集功能口径（依 D1 决策保留、不构成 1:1 偏差、不参与本模块 1:1 验收）：未保存提示/派生 ROM 输出/覆盖前时间戳备份（F-S15，REQ-BEH-SUPER-001）；"扩展功能""工程"超集菜单组及工程快捷键——.dcmod 工程系统（F-S04）、撤销/重做 Ctrl+Alt+Z/Y（F-S05）、F7 变更与验证完整检查（F-S06）、IPS/构建报告 Ctrl+B 一键构建（F-S07）、完整 ROM 数据读取·七类全表+全量 HEX（F-S09，"扩展功能"菜单入口）、扩展容量规划·地图/机体/剧情分区（F-S10）（主文档 11.18）。约束：参考版已有的控件、菜单项与动线不得因超集功能而增删、改名或改变触发方式（REQ-NFR-SUPER-001）。

## 2. 功能点清单（继承主文档 11.1）

| 编号 | 功能点 | 当前状态 | 本期目标 | 验收方式 |
|---|---|---|---|---|
| F-001 | 启动器（作者信息+打开 ROM，不参考截图） | 已实现（新动线待用户复验） | 作者署名 `断月残心`、无链接/QQ，仅保留“打开ROM” | 启动器文字规格、拖放和成功载入后 HWND 销毁现场核对 |
| F-002 | 打开 ROM（启动按钮/启动页拖入/EXE 图标拖入/Ctrl+O） | 已实现（旧 Ctrl+O 动线已签收；新启动动线待复验） | 载入身份校验是保留超集 | 离屏 GUI 回归+新动线现场复验 |
| F-003 | 保存（两级提交+直接覆盖） | 部分实现（两版保存/不保存重开现场证据已审计；D1 无规划派生 ROM 重开离屏通过；用户已签收） | D1 首次另选派生 ROM；连续备份唯一；参考版双环境无自动备份 | 两级提交录屏 + 差分审计 |
| F-004 | 主窗口菜单（文件/数据/帮助 3 组 14 项） | 已实现（两版 14 项现场证据及 B03 探针；用户已签收） | 超集命令归入扩展功能/工程菜单 | `controls/B03_完整菜单树.json` 与现场菜单录屏 |

注：当前为 ROM 优先精简启动页（作者+“打开ROM”按钮）；参考数据菜单 10 项均有入口，另含超集菜单组（超集项不参与验收）。
当前实现已将超集“完整ROM数据读取”移至“扩展功能”，参考数据菜单保留 10 个命令项；文件 3、帮助 1，共 14 个参考命令。Qt 菜单项携带 B03 对应逻辑 ID，主窗口接受指定 ID 的 Win32 `WM_COMMAND` 并分发到动作；真实原生菜单命令树仍需交互桌面探针复测。

## 3. 参考版界面与行为规格

**启动器**（本产品自定义，**不参考** `截图/A01_启动器.png`，不复刻参考版 8 控件布局）：仅含作者署名区与“打开ROM”按钮。作者署名为 `断月残心`；不含任何外部链接按钮（贴吧/公共网盘/资料集/修改器更新查看 4 链接全部移除）；不含 QQ、QQ 群等联系方式信息。

- REQ-L-001：点击“打开ROM”选择单个 `.nes`；只有载入成功后启动器才**销毁**（非隐藏）并显示主窗口。取消或载入失败保留启动器。
- REQ-L-002：作者署名显示为 `断月残心`。
- REQ-L-003：启动器不含任何外部链接按钮，不含 QQ/QQ 群信息；本模块启动器不参与对参考版截图的 1:1 比对。
- REQ-L-004：启动页接受拖入一个本地 `.nes`，与“打开ROM”共用校验/载入逻辑；多文件或其他扩展名不接受。
- REQ-L-005：把单个 `.nes` 拖到 Windows EXE 图标上时，程序解析该唯一 `.nes` 启动参数并直接载入；无效 ROM 不跳过既有安全校验。

**主窗口**（控件树 A02/B02 节，63 控件）：标题 `SRW2扩容版修改器V1.0`；载入后追加全角冒号+ROM 绝对路径（REQ-MW-003；`截图/B01_打开ROM对话框.png`、`截图/B02_主窗口_载入后.png`）。

**菜单栏**（唯一权威 `controls/B03_完整菜单树.json`，主文档 4.2.1 表展开）：

| 菜单 | 菜单项 | 快捷键 | 命令 ID |
|---|---|---|---|
| 文件(F) | 打开 | Ctrl+O | 20001 |
| | 保存 | Ctrl+S | 20004 |
| | 退出 | Ctrl+X | 20006 |
| 数据(A) | 数据库 | Ctrl+D | 20008 |
| | 文字库 | Ctrl+W | 20009 |
| | 地图动画 | Ctrl+M | 20011 |
| | 文字转换 | Ctrl+Z | 20013 |
| | 剧情事件 | Ctrl+J | 20015 |
| | 导出机体 | Ctrl+F | 20017 |
| | 导出头像 | Ctrl+L | 20018 |
| | 属性计算器 | （无） | 20020 |
| | 存档修改器 | （无） | 20021 |
| | 其他 | Ctrl+T | 20023 |
| 帮助(H) | 关于 | （无） | 20025 |

数据菜单各子项对应目标模块：数据库（M05—M10）、文字库（M11）、地图动画（M12）、文字转换（M13）、剧情事件（M14）、导出机体/头像（M18）、属性计算器（M15）、存档修改器（M16）、其他（M17）。

- REQ-MW-001：未载入 ROM 时"数据(A)"整组隐藏。
- REQ-MW-002：快捷键与命令 ID 按上表实现（命令 ID 用于自动化验收 WM_COMMAND 定位）。
- REQ-MW-004：保存（Ctrl+S/菜单）直接写回当前 ROM 文件，无文件名选择对话框、无确认弹窗（【否定证据】无未保存提示，NEG-2）。

**三页签工作区**（REQ-MW-010）：CPageControl(120) 三页签"战场地图/初始配置/商店事件"，页签切换采用点击页签标题方式（参考版为子容器显隐模型）；左下共享"关卡选择"列表（关卡列表 100，条目如 `001：伏击之战`）。

**未载入/载入两态**（REQ-MW-001/003 联动）：两态仍是主窗口内部状态，但正常启动路径只在 ROM 载入成功后显示主窗口，不再向用户暴露空主窗口。

| 状态 | 菜单栏 | 标题栏 | 工作区 |
|---|---|---|---|
| 未载入 | 仅"文件(F)+帮助(H)"（"数据(A)"整组隐藏） | `SRW2扩容版修改器V1.0` | 页签区可见但无关卡数据 |
| 载入后 | 三组 14 项全量 | 追加全角冒号+ROM 绝对路径 | 关卡列表载入条目（如 `001：伏击之战`），三页签可编辑 |

**保存模型**（6.1，两级提交）：编辑草稿层（子窗口确定前均为草稿）→ 提交缓冲层（确定→内存 ROM 缓冲；取消→丢弃）→ 落盘层（仅主窗口 Ctrl+S 写回文件，直接覆盖无确认）。REQ-BEH-001~004；例外：文字库只有确定、文字转换/属性计算器无确定取消、存档编辑器独立四阶段。

**否定证据**：NEG-2（无未保存提示）、NEG-3（无自动备份——存在证据冲突 C-5，见第 6 节）。

## 4. 数据与写入规格

本模块无直接 ROM 可写字段（打开/保存为文件级操作）。写入语义约束：

| 事项 | 规格 | 状态 |
|---|---|---|
| 保存方式 | 直接写回当前 ROM 文件，无确认弹窗、无备份（REQ-BEH-002） | confirmed（动线规格+审计脚本实测） |
| 保存往返 | 保存后重开，显示值必须等于已保存值（黄金对照第 4 步语义） | confirmed（#19 模板区已验证） |
| 标题追加 | 载入后 `SRW2扩容版修改器V1.0：<ROM绝对路径>`（全角冒号） | confirmed（控件树 B02） |
| 归一化排除 | 保存不得执行 6 偏移 C9 02→D8 47 归一化（REQ-BEH-EX-001） | confirmed |
| 超集保存行为 | 派生 ROM 输出/未保存询问/时间戳备份保留（REQ-BEH-SUPER-001） | 超集口径（D1） |

实现侧依 D1 保留超集安全路径：第一次“文件→保存”选择派生 ROM；之后直接保存到该派生文件、不再选名；覆盖前建立唯一时间戳 `.bak`，当前载入 ROM（包括可写工作副本）不原地覆盖，`references/` 始终只读。参考版“直接写回/无自动备份”是黄金行为基线，但不作为移除 D1 的依据；C-5 仍按双环境复测，不能据派生保存往返撤销 NEG-3 或签核参考动线。

## 5. 黄金对照采集清单

| # | 采集对象 | 参考版操作 | 预期差分语义 | 备注 |
|---|---|---|---|---|
| 1 | 保存往返（往返一致性） | 载入→改任一模板区字段→确定→保存→关闭→重开读值 | 重开值=写入值（差分仅该字段+归一化） | 可复用 #19 审计脚本 |
| 2 | 纯打开无写入 | 载入→不修改→直接保存 | 差分仅 6 个归一化偏移 | 验证 NEG-8 副作用边界 |
| 3 | 关窗丢弃 | 载入→改字段→直接关窗（不点确定） | 差分为空（草稿丢弃） | NEG-2 动线佐证 |
| 4 | 备份行为（探针环境） | 保存前后对比工作目录文件列表 | 无新增备份文件 | NEG-3 复测 |
| 5 | 备份行为（常规环境） | 常规桌面环境重复 #4 | 两种环境结论一致或复现 `自动备份/YYYY年M月D日/H时M分S秒原文件名` | C-5 冲突复测；复现则撤销 NEG-3 并按参考版实现 |
| 6 | 菜单命令触发（自动化） | WM_COMMAND 按命令 ID 逐项触发 | 各子窗口按预期打开/关闭 | 探针 real_click/snapshot 范式 |
| 7 | 退出菜单 | Ctrl+X/菜单退出 | 主窗口关闭行为与参考版一致 | 动线签核用例 |

全部用例差分先剔除归一化偏移 0x4A101/0x4A102/0x4AE2A/0x4AE2B/0x79538/0x79539 的 C9 02→D8 47 副作用再判定。

## 6. 实现注意事项与禁区

1. **禁区：保存加确认弹窗/自动备份**（REQ-BEH-002，NEG-2/NEG-3）——但超集安全行为（未保存询问/派生 ROM/时间戳备份）依 D1 保留（REQ-BEH-SUPER-001），二者并存时不得干扰参考版动线执行结果（REQ-NFR-SUPER-003）；
2. **禁区：未载入时显示"数据"菜单**（REQ-MW-001）；
3. **禁区：标题半角冒号或省略路径**（REQ-MW-003）；
4. 启动器成功打开 ROM 后必须销毁窗口（HWND 失效，探针需重新枚举）；取消或失败时不销毁；
5. C-5 处理原则：探针环境按否定证据处理（不实现自动备份）；若 #5 用例复现则按参考版目录格式实现并撤销 NEG-3（主文档 5.11）；
6. 衔接点：`src/dc_modifier/` 主窗口与启动器代码（启动器保持“作者署名+打开ROM”精简布局，支持启动页/EXE 图标两种拖放，不扩展外部链接/QQ 群信息区、不参考截图）。

## 7. 用户验收清单（按模块验收）

| # | 验收步骤 | 预期结果 | 通过 | 验收日期 |
|---|---|---|---|---|
| 1 | 启动程序，查看启动器（不参考截图） | 仅作者署名 `断月残心`+"进入修改器"按钮；无外部链接、无 QQ/QQ 群信息 | 签收；现场录屏已审计 | 2026-09-16 |
| 2 | 点击"进入修改器" | 启动器销毁，主窗口出现；未载入仅"文件+帮助"菜单 | 签收；现场录屏已审计 | 2026-09-16 |
| 3 | Ctrl+O 打开 ROM | 系统打开对话框；标题追加全角冒号+绝对路径；"数据"菜单出现 | 签收；两版现场录屏已审计 | 2026-09-16 |
| 4 | 任意子窗口改一字段→确定→关窗不保存→重开 | 重开无该修改（两级提交签核） | 签收；两版不保存重开已审计 | 2026-09-16 |
| 5 | 任意子窗口改一字段→确定→Ctrl+S→重开 | 重开显示=写入值 | 有条件；两版重开通过，新版依 D1 保存派生 ROM | 2026-09-16 |
| 6 | 菜单逐项核对 | 与 B03 JSON 14 项一致（命令 ID/快捷键） | 签收；两版菜单与 B03 证据已审计 | 2026-09-16 |
| 7 | 修改后直接关主窗口 | 与参考版行为一致（超集未保存询问保留，不阻断） | 签收；现场关闭/重开已审计 | 2026-09-16 |

用户于 2026-09-16 确认本模块已签收。字段级黄金档案未补齐，D1 派生保存是保留的有意差异，故模块按遗留项登记为“有条件通过”，不升级为“已对齐”。

上述第 1—3 项签收对应旧“进入修改器 → 空主窗口 → Ctrl+O”动线。2026-09-19 用户明确改为 ROM 优先启动后，旧证据只保留为历史记录，不能替代下列变更复验。

### 7.1 2026-09-19 ROM 优先启动变更复验

前置条件：使用包含本次改动的最新构建；准备推荐 ROM `output/rom/DC_kuorong_464K.nes`，并准备一个非 `.nes` 文件用于拒绝测试。

| # | 验收步骤 | 预期结果 | 证据/问题记录 | 用户结论/日期 |
|---|---|---|---|---|
| L-01 | 正常双击 EXE | 启动页只显示作者署名和“打开ROM”；不存在“进入修改器” |  |  |
| L-02 | 点“打开ROM”后取消文件选择 | 仍停留在启动页，不创建空主窗口 |  |  |
| L-03 | 点“打开ROM”并选择推荐 ROM | 校验通过后启动页销毁，主窗口直接显示真实关卡、图块和 ROM 路径 |  |  |
| L-04 | 把一个推荐 `.nes` 拖入启动页 | 与 L-03 相同，直接打开该 ROM |  |  |
| L-05 | 向启动页拖入非 `.nes` 或同时拖入两个文件 | 不接受拖放，启动页保持可用 |  |  |
| L-06 | 在 Windows 资源管理器把推荐 `.nes` 拖到 EXE 图标 | 启动程序并直接打开该 ROM，不需要再次选择 |  |  |
| L-07 | 通过按钮或 EXE 图标传入损坏/不兼容 `.nes` | 显示原有校验错误，启动页保留，可重新选择 |  |  |

最终签收：`□ 通过  □ 有条件通过  □ 不通过`；签收人/日期：________；关联问题：________。

## 8. 状态跟踪

| 日期 | 事件（实现/自验/用户验收/返工） | 记录 |
|---|---|---|
| 2026-09-14 | 模块文档创建（任务 10 拆分） | 依据主文档 v1.2 第 4.1/4.2/6/11.1 章 |
| 2026-09-14 | 启动器需求调整 | 启动器不参考截图；移除 4 个外部链接与 QQ/QQ 群信息；作者署名改为 `断月残心` |
| 2026-09-15 | 实现侧续做与离屏自验 | 启动器进入时销毁；未载入态显示禁用的三页签空壳、关卡列表为空；参考菜单 14 命令与全部 B03 分隔 ID 整理、超集读取移出数据菜单、关于文字修正，支持按 ID 分发 `WM_COMMAND`；依 D1 保留派生 ROM 首次选名、后续保存与只读参考路径，连续备份不互相覆盖；标题不再添加未保存星号。派生保存→重开与合成 `WM_COMMAND` 离屏测试通过；B01/B02 原生窗口、G4 动线与 C-5 备份现场复测未完成，用户验收空白。 |
| 2026-09-15 | 用户截图反馈与空状态修正 | 用户截图标题无 ROM 路径、仅文件/帮助菜单、0 个地图，确认编辑按钮禁用属于 REQ-MW-001 未载入态；保留不自动载入。未载入时底部提示 `Ctrl+O`/文件→打开，画布不再绘制假绿色格，图块/笔刷改为中性空位；载入后仍从当前 ROM 渲染实际图库与地图。原生打开/保存动线和用户签核尚未完成，M01 状态不升级。 |
| 2026-09-15 | 派生 ROM 重开故障修正 | 用户截图显示无自动容量表的普通派生 ROM 被基准哈希门禁误拒绝；旧“派生保存→重开离屏通过”仅验证 `RomProject.load`，未走主窗口 `load_rom`，现补全新窗口回归。新 DC 扩容无规划输出须通过固定代码认证、预留区全零及完整性校验；带规划输出继续按规划校验。未收到用户工作 ROM 本体，实际文件与原生现场仍待验，用户验收栏不代填。 |
| 2026-09-15 | 用户现场录屏——启动/打开部分覆盖 | 用户指定原名 `M02.mp4` 的 66.97 秒录屏回答 M01；归档为 `output/verification/manual-acceptance-2026-09-15/m01-manual-validation.mp4`。画面可确认新版已载入地图、关闭回到启动器、重新进入未载入主窗口、打开系统文件对话框并载入地图。未覆盖参考版同流程、14 项菜单逐项核对、两级提交两条完整路径及双环境备份观察，因此只作为 H-02 部分证据，不代填第 7 节签核。 |
| 2026-09-15 | 用户补充两段参考版录屏 | 归档 `m01-supplement-01.mp4`（92.95 秒）与 `m01-supplement-02.mp4`（17.11 秒）。画面覆盖参考版打开/标题绝对路径、文件 3+数据 10+帮助 1 共 14 项菜单及快捷键、修改地图后保存、关闭并在全新窗口打开 `M01-参考版-after.nes` 后修改仍存在；结合首段新版录屏，两版“保存后重开”路径均有现场画面。仍缺新版 14 项菜单展开、两版“不保存后重开”路径和保存目录/连续备份观察；不代填第 7 节用户签核。详见 `output/verification/manual-acceptance-2026-09-15/README.md`。 |
| 2026-09-15 | 用户第三段补充录屏——M01 现场材料基本齐备 | 245.85 秒录屏归档为 `m01-supplement-03.mp4`。新版数据 10 项、帮助 1 项已展开，文件的打开/保存/关闭动作已记录；新版未应用地图草稿关闭重开后消失，连续保存目录由 14→15→16 个文件并生成不同时间戳 `.bak`。参考版修改后不保存重开恢复，常规桌面未产生自动备份，与探针 NEG-3 一致。尚缺新版文件菜单稳定展开 3 项，以及新版“先应用到项目、再不 Ctrl+S 关闭重开”的严格两级提交路径；第 7 节与 H-06 不代填。 |
| 2026-09-15 | 用户第四段补充录屏——M01 现场证据收齐 | 64.92 秒录屏归档为 `m01-supplement-04.mp4`。新版文件菜单稳定显示打开/保存/退出及快捷键；地图变化后点击“应用地图、部署与事件”，状态确认已更新项目，未 Ctrl+S 即关闭，全新进入并重开同一 ROM 后变化消失。至此 H-02/H-03 现场证据通过审计，连同既有 B03 探针覆盖第 7 节各项所需材料；但用户尚未明确最终验收结果，第 7 节通过栏与 H-06 仍不代填。 |
| 2026-09-16 | 用户验收签收 | 用户确认 M00—M02 已签收；M01 第 7 节依据 `output/verification/manual-acceptance-2026-09-15/` 既有证据逐项登记，按 D1 差异及字段黄金档案缺口“有条件通过”。未在本轮重跑界面测试，产品状态仍为“部分实现”。 |
| 2026-09-19 | ROM 优先启动返工 | 按用户新要求移除启动页“进入修改器”，改为唯一“打开ROM”；点击选择、启动页单 `.nes` 拖放及把 `.nes` 拖到 EXE 图标（启动参数）均复用同一校验/载入路径。取消、非 `.nes`、多文件或载入失败不会进入空主窗口；只有成功载入才销毁启动页，失败提示归属于仍可见的启动页。新增按钮、取消、拖放过滤、成功/失败载入、EXE 参数及窗口生命周期离屏回归，7/7 通过；旧启动步骤签收不覆盖新动线，第 7.1 节待用户复验。 |
