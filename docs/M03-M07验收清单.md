# M03—M07 研发验收清单

> 创建日期：2026-09-18 ｜ 验收对象：M03 初始配置、M04 商店事件、M05 机体修改、M06 人物修改、M07 武器修改
>
> 本清单是本轮研发成果的集中验收入口。详细字段、地址、黄金对照和禁区仍以 `docs/需求拆分/M03_初始配置.md` 至 `M07_武器修改.md` 第 5—8 节及 `docs/SRW2修改器1比1复刻需求文档.md` 为准。勾选本清单只能证明当前交付范围通过人工验收；未完成正式黄金对照的功能不得据此直接标为“已对齐”。

## 1. 当前交付边界

| 模块 | 当前状态 | 本次允许验收 | 本次不能判定通过的范围 |
|---|---|---|---|
| M03 初始配置 | 部分实现 | 三组图标路由、部署列表、地图图标、我方槽位推导、悬停说明、空表语义、只读门禁 | 部署编辑、新增、删除、拖动和保存写回；D3 黄金差分完成前保持禁用 |
| M04 商店事件 | 部分实现 | 事件/商店列表、地图标记、坐标定位、空表语义、无效商店号说明、只读门禁 | 事件/商店编辑、新增、删除和保存写回；D3 黄金差分完成前保持禁用 |
| M05 机体修改 | 部分实现 | `$01—$FF` 机体读取、现有属性/名称/武器/配色/图库/图标/拼图编辑、共享记录确认、事务与保存重开 | 新增机体仍禁用；各字段仍待正式黄金对照，移动力现有正式样本超过 30 秒门禁 |
| M06 人物修改 | 实现完成，待黄金对照与用户验收 | `$01—$C8`、双名称、音乐、属性、精神、头像、战斗/变形台词、共享记录保护、容量门禁、保存重开 | 新增 `$C9` 因三固定池已满而拒绝；字段级“已对齐”结论仍需黄金档案 |
| M07 武器修改 | 当前底板实现完成，待黄金对照与用户验收 | `$01—$FF`、名称与属性、敌我动画、使用机体反查、代码/规律入口、Mesen 测试、共享记录保护、容量门禁 | 第 256 件武器和独立动画脚本新增不在当前容量内；脚本搬移归 M12 |

## 2. 验收准备

| 编号 | 操作 | 通过标准 | 结果 | 备注/证据 |
|---|---|---|---|---|
| A-01 | 完全退出旧修改器进程，启动 `output/app/新DC篇完整修改器.exe` | EXE SHA-256 为 `CB075CA7275B213938FD15546807C27CC6CCB8FC899B197CCBBB92E646A37775` | [ ] | |
| A-02 | 从 `output/rom/DC_kuorong_464K.nes` 复制一份独立工作 ROM，再用 `Ctrl+O` 打开副本 | 基线 SHA-256 为 `82C218275459D53C0F306F6BC036C4797316976E0FA7AD1D5A8247E338995B8E`；不直接覆盖推荐 ROM | [ ] | |
| A-03 | 建立证据目录，如 `output/verification/manual-acceptance-m03-m07-20260918/` | 记录操作系统、EXE/ROM 哈希、修改器截图或录屏、保存前后 ROM 和失败提示 | [ ] | |
| A-04 | 每个会写入的模块使用独立 ROM 副本；参考 EXE、参考 ROM 和 `references/` 只读 | 一个用例失败时不会污染下一用例；`references/` 无改动 | [ ] | |
| A-05 | 打开主窗口后连续切换前 12 个关卡，再往返两个有明显部署数量差异的关卡 | 无秒级冻结；标题、部署、图标和地图不会残留上一关数据 | [ ] | |

## 3. M03 初始配置

> 本轮按 D3 验收只读范围。编辑控件被禁用是预期结果，不应登记为缺陷；部署写入不纳入通过结论。

| 编号 | 验收步骤 | 预期结果 | 结果 | 备注/证据 |
|---|---|---|---|---|
| M03-01 | 进入“初始配置”，分别选择第 1、4、28 关 | 三组图标地址随关卡变化；已知路由分别包含 `$34/$35/$36`、`$34/$35/$3A`、`$46/$47/$3C` | [ ] | |
| M03-02 | 展开三组机体图标库，再收起 | 展开后显示真实两行图标；收起后不再生成整页预览，地图仍可操作 | [ ] | |
| M03-03 | 选择有敌军/客军记录的关卡，核对部署明细 | 敌/客记录按 X、Y、驾驶员、机体、等级、行动显示，列表数量与地图标记一致 | [ ] | |
| M03-04 | 选择部署对象并在地图与列表间联动 | 列表选择能定位地图图标；地图选择能定位对应列表项；悬停显示人物、机体、成长属性、武器和行动说明 | [ ] | |
| M03-05 | 核对我方出击位 | 第三字段按队伍槽解释；能从初始阵容和此前事件推导的槽位显示真实机体图标，空槽不伪造数字机体 | [ ] | |
| M03-06 | 选择第 1 关等空配置表 | 显示“由关卡事件生成”语义，无报错、无虚构部署 | [ ] | |
| M03-07 | 尝试表格编辑、弹窗保存、增删或拖动部署 | 写入入口禁用或不提交，工程不变脏；只读查看、选择和悬停仍可用 | [ ] | |

## 4. M04 商店事件

> 本轮同样按 D3 验收只读范围。新增、删除、编辑和写回须等逐字段黄金差分完成后另行验收。

| 编号 | 验收步骤 | 预期结果 | 结果 | 备注/证据 |
|---|---|---|---|---|
| M04-01 | 进入“商店事件”，选择一个有事件/商店记录的关卡 | 左侧记录数量与地图上的紫色“事”/绿色“店”标记一致 | [ ] | |
| M04-02 | 单击一条事件或商店记录 | 地图定位到该记录 X/Y 坐标，列表与地图保持双向联动 | [ ] | |
| M04-03 | 选择第 1 关等空表关卡 | 显示无记录语义，无报错、无虚构标记 | [ ] | |
| M04-04 | 打开已有记录的设置窗口 | 能查看原始坐标、人物限定及事件/商店编号；确定、删除和写回能力保持禁用 | [ ] | |
| M04-05 | 核对商店编号边界 | F0—F4 与 FF 按现有有效语义显示；F5—FE 显示指向地图触发表的无效原因并禁止选择/写入 | [ ] | |
| M04-06 | 在只读状态切换关卡并关闭修改器 | 不产生未保存修改提示；ROM 字节不变化 | [ ] | |

## 5. M05 机体修改

| 编号 | 验收步骤 | 预期结果 | 结果 | 备注/证据 |
|---|---|---|---|---|
| M05-01 | 打开“数据→数据库→机体修改”，搜索并切换多个机体 | `$01—$FF` 可读取；名称、属性、两武器、机体/碎片预览和地址随选择正确联动 | [ ] | |
| M05-02 | 在独立副本中修改移动、速度、强度、防御、HP、基础经验、基础金钱和一项成长 | 输入范围正确；基础金钱按显示值÷10写入；暂存后切换记录再返回，值仍保留 | [ ] | |
| M05-03 | 修改地形、变形和特殊技能组合 | 地形只改低两位且保留变形高位；变形只改高六位；组合技能不会丢失未改位 | [ ] | |
| M05-04 | 修改名称、机体/碎片各一色及图片地址 | 预览即时更新；名称超出原槽或资源超容时拒绝写入并保留表单 | [ ] | |
| M05-05 | 选择共用属性记录，先在影响确认中取消，再确认一次 | 弹窗列出全部受影响机体并默认取消；取消时 ROM 不变，确认后所有共享 ID 同步变化 | [ ] | |
| M05-06 | 打开机体拼图/碎片图库，编辑一个图块或移动/翻转一项拼图后确定 | 子窗口草稿正确合成；确定后回到数据库仍可取消整窗事务；非法脚本或资源超容会拒绝 | [ ] | |
| M05-07 | 更改一个机体图标并编辑一块真实 CHR 像素 | 图标按关卡三图库路由显示；绑定和像素预览正确，数据库取消能恢复 | [ ] | |
| M05-08 | 点击“转到武器 1/2” | 切换到武器页并定位对应武器 | [ ] | |
| M05-09 | 检查“新增机体” | 入口保持禁用或明确拒绝，不会擅自移动未验证指针表 | [ ] | |
| M05-10 | 记录一组改动，分别验证数据库“取消”和“确定→保存派生 ROM→全新进程重开” | 取消完整回滚；确定并保存后，重开仍读回全部确认值 | [ ] | |

## 6. M06 人物修改

| 编号 | 验收步骤 | 预期结果 | 结果 | 备注/证据 |
|---|---|---|---|---|
| M06-01 | 打开人物页并检查首尾记录 | 列表完整覆盖 `$01—$C8` 共 200 槽；`$C8` 明确没有战斗名称/音乐槽，不越界读取 | [ ] | |
| M06-02 | 对同一人物分别修改“名称”和“战斗名称” | 两字段独立写入；修改其中一个不会改变另一个；切换人物后草稿仍保留 | [ ] | |
| M06-03 | 修改双方战斗音乐、精神值、精神成长、五项补正和“击落不消失” | 控件可用、值可读回；未改字段和保留位不变化 | [ ] | |
| M06-04 | 勾选/取消一项精神并修改一项全局精神消耗 | 精神掩码只影响所选人物；消耗明确为全局值，还原单个人物不会误重置全局消耗 | [ ] | |
| M06-05 | 修改头像三色、正面/背景图库，并暂存一次 32×32 BMP/PNG | 头像预览与图库联动；上传保持窗口草稿，取消数据库后 CHR 完整恢复 | [ ] | |
| M06-06 | 对共享属性、头像、名称或战斗台词先取消影响确认，再确认一次 | 提示列出所有受影响人物且默认取消；取消零写入，确认后共享引用者同步变化 | [ ] | |
| M06-07 | 编辑一次攻击/二次攻击/三次攻击规则，以及攻击命中、攻击受阻和六项防御台词中的至少一项 | 只改既有规则或直接绑定；共享记录确认生效；切换人物后可读回 | [ ] | |
| M06-08 | 在“变形起飞”中添加或清空一条人物/机体范围/05 段对话编号 | 仅在固定 15 项容量内重排；不移动后续全局数据；超容时明确拒绝 | [ ] | |
| M06-09 | 尝试新增 `$C9` 或制造名称/属性/头像池超容 | 明确提示三固定池或具体池容量不足；整个操作原子失败，已有数据不变 | [ ] | |
| M06-10 | 验证数据库取消，以及确定→保存派生 ROM→全新进程重开 | 取消完整回滚；确认后的双名称、属性、头像和台词逐项保持 | [ ] | |

## 7. M07 武器修改

| 编号 | 验收步骤 | 预期结果 | 结果 | 备注/证据 |
|---|---|---|---|---|
| M07-01 | 打开武器页并检查首尾记录 | 列表完整覆盖 `$01—$FF` 共 255 槽；`$FF` 是可选择、可编辑的真实槽位 | [ ] | |
| M07-02 | 修改名称、射程、命中、距离补正、对空/陆/海火力和武器特技 | 显示值可读回；射程/特技共字节及距离补正保留位互不覆盖 | [ ] | |
| M07-03 | 对共享六字节属性记录先取消影响确认，再确认一次 | 提示列出全部受影响武器且默认取消；取消零写入，确认后共享 ID 同步变化 | [ ] | |
| M07-04 | 分别打开我方/敌方武器动画，修改一个允许的等长参数 | 两种模式独立；差分只落对应脚本参数；增删指令或变长写入被拒绝 | [ ] | |
| M07-05 | 修改共享动画脚本时先取消、再确认 | 弹窗列出共用该物理脚本的全部武器；取消不写入，确认后作为同一事务提交 | [ ] | |
| M07-06 | 打开“使用这个武器的机体”并执行双向跳转 | 使用列表正确；能跳到机体页并定位记录，再从机体武器返回该武器 | [ ] | |
| M07-07 | 点击“代码编辑”和“规律” | 代码入口显示当前模式的真实脚本/指针并遵守等长门禁；规律入口打开共用规律编辑器 | [ ] | |
| M07-08 | 执行“动画测试”：暂存当前合法修改、选择双方机体和 Mesen 路径并开始 | 生成隔离测试 ROM，启动 Mesen；实战能看到当前所选武器的对应动画 | [ ] | |
| M07-09 | 尝试添加第 256 件武器或新增独立动画脚本 | 明确提示 `$01—$FF` 已满；不会静默无动作或覆盖现有记录；脚本搬移说明归 M12 | [ ] | |
| M07-10 | 验证数据库取消，以及确定→保存派生 ROM→全新进程重开 | 取消完整回滚；确认后的名称、属性和敌我动画参数保持 | [ ] | |

## 8. 跨模块事务与失败用例

| 编号 | 验收步骤 | 预期结果 | 结果 | 备注/证据 |
|---|---|---|---|---|
| X-01 | 在 M05、M06、M07 各暂存一个合法改动后点击数据库“取消” | 三个模块全部恢复到打开数据库前，撤销历史和资源分配不残留本次会话 | [ ] | |
| X-02 | 重开数据库，三个模块各修改一项，直接点击右下“确定” | 尚未单独暂存的合法表单会自动提交；三个模块修改同时保留 | [ ] | |
| X-03 | 制造一个非法/超容草稿，再点击“确定” | 阻止整窗提交，明确指出具体表单错误；其他表单和草稿仍可修正，不发生半提交 | [ ] | |
| X-04 | 保存派生 ROM，记录 SHA-256，并用全新修改器进程重开 | 所有已确认值可读回；状态栏无未保存修改；推荐基线和 `references/` 哈希不变 | [ ] | |
| X-05 | 使用主窗口撤销/重做一次 M05—M07 已确认事务 | 撤销恢复全部相关字节，重做恢复全部相关字节；共享记录不会只恢复部分引用者 | [ ] | |

## 9. 问题记录模板

发现问题时不要覆盖失败文件或勾选通过。每个问题至少记录：

| 字段 | 内容 |
|---|---|
| 清单编号 | 例如 `M06-07` |
| EXE / ROM SHA-256 | |
| 当前关卡、记录 ID、子页签 | |
| 操作前值 / 输入值 / 实际结果 | |
| 是否点击页内暂存、数据库确定、主窗口保存 | |
| 错误提示全文 | |
| 录屏/截图路径 | |
| 修改前/后 ROM 路径与哈希 | |
| 是否可在全新进程重现 | |

## 10. 用户签核

结论填写规则：

- **通过**：本模块当前允许验收的所有项目通过，且没有未解决的阻断缺陷。
- **有条件通过**：主要动线可用，但存在已明确列出的限制或待补证项目；限制必须写入备注。
- **不通过**：当前交付范围存在可复现的错误、数据破坏、错误写入或关键动线不可用。
- M03/M04 本轮最多签核“只读范围通过”或“有条件通过”；D3 写入门禁未解除前不能签核部署/事件编辑通过。
- M05—M07 的人工通过不自动等于字段级“已对齐”；模块第 5 节黄金对照、差分审计和必要的模拟器验证仍须分别完成。

| 模块 | 结论 | 验收日期 | 验收人 | 未通过项/条件/证据路径 |
|---|---|---|---|---|
| M03 初始配置 |  |  |  |  |
| M04 商店事件 |  |  |  |  |
| M05 机体修改 |  |  |  |  |
| M06 人物修改 |  |  |  |  |
| M07 武器修改 |  |  |  |  |

用户明确确认签核后，实现侧再把结论回写 `docs/需求拆分/README.md` 第 6 节、各模块第 8 节和主需求第 11 章；清单创建本身不算验收完成。
