# M03—M07 验收清单

> 更新日期：2026-09-22 ｜ 当前分支：`shanghua`
>
> 本文件区分机器验收、代理界面验收和用户签收。机器报告通过不自动代替用户最终签收。

## 1. 本轮正式输入

| 项目 | 值 |
|---|---|
| 正式 EXE | `output/app/新DC篇完整修改器.exe` |
| EXE SHA-256 | `48C973383E37A8E87A423F81BB5F5AABBB48A11E8C0C629ECE19B9F45272E0D5` |
| 推荐 ROM | `output/rom/DC_kuorong_464K.nes` |
| 推荐 ROM SHA-256 | `82C218275459D53C0F306F6BC036C4797316976E0FA7AD1D5A8247E338995B8E` |
| 独立验收副本 | `output/verification/manual-acceptance-fixes-20260921/expanded-acceptance-after.nes` |
| 本轮证据 | `output/verification/legacy-m03-m04-structural-20260921/`、`output/verification/m03-incremental-acceptance-20260922/`、`output/verification/m04-shop-events-20260922/`、`output/verification/m06-character-acceptance-20260922/` |
| 汇总报告 | `output/verification/m03-m07-current-build-machine-recheck-2026-09-21.json`；M03/M06 增量报告见各证据目录的 `report.json` |

推荐 ROM 在验收前后哈希一致；所有写入验证均使用独立副本。

## 2. 模块状态

| 模块 | 本轮机器验收 | 已完成的关键检查 | 用户签收 |
|---|---|---|---|
| M03 初始配置 | 通过 | 1380 字段、关卡路由、真实部署、列表/地图联动、代表性新增/删除结构重排 | 2026-09-21 原范围通过；2026-09-22 新增开放范围由用户确认增量签收通过 |
| M04 商店事件 | 通过 | 103 字段与结构黄金；当前构建真实控件增删改、另存重开、取消及归一化基线字节可逆 23/23 通过；正式 EXE 原生界面 299→303 字节超容及 `(26,0)` 对 26×26 地图越界均拒绝应用、保留草稿、零写入；通用打开/保存/重开路径 H-11 通过 | 2026-09-21 原范围通过；2026-09-22 新增开放范围由用户确认增量签收通过 |
| M05 机体修改 | 通过 | 数据字段、事务、共享记录、图形编辑；拼图 12/12、全 255 槽预览与其余真实按钮动作 27/27：武器 1/2 跳转、图标绑定、主体/碎片上传与清空、另存重开、添加门禁及外层取消回滚 | 2026-09-21 按当前实现范围通过；新增产品侧证据不代签参考版黄金 |
| M06 人物修改 | 通过 | 双名称、属性、精神、台词、共享记录、头像；当前构建 27/27 检查通过，人物 `$06` 正面 `25/1`、背景 `27/2`，变形绑定 `4→3→4` 字节完全可逆 | 2026-09-21 按当前实现范围通过 |
| M07 武器修改 | 通过 | 属性、共享记录、敌我动画、跳转、容量门禁；当前构建 26/26 GUI→磁盘复验，实际装备反查/跳转与敌我等长参数保存冷读通过，独立规律库 255/252/250/255 且只读零写入；F-040 所选武器活动战场夹具机器通过 | 2026-09-21 按当时实现范围通过；新增动画测试动线待用户签收 |

## 3. 本轮修复复核

| 编号 | 问题 | 结论 | 证据 |
|---|---|---|---|
| H-07 | 人物头像图库字段疑似颠倒 | 通过；旧目标写反，参考黄金证明当前 `正面 25/1、背景 27/2` 正确 | `H07-character-06-reference-fields.png` |
| H-08 | 商店事件不可编辑 | 通过；生产入口可编辑，保存后全新加载一致 | `H08-shop-event-editing-enabled.png` |
| H-09 | M05 导图布局/真实落盘 | 通过；用户真实 BMP 经裁切、扩容、非零偏移保存并重载一致；当前构建补充拼图 12/12 与其余动作 25/25 | `H09-real-bmp-import-offset-01.png`、`m05-actions-current-build-20260922/report.json` |
| H-10 | 武器规律没有独立规律库 | 通过；四类规律使用独立只读页签展示 | `H10-four-weapon-rule-tabs.png` |

上述截图均位于 `output/verification/manual-acceptance-fixes-20260921/`；结构化结果见同目录的 `repair-acceptance-report.json`。

## 4. 回归结果

最终代码执行完整测试：

```text
Ran 728 tests in 867.458s
OK
```

受影响区域组合回归为 228/228 通过；2026-09-22 新增的 M06 定向回归为 33/33，通过真实控件的构建验收为 27/27。M05 又完成拼图 12/12，以及全 255 槽预览与其余动作 27/27 的隔离验收；后者直接驱动数据库页真实按钮，并分别覆盖“转到武器 1/2”，报告位于 `output/verification/m05-actions-current-build-20260922/report.json`。2026-09-21 已通过 Windows 原生窗口控制实际操作当时的正式 EXE：M03 的关卡部署与空表语义、M04 的商店编辑入口及“任何人物”布局、M05 的机体数据与图形控件、M06 人物 `$06` 的正面 `25/1` 与背景 `27/2`、M07 的四页签武器规律库均正常显示。该轮窗口均以取消或关闭结束。2026-09-22 H-11 使用哈希 `E3D17EB2…3C419` 的正式 EXE，经 ROM 启动参数加载隔离副本，切换 M03—M07 页签，在 M07 修改一项命中并经原生 Ctrl+S 另存，全新进程重开读回一致；全文件唯一差分和三份哈希见 `output/verification/h11-native-exe-20260922/report.json`。后续 `111C3B3D…BD08E9` 构建完成隐藏自检与动画夹具回归；当前 `48C97338…E0D5` 构建完成隐藏自检和 746/746 全量回归。M03、M04 新开放范围已于 2026-09-22 由用户明确确认增量签收通过。

## 5. 用户现场签收

用户可直接按下表给出结论；如发现异常，记录模块、人物/机体/武器或关卡 ID、操作前值、输入值、实际结果以及截图/录屏路径。

| 模块 | 结论（通过 / 有条件通过 / 返工） | 日期 | 备注 |
|---|---|---|---|
| M03 初始配置 | 通过（按当前安全实现范围） | 2026-09-21；2026-09-22 增量 | 2026-09-22 新开放范围由用户明确确认通过，边界见第 6 节 |
| M04 商店事件 | 通过（按当前安全实现范围） | 2026-09-21；2026-09-22 增量 | 2026-09-22 新开放范围由用户明确确认通过，边界见第 7 节 |
| M05 机体修改 | 通过（按当前实现范围） | 2026-09-21 | 用户在本对话明确签收 |
| M06 人物修改 | 通过（按当前实现范围） | 2026-09-21 | 用户在本对话明确签收 |
| M07 武器修改 | 通过（按当前实现范围） | 2026-09-21 | 用户在本对话明确签收；既有安全门禁保留 |

用户明确签收后，再同步更新 `docs/需求拆分/README.md`、各模块状态和主需求状态；本清单不提前代填。

## 6. M03 新增开放范围增量验收

本节只验收 2026-09-22 新开放的部署编辑与结构动作，不改变第 5 节已经完成的历史签收。

| # | 操作 | 预期结果 | 结果 | 证据/备注 |
|---:|---|---|---|---|
| 1 | 打开推荐 ROM 的“初始配置”，选择一个有部署的关卡，双击敌军记录 | “配置设置”可修改坐标、驾驶员、机体、等级和行动，不再是只读窗口 | 通过（自动） | `01-existing-record-editor.png`；编辑器与字段控件均启用 |
| 2 | 修改一个字段，应用并另存隔离 ROM，完全退出后重开 | 修改值保持；未选字段不变；推荐 ROM 哈希不变 | 通过（自动） | 第 04 关敌军等级 56→57；全新工程重开精确一致；输入哈希不变 |
| 3 | 在空格右键新增一条敌军配置，应用并重开 | 新记录保持；固定槽不足时明确提示容量规划，不覆盖下一关数据 | 通过（自动） | 选用原有 23 字节余量的第 0C 关；新增 6 字节记录并重开一致 |
| 4 | 删除刚新增的记录并应用 | 记录数恢复，其他部署记录及地图数据不变 | 通过（自动） | 修复固定槽尾部残留后，删除输出与新增前 ROM SHA-256 完全一致 |
| 5 | 执行复制、粘贴和拖动坐标后点“取消” | 所有草稿丢弃，工程字节与打开窗口前一致 | 通过（自动） | 草稿存在但未进入工程；关闭后重开布局与工程字节均不变 |
| 6 | 运行 `tools/report_m03_m04_structural_compatibility.py` | 报告 `passed=true`，M03/M04 四个结构案例及空表 5 字节槽规则全部通过 | 通过（自动） | `m03-m04-structural-compatibility.json` 与增量报告均为 `passed=true` |

| 增量签收项 | 填写 |
|---|---|
| M03 2026-09-22 新增开放范围结论 | 通过（机器增量验收 17/17，用户明确确认签收） |
| 未关闭问题 | 本轮机器范围无；验收中发现的“派生 ROM 重开后容量规划误报 Hook 占用”已修复并回归通过 |
| 验收人 | 用户（本对话明确确认） |
| 验收日期 | 2026-09-22 |

机器证据位于 `output/verification/m03-incremental-acceptance-20260922/`；用户在机器复验通过后于 2026-09-22 明确确认签收。

## 7. M04 新增开放范围增量验收

本节针对 2026-09-22 新开放的商店事件编辑与结构动作；2026-09-21 历史签收不自动扩大。隔离证据位于 `output/verification/m04-shop-events-20260922/`，自动报告 23/23 检查通过。首次写入会将旧版指针布局归一化到托管池，因此“新增→改值→删除”的整文件可逆性以语义相同的隔离归一化空表 ROM 为基线；推荐输入 ROM 始终不改。

| # | 操作 | 预期结果 | 机器结果 | 用户结果/证据 |
|---:|---|---|---|---|
| 1 | 在可见格新增商店后，打开该记录将 `$F2` 改为 `$F3`，应用、另存、完全重开 | 商店换绑值保持，其余关卡语义不变；不把推荐 ROM 中视口外的既有事件当作参考可编辑样本 | 通过 | `01-shop-rebind.png`；`m04-field-edit.nes` |
| 2 | 在原为空表的关卡添加商店 `$F2`，保存并重开 | 得到 `(3,4,FF,F2)`；空表此前无报错 | 通过 | `02-added-shop.png`；`m04-add-shop.nes` |
| 3 | 删除刚新增并已换绑的记录，再次保存重开 | 该关恢复空表，整文件哈希与首次写入归一化后的空表基线完全一致 | 通过 | `m04-normalized-empty-baseline.nes`、`m04-delete-shop.nes`；`report.json` |
| 4 | 打开“商店设置”后修改草稿，但关闭页面而不应用 | 项目字节保持原值 | 通过 | `03-cancelled-draft.png` |
| 5 | 查看 `$F5—$FE` 商店选项并核对结构报告 | 无效目录不能选择；参考结构报告通过，空表仍占 5 字节槽 | 通过 | `report.json`；`m03-m04-structural-compatibility.json` |

| 增量签收项 | 填写 |
|---|---|
| M04 2026-09-22 新增开放范围结论 | 通过（机器增量验收 23/23，用户明确确认签收） |
| 未关闭问题 | 本轮机器范围无；视口外无入口字段、forced-FF 人物字段和无效商店仍不开放 |
| 验收人 | 用户（本对话明确确认） |
| 验收日期 | 2026-09-22 |

## 8. M07 当前构建复验（不扩大签收）

`output/verification/m07-weapon-current-build-20260922/report.json` 的 26/26 机器检查通过；输入与派生 ROM 的哈希、逐字节差分和截图均在该目录。新增检查通过真实按钮完成装备反查跳转，并分别提交我方/敌方动画的合法等长参数后冷重开核对。此处只证明产品当前实现，不把语义编辑超集解释为参考动画指针写入，也不替代 F-040 的用户可见实战签收。

| # | 操作 | 预期结果 | 机器结果 | 证据 |
|---:|---|---|---|---|
| 1 | 武器 `$01` 修改命中、特技和距离补正，确定后另存并全新打开 | 只改六字节记录前三字节；射程和保留半字节不变，值重开保持 | 通过 | `01-weapon-field-draft.png`、`m07-weapon-edited.nes`、`report.json` |
| 2 | 浏览四套武器规律的首末记录 | 数量为 255/252/250/255；名称和代码只读，工程零写入 | 通过 | `02-four-rule-libraries.png`、`report.json` |
| 3 | 输入非法动画代码并同时修改合法命中值，尝试应用后取消 | 全笔拒绝，无部分写入，取消保持派生 ROM | 通过 | `report.json` |

运行时补证：最初的普通测试 ROM 确实会直接进入 GAME OVER；随后已用 ROM 读跟踪锁定战场单位与机体武器表链路，并把校验有效的活动战场 SRAM 夹具接入正式按钮。Mesen 0.9.9 无窗口复验已显示所选 `$01 光束军刀`、射程选敌层和攻击战斗画面，F-040 机器范围通过；证据见 `output/verification/m07-animation-runtime-20260922/report.json`。这项新增动线仍待用户签收；参考版规律代码保存黄金仍缺，不得把“代码可浏览”升级为“代码可写”。

修复后的最终全仓回归为 743/743 通过，见 `output/verification/m07-full-regression-20260922/report.json`。首轮唯一失败是交付门禁测试仍断言“只有 M18 已签收”，与既有报告中 M08—M18 共 11 个模块均按当前范围签收的状态不一致；更新该过期断言后从头完整重跑通过，不涉及产品运行逻辑。
