# M18 导出机体与头像验收清单

> 当前状态：实现与自动黄金验证完成，等待用户现场验收。本文档不预填通过结论。

## 1. 验收基准

| 项目 | 固定值 |
|---|---|
| 正式程序 | `output/app/新DC篇完整修改器.exe` |
| 正式程序 SHA-256 | `E1FA6624851776BF4505CC3E97EC0F7E4189C14441C4216C0ECE3830DC035D3A` |
| 输入 ROM | `output/rom/DC_kuorong_464K.nes` |
| 输入 ROM SHA-256 | `82C218275459D53C0F306F6BC036C4797316976E0FA7AD1D5A8247E338995B8E` |
| 参考 ZIP | `output/verification/legacy-user-evidence-2026-09-14/legacy-unit-export-samples.zip` |
| 参考 ZIP SHA-256 | `230E55274B647D0E903C6B2753EE9BD54580F188FD8B3F0DC74F57F8FB0E2C39` |
| 自动验证报告 | `output/verification/m18-unit-export-verification.json` |
| 生成/参考聚合 SHA-256 | `FB2122766C647507D357B62B5799B0856FCAB01F4EC649A889793F63A9FAC102` |

验收前先核对 EXE、ROM 和参考 ZIP 哈希。若任一不一致，请停止并记录实际哈希，不要沿用本清单结论。建议选择一个不含既有 `导出的机体` 子目录的空目录作为输出位置。

## 2. 编号验收步骤

| # | 操作 | 预期结果 | 结果（通过/失败） | 证据路径或备注 |
|---:|---|---|---|---|
| 1 | 启动正式程序，载入上述 ROM，展开“数据”菜单 | “导出机体(P)”可用且显示 Ctrl+F；“导出头像(L)”位于其下、显示 Ctrl+L 但为置灰状态 | | |
| 2 | 将鼠标停在置灰的“导出头像(L)”上，并按一次 Ctrl+L | 状态/提示说明“参考版此入口不可触发”；不弹窗口、不产生头像文件 | | |
| 3 | 按 Ctrl+F | 弹出标题“导出机体”的 Windows 系统保存对话框，具有“保存/取消”按钮 | | |
| 4 | 在保存对话框直接点“取消” | 对话框关闭；测试目录中没有创建文件；当前 ROM/工程状态不变 | | |
| 5 | 再次按 Ctrl+F，在空测试目录中保留默认标记名并点“保存” | 状态栏从 001/255 更新至 255/255；结束后无失败弹窗，提示共 1275 个 BMP | | |
| 6 | 检查输出根目录 | 固定生成 `导出的机体/`；其下恰有 255 个目录，编号 001—255，不含 000 | | |
| 7 | 检查目录与文件名 | 目录使用 `<三位ID>：<名称>`；246—255 名称为空但保留全角冒号；每目录恰有 `[效果]/[机体]/[碎片]/[图标1]/[图标2]` 五张 BMP | | |
| 8 | 抽查 001、009、241 三个目录 | 三者分别覆盖普通图、负坐标碎片整块裁切、碎片黑色透明叠加边界；与参考 ZIP 对应五图目视相同 | | |
| 9 | 查看任一 `[效果]/[机体]/[碎片]` 文件属性或十六进制头 | 128×128、49206 字节、24 位 BI_RGB/BGR、正高度 bottom-up、像素偏移 54 | | |
| 10 | 查看任一 `[图标1]/[图标2]` | 16×16、822 字节、24 位 BI_RGB/BGR、正高度 bottom-up | | |
| 11 | 在仓库根目录运行 `.\.venv\Scripts\python.exe tools\verify_legacy_unit_export.py` | 输出 `1275/1275`、`mismatches=0`、`passed=True`；JSON 中 missing/extra/mismatches 均为空且两个聚合哈希相同 | | |
| 12 | 再次计算输入 ROM SHA-256 | 仍为 `82C218…95B8E`，证明导出未写 ROM | | |
| 13 | 打开“扩展功能 → 导出头像 BMP（增强）”，任选人物导出 | 能生成增强版 `[背面]/[正面]/[效果]` 三张 32×32 BMP；该入口没有 Ctrl+L 快捷键 | | |
| 14 | 打开“扩展功能 → 机体导入与CHR图像”，回归 `.dcunit` 导出/导入 | `.dcunit` 超集功能仍可用，且不会占用或覆盖五 BMP 目录协议 | | |

## 3. 明确不在本次通过范围

- 机体目录右键“只导出当前项”来自差异矩阵推断，参考探针尚未复现，当前不开放、不计入 M18 通过条件。
- 数据菜单的参考头像入口按 D2 有意禁用；不可把“置灰”记作缺陷，也不可把增强头像 BMP 冒充参考版协议。
- `.dcunit` 与增强头像 BMP 是保留的超集功能，分别做自身回归，不计入 1275 张 G6 分母。
- 本清单只验收 M18；M08—M17 的其他未完成或未签项目不会因 M18 通过而自动升级状态。

## 4. 问题记录

| 问题编号 | 失败步骤 | 实际结果 | 截图/目录/日志路径 | 严重度 | 处理状态 |
|---|---:|---|---|---|---|
| | | | | | |

若任一步失败，请保留整个测试输出目录、程序版本哈希和截图；不要覆盖参考 ZIP。

## 5. 用户最终签收

| 签收项 | 填写 |
|---|---|
| 结论（通过 / 有条件通过 / 返工） | |
| 未关闭问题编号 | |
| 验收人 | |
| 验收日期 | |
| 备注 | |

只有用户明确填写或回复签收结论后，才可把 M18 从“实现完成、待用户验收”升级为“用户验收完成”。
