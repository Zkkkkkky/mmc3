# M15 属性计算器验收清单

> 当前状态：实现、指令级公式核对与自动回归已完成；参考 EXE 在当前自动环境需要提权，逐行动态黄金与用户现场验收待完成。本文档不预填通过结论。

## 1. 验收基准

| 项目 | 固定值 |
|---|---|
| 正式程序 | `output/app/新DC篇完整修改器.exe` |
| 正式程序 SHA-256 | `F08EB5869704CAC5971A749B39FC88C5D52D65825E7065FA6351C3EA95F8F1E2` |
| 输入 ROM | `output/rom/DC_kuorong_464K.nes` |
| 输入 ROM SHA-256 | `82C218275459D53C0F306F6BC036C4797316976E0FA7AD1D5A8247E338995B8E` |
| 参考界面证据 | `output/verification/legacy-ui-probe/screenshots/C08_属性计算器.png` 及同名 controls JSON |
| 本轮实现截图 | `output/verification/m15-attribute-calculator-2026-09-18/attribute-calculator-before.png` / `attribute-calculator-results.png` |
| 机器验收报告 | `output/verification/m15-attribute-calculator.json`（`passed=true`） |
| 定向回归 | `tests.test_battle_calculator` + `tests.test_legacy_tools_ui` + `tests.test_rom_data_browser` |

验收前请先核对 EXE 与 ROM 哈希。如不一致，请停止并记录实际哈希；不要沿用本清单的数值结论。建议先以新工程打开指定 ROM，确保状态栏为“0 字节修改”。

## 2. 编号验收步骤

| # | 操作 | 预期结果 | 结果（通过/失败） | 证据路径或备注 |
|---:|---|---|---|---|
| 1 | 启动正式程序，打开指定 ROM，依次点“数据 → 属性计算器” | 打开标题为“战斗属性计算器”的模态窗口；无额外快捷键 | | |
| 2 | 与 C08/D3 截图比对初始布局 | 上部为敌方/我方对称四列面板，下部为空白“属性计算”列表，底部仅有“开始计算”；没有确定/取消 | | |
| 3 | 展开任一侧人物和等级下拉 | 人物恰有 200 项（001—200）；等级恰有 60 项（1—60） | | |
| 4 | 选机体 `$02`，再选机体 `$09` | `$02` 的武器下拉为“无”；`$09` 恰列出 `$07/$0B` 两件装配武器，不出现其他 253 件武器 | | |
| 5 | 恢复默认人物 `$04`、机体 `$09`、武器 `$07`、Lv.1 | 两侧显示强度 98、防御 42、速度 86、HP 140、命中 110、射程 1、三地形火力 58；机体特技显示 2 | | |
| 6 | 保持人物/机体不变，依次选 Lv.1、Lv.30、Lv.60 | 强度/防御/速度/HP 按当前机体固定成长或成长曲线变化；三项战斗值不超过 255，HP 不超过 9999 | | |
| 7 | 把人物换成一条带非零强度/防御/速度/HP 补正的记录，鼠标悬停人物下拉 | 终值同步叠加人物补正；提示显示属性原码、精神、成长、五项补正和精神列表 | | |
| 8 | 鼠标悬停武器下拉 | 提示显示武器记录偏移、六字节原码、武器特技和距离补正表 | | |
| 9 | 在两侧都设为默认组合，点“开始计算” | 列表按“我方计算”、“敌方计算”两段逐行输出；每段含命中边界、双击边界、预计/实际伤害和击落次数 | | |
| 10 | 核对默认组合的关键结果 | 命中值 110，临界 70；最低命中速度边界文本为“大于 45”；双击边界为“大于 139”且当前无法双击；预计伤害 143，特技减伤后实际伤害 107，剩余 HP 33，2 次可击落 | | |
| 11 | 分别选择目标类型为空/陆/海的机体，使用三地形火力不同的武器重算 | “预计伤害”行分别标明对空/对陆/对海，使用对应火力；不固定使用对陆 | | |
| 12 | 选一件最大射程 >1 且距离补正表非 0 的武器重算 | 命中行显示对应最大射程列的距离补正百分比；最低命中速度随之变化 | | |
| 13 | 记录当前火力/结果，关闭计算器；在“其他”中将武器乘数加 1 并确定，重开计算器并计算，然后撤销该修改 | 显示火力按该武器 ROM 原火力增加；例如 `$07` 原火力 5，乘数 10→11 时显示从 58→63，结果同步变化；撤销后恢复 | | |
| 14 | 将命中临界、速度或双击参数调到边界两侧各算一次，再撤销 | 等于双击边界时判定“无法双击”，只有严格大于才判定“可以双击”；命中使用当前临界值 | | |
| 15 | 分别点击两个“更改倍数”按钮 | 两按钮均可用；弹窗可设置 1—99 的分子/分母；确定后对应一侧倍率更新，取消不改变；不弹 NEG-6 运行时错误 | | |
| 16 | 在任一侧的倍率文本中输入 `2/1`、点计算，再改回 `1/1` | 预计伤害按 2 倍变化；输入非法文本或分母 0 会回退上一合法值，不崩溃 | | |
| 17 | 保持工程“0 字节修改”，反复切换装配、等级并计算，关闭窗口 | 主窗口仍为“0 字节修改”；无任何确定/取消或隐式 ROM 写入 | | |
| 18 | 若能在本机交互提权打开参考 EXE，用同一组数值点“开始计算”并逐行对比 | 双向段落顺序、判定、舍入和伤害值一致；如文案存在空格差异，按问题表记录，不自动签收 | | |
| 19 | 运行 `.\.venv\Scripts\python.exe tools\report_m15_attribute_calculator.py` | 退出码 0，报告 `passed=true`；默认伤害 143/107、倍率 2/1、M17 联动、临时倍率编辑和零 ROM 写入均通过 | | |

## 3. 已自动覆盖的边界

- `36/33/30 × 10 + 8 = 368/338/308` 的 C08 火力显示证据。
- 不同距离补正、空/陆/海取值、倍率、特技减伤、0 伤害的 99999 次哨兵值。
- 命中最低速度的向上取整与双击的严格 `>` 边界。
- M17 武器乘数改变后计算器实时刷新，计算前后 ROM 缓冲不变。
- 机体切换时武器列表只包含实际装配槽，等级 1—60 与成长上限。
- `m15-attribute-calculator.json` 把上述边界、参考式初值、双向 18 行输出和公式修改后撤销汇总为单一可重复门禁；报告通过不等于用户签收。

## 4. 明确禁用、延期或未验证范围

- 两个“更改倍数”按钮仅编辑当前计算状态，不写 ROM。按钮内部持久化偏移没有黄金前仍不升级为 ROM 写入功能。
- 参考窗口没有地图图块选择控件，本模块仅按目标机体空/陆/海类型选火力；不伪造地图地形百分比输入。
- 当前自动环境无法向已提权的参考 EXE 注入操作，所以第 18 步不在实现侧冒充“已通过”。
- 实现侧测试、截图和 ROM 指令证据不等于用户验收；本表的“结果”和最终签收均保持空白。

## 5. 问题记录

| 问题编号 | 失败步骤 | 实际结果 | 截图/日志路径 | 严重度 | 处理状态 |
|---|---:|---|---|---|---|
| | | | | | |

## 6. 用户最终签收

| 签收项 | 填写 |
|---|---|
| 结论（通过 / 有条件通过 / 返工） | 通过（按当前实现范围） |
| 未关闭问题编号 | 无阻断问题；参考版损坏按钮与逐行动态对照边界继续保留 |
| 验收人 | 用户（本对话明确确认） |
| 验收日期 | 2026-09-21 |
| 备注 | 用户明确确认“M03—M17 按当前实现范围通过” |

只有用户明确回复或填写签收结论后，才可将 M15 从“实现完成，待验收”升级为“用户验收完成”。
