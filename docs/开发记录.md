# 新DC修改器开发记录

本文件从 2026-09-15 起作为持续开发日志，按时间顺序**只追加新条目**，不重写历史条目。正式执行队列见 `docs/需求拆分/README.md` §4.1；需求与功能状态基准仍是 `docs/SRW2修改器1比1复刻需求文档.md`，本日志不替代模块文档第 8 节、用户使用说明或用户验收签核。

每次开发工作结束、向用户报告结果之前都记录一次；包括模块只完成阶段目标、验证失败、尚有遗留或因证据暂停的情况。开发记录与代码/测试/相关说明在**同一次修改**中更新，不等待用户提醒。除明确获授权外，“完成开发”不等于“用户验收通过”，不能据此填写签核总表。

每条至少填写：日期与队列位置/模块、实际做了什么（对应功能点和文件/产物）、什么没做（具体遗留与禁用）、验证命令及结果/证据、风险或阻塞、下一步及是否待用户验收。若本轮无代码改动，明确标注；未执行的测试写“未运行”及原因，不能写作“通过”。涉及 ROM/IPS 等二进制变动时补前后 SHA-256 和相关 Bank/偏移；涉及当前修改器的代码、界面或操作变化时同次更新 `docs/DC修改器使用说明.md`。

## 条目模板（每次复制到文件末尾后填写）

```markdown
## YYYY-MM-DD — 队列第 N 步 / Mxx（本轮目标）

- 已做：具体实现或审计结果、F 编号、变更文件与生成产物；若无代码变动写明。
- 未做：未实现/未验证/仍禁用的具体内容、原因、剩余验收点。
- 验证：命令或人工/模拟器步骤、通过/失败/未运行，以及报告或录屏路径；二进制变动另附前后哈希。
- 风险与阻塞：容量、字节映射、回归问题或“无”；区分实现侧问题与需用户选择的问题。
- 下一步：具体下一项与所属队列步骤；说明是否进入用户模块验收，未验收不得标“已对齐”。
```

## 2026-09-15 — 开发前准备 / 队列尚未开始

- 已做：按用户确认的顺序固化 M00 → M01 → M02 → M17 → 核心数据 → 初始配置/商店事件 → 文本/事件 → 图形/计算器 → P3 逆向的 11 步队列，并建立每次开发必填的已做/未做/验证/下一步记录规则；本轮没有改动产品代码。
- 未做：M00 尚未开发完成；其后的模块未因本次计划归档而改变实现状态或用户验收状态。
- 验证：本轮仅校验文档差分与引用；没有运行产品测试或 ROM 构建（无代码、ROM 改动）。
- 风险与阻塞：用户侧强制补证已完成；M03/M04 的 ROM 字节映射与保存重开仍是实现侧黄金对照任务。
- 下一步：从队列第 1 步 M00 开始；模块实现侧自验完成后再交用户逐模块验收。

## 2026-09-15 — 队列第 1 步 / M00（黄金对照流水线续做）

- 已做：新增 `tools/golden_pipeline_collect.py` 的参数化单字段采集器，按隔离副本执行复制、参考版 GUI 编辑/确定/保存、落盘确认、关闭并确认进程退出、全新 PID 重开读取、全字节差分、归一化剔除、必写偏移与 30 秒门禁；白名单配方支持菜单、窗口、Edit、ComboBox、ListView 行、控件与坐标操作。`tools/report_golden_coverage.py collect` 连通在线结果归档/统计；档案索引支持同字段多用例，新增在线结果适配、失败目录识别和 10 项模拟/适配器测试。开发依赖加入 `pywinauto`；准备 M17 命中阈值 `cold_start_02` 配方。重新生成 61 个既有黄金 JSON、index、字段注册表与覆盖率报告，原有快照和历史审计源 JSON 未改；同步更新 M00 模块文档及队列状态。本轮未改 `src/` 产品代码，因此无用户操作说明变化。
- 未做：没有取得真实参考版 `cold_start_02` 的单字段写入→保存→全新进程重开现场档案，也没有实测完整用例 ≤30 秒；M01—M18 全字段的配方与全量 G1 分母尚未采集/登记。用户验收清单第 3 项及最终 G1/G2 签核仍空白，M00 保持“部分实现”，不据此解禁其他模块字段。
- 验证：`.\.venv\Scripts\python.exe -m unittest discover -s tests -t . -p "test_*.py"`：492 项通过；`tests.test_golden_pipeline_collect`：最新 10 项通过；`report_golden_coverage.py archive`：61/61 归档、golden 58、discovery 3、失败在线目录不计档案孤儿；`verify`：61 pass、0 fail、0 missing（3 known-exception）；`stats`：G1=58/61=95.08%（仅已登记字段分母），G2=58/58=100%。沙箱内 Markdown 报告写入失败，获准在沙箱外重跑 `stats` 成功。`git status -- references src` 无改动；完整测试生成 ROM SHA-256 `82c218275459d53c0f306f6bc036c4797316976e0fa7ad1d5a8247e338995b8e`，未提交 ROM/IPS 二进制变更。
- 风险与阻塞：命令行会话与交互桌面窗口隔离；真实在线尝试在等待参考程序窗口阶段失败，证据 `output/build/legacy-diff-audit/cases/legacy_live/M17/hit_threshold/cold_start_01/error.json`，尚无已确认的第一/第二 PID、实际偏移或耗时。隔离参考 EXE 已验证可在交互桌面显示主窗口，但直接窗口操作遇到几何状态错误后按 computer-use 操作指引暂停；已核实并关闭本轮启动的隔离程序进程。此前失败不能解释为字段语义失败，也不能冒充四步闭环通过。
- 下一步：在可访问交互桌面的 Windows 终端运行 M17 `cold_start_02` 在线采集命令，核对 `case.json` 两 PID、变化偏移、重开显示值、30 秒耗时及归档/统计结果；若 GUI 适配器有实际控件差异，先修驱动再重复用新 case_id。现场通过后再请用户按 M00 第 7 节验收，随后进入队列第 2 步 M01。

## 2026-09-15 — 队列第 2 步 / M01（启动器与主窗口）

- 已做：启动器进入主窗口后即关闭并销毁；未载入 ROM 时主窗口仍显示三个空标签页且禁用编辑。按 B03 整理参考版菜单顺序、分隔线与 14 个命令的逻辑 ID，并将“完整 ROM 数据读取”移到扩展功能菜单；加入原生 WM_COMMAND ID 分发入口。窗口标题不再用 `*` 标记未保存，状态栏仍提示草稿状态。沿用最新需求 D1 的派生 ROM 保存规则，首次保存另存为、后续保存当前派生文件；增强派生文件的快速重复保存备份命名，避免时间戳碰撞。同步修改 `src/dc_modifier/app.py`、`src/fc_rom_editor_core.py`、相关测试、`docs/DC修改器使用说明.md`、M01 模块文档及主需求状态，并更新启动器/空主窗口截图。构建 `output/app/新DC篇完整修改器.exe`，前后 SHA-256 为 `BCD926B9FE3326DBE503D40DCBD222A77BD961A42775E8314837E86CD29B786B` → `78008B2AFF9E1101FE13F3A0FE05F397CCB30445CAB1BB880E13123E8253D16C`。
- 未做：M00 的真实参考程序黄金对照未闭环；M01 的 B01/B02 原生菜单树、打开/保存两级路径和 NEG-3/C5 双环境现场测试、G4 用户签核均未完成。未把参考版“直接保存原文件”行为移植到新产品，按 D1 明确维持派生输出，不覆盖源 ROM；不能宣称 M01 已对齐。
- 验证：`.\.venv\Scripts\python.exe -m unittest discover -s tests -t . -p "test_*.py"`：500 项通过；新增启动器生命周期、空标签页、菜单逻辑 ID、WM_COMMAND 模拟、派生保存和快速备份用例通过。`.\tools\build_modifier_exe.ps1` 构建成功，新 EXE 的隐藏 `--self-test` 返回 0；截图 `docs/images/dc_modifier/00b-empty-main.png` 已目视检查。`git diff --check` 通过；`references/`、推荐 ROM 和 IPS 未改，测试生成 ROM SHA-256 保持 `82c218275459d53c0f306f6bc036c4797316976e0fa7ad1d5a8247e338995b8e`。
- 风险与阻塞：离线 Qt/WM_COMMAND 模拟不能替代交互桌面对参考 EXE 的实测；M00 的命令行会话与交互窗口隔离仍阻断参考版现场闭环。当前保存语义依 D1 与参考版直接保存有意不同，后续验收需按最新主需求而非旧截图判定。
- 下一步：在可访问交互桌面的环境补 B01/B02/C5 和 G4 现场证据，结合 M00 黄金流水线复核菜单与保存；未验收前保持“部分实现”，队列第 3 步 M02 的实现与自验另行记录。

## 2026-09-15 — 队列第 3 步 / M02（战场地图）

- 已做：主地图页宽、高输入框改为可编辑的 1—32 范围，并与高级面板的宽高值双向同步；改动立即更新画布与待保存草稿，点击应用前不写工作 ROM。更新 `src/dc_modifier/map_page.py`、地图 UI 测试、`docs/DC修改器使用说明.md`、M02 模块文档及主需求状态；更新 `docs/images/dc_modifier/01-map-editor.png` 截图。与 M01 同批构建新版 EXE，SHA-256 为 `78008B2AFF9E1101FE13F3A0FE05F397CCB30445CAB1BB880E13123E8253D16C`；本模块未调整 ROM 布局或 ASM。
- 未做：尚无同一 ROM 基线的参考版与新版逐格地图黄金对照，宽高/布局、A—G 功能、边界溢出及 H 只读相关现场证据未收齐；M02 第 8 节用户签核未完成，不能宣称已对齐或解禁未经黄金确认的字段。
- 验证：同批完整 unittest 500 项通过；新增主宽高编辑、与高级面板同步、画布草稿变化和应用前 ROM 不变的独立用例在最后补充断言后再次通过。`docs/images/dc_modifier/01-map-editor.png` 已目视检查，参考截图 `output/verification/legacy-ui-probe/screenshots/D5_主窗口_战场地图页.png` 仅用于差异观察，不能作同基线验收证据。`git diff --check` 通过；参考 ROM、推荐 ROM 与 IPS 均未改。
- 风险与阻塞：参考截图与新版截图来自不同 ROM 基线，且当前布局比参考版宽，不能从静态画面推断逐格/逐像素一致。M00 现场采集阻塞同样影响 M02 黄金证据。
- 下一步：取得同基线地图操作录屏与黄金用例，覆盖单格、尺寸、A—G、溢出和 H 只读；跑完整保存重开与对照后再请用户验收 M02。M00、M01、M02 的未做项均保留在模块文档和本日志中，未因进入后续队列而自动标记完成。

## 2026-09-15 — 交接补充 / M00—M02 人工待办归档

- 已做：按用户要求新增 `docs/待人工完成事项.md`，用 H-01—H-06 明确 M00 交互桌面采集、M01 打开/两级提交/备份双环境、M02 同基线地图与图块属性、用户最终签核的操作和应留证据；从需求拆分 README 链接该清单，供拉取分支的 agent 接手。本轮没有改动产品代码、ROM、IPS 或 EXE。
- 未做：H-01—H-06 现场操作和用户签核均尚未发生；清单不是已完成证据，M00—M02 状态维持部分实现。
- 验证：核对清单引用的模块文档、B03 控件树和 `cold_start_02` 配方路径；本轮只更新文档，未运行产品测试或构建，不能沿用前一轮 500 项结果冒充本轮重跑。
- 风险与阻塞：交互桌面窗口隔离仍影响 H-01；M01 参考备份证据冲突和 M02 同基线逐字段样本仍需现场复测。
- 下一步：优先完成 H-01，再按 H-02—H-05 采集与审计；实现侧处理差异后请用户执行 H-06，未签核前不标已对齐。

## 2026-09-15 — 用户截图反馈 / M01—M02 未载入状态纠偏

- 已做：核对用户截图的标题没有 ROM 路径、只有文件/帮助菜单、关卡数为 0，确认按钮禁用是设计中的未载入态，并保留 `Ctrl+O`/文件→打开作为可用载入入口，不自动打开或覆盖基线。修正误导性的空状态占位：`MapCanvas` 从无数据的 0×0 开始，空画布不再绘制一格绿色地形，调色板/笔刷显示中性空位而非假图块；地图下方与状态栏提示打开 ROM，载入后继续使用 ROM 的 16 张图库图像与真实地图。修改 `src/dc_modifier/map_page.py`、`app.py` 与 UI 测试，同步更新 `docs/DC修改器使用说明.md`、M01/M02 第 8 节和 `docs/images/dc_modifier/00b-empty-main.png`。重新构建 `output/app/新DC篇完整修改器.exe`，SHA-256 `78008B2AFF9E1101FE13F3A0FE05F397CCB30445CAB1BB880E13123E8253D16C` → `E4C307CFC34D2723E6AA5D27012DD8E41A5D089C5BF715CA7A7F519BC50A600E`；已更新校验文本。
- 未做：没有让无 ROM 的编辑按钮可用（会导致对不存在的关卡写入）；用户截图不能证明载入后的图块仍错，同基线参考版逐格/像素黄金对照、M00 现场闭环和 M01/M02 用户签核继续待做。没有修改 ROM/IPS 或图库字节映射，也未宣称 M02 已对齐。
- 验证：`.\.venv\Scripts\python.exe -m py_compile src\dc_modifier\app.py src\dc_modifier\map_page.py` 通过；空状态→打开 ROM→编辑区启用/16 张真实图块和地图安全编辑的 2 项定向 UI 测试通过；`tests.test_legacy_map_ui`+`tests.test_map_feedback_ui` 39 项通过。`.\.venv\Scripts\python.exe tools\capture_dc_modifier_guide.py --m01-m02-only` 成功，空状态截图已目视检查；已载入地图截图哈希保持 `C7DA1827AF82C49BD741461298374DF1C945C8BD502BA9FEEFA2EA617C6B6A3B`。`.\tools\build_modifier_exe.ps1` 构建成功、打包隐藏 `--self-test` 返回 0；`git diff --check` 通过，参考 ROM/推荐 ROM/IPS 无改动。本轮未运行完整 discovery 测试（限于未载入界面改动且无 ROM 变更），未在用户交互桌面复现其后续载入流程。
- 风险与阻塞：若打开兼容 ROM 后图块仍不正确，现有用户图只展示未载入态，无法推断具体关卡/图库或像素错误；需带 ROM 路径的载入后截图、ROM SHA-256、关卡 ID、图库选择及错误提示，再与参考版同基线比对。
- 下一步：请用户在新版 EXE 用 `Ctrl+O` 打开 `output/rom/DC_kuorong_464K.nes` 或兼容工作副本，检查标题/关卡数/真实地图；若仍不对，按 H-04/H-05 获取同基线现场证据并由实现侧复核。M00—M02 仍保持部分实现，用户验收栏不代填。

## 2026-09-15 — 用户截图反馈 / M01 普通派生 ROM 无法重开

- 已做：按弹窗复现新 DC 扩容基准改一字段、保存成无自动容量表的派生 ROM 后 `RomProject.load` 成功但主窗口 `load_rom` 拒绝；修正界面门禁：该已认证扩容布局的无规划输出在固定代码签名、464 KiB 预留区全零与完整性检查通过时允许全新窗口打开，带规划输出继续按容量布局检查。补全新窗口字段和地图单格保存/重开值一致、固定代码损坏及预留区含未登记数据两种拒绝回归；同步 `docs/DC修改器使用说明.md`、M01 状态及主需求 F-003。重新构建 `output/app/新DC篇完整修改器.exe`，SHA-256 `E4C307CFC34D2723E6AA5D27012DD8E41A5D089C5BF715CA7A7F519BC50A600E` → `F42E9A064CF0CEB1402BE3AF2F7B29B07E1BD71FBDD7D7F9E5E30F453A1871FD`，校验文本已更新；ROM/IPS 和布局未调整。
- 未做：没有获取截图所示 `D:\GIT\mmc3\output\rom\新修改器work.nes` 文件（当前环境中该路径不存在），无法对用户实际输出逐字节验收；M01 的参考版黄金对照、B01/B02 原生现场、NEG-3/C-5 和用户签核仍未完成，也未拓宽旧兼容布局的无规划非基准 ROM 续改权限。本轮未修复与 ROM 重开无关的 M00 黄金采集适配器时间戳缺陷。
- 验证：5 项 ROM 打开/保存定向 UI 测试通过（地图单格用例在完整 discovery 后补测）；完整 discovery 当时共 502 项，501 项通过、1 项失败，为 `tests.test_golden_pipeline_collect.LiveCollectionTests.test_win32_adapter_waits_for_disk_save_and_process_exit`，独立重跑仍失败。诊断证实 Windows 极短间隔写入可更新字节而 `st_mtime_ns` 不变，既有适配器只检查修改时间，5 秒后误判未保存；这不是本轮 ROM 打开逻辑的回归。打包隐藏 `--self-test` 返回 0，`git diff --check` 通过。
- 风险与阻塞：仅从截图确认其报错和已载入状态，不能断言用户工作 ROM 的所有数据都符合新门禁；若预留区已被其他程序写入且无规划表，新版会明确拒绝，需保留该 ROM 并进一步核对来源。M00 的全套测试仍有上述独立失败，不记为 502/502 通过。
- 下一步：请用户用新版 EXE `Ctrl+O` 重开已保存的派生 ROM 并确认字段/地图；若仍报错，提供该 `.nes` 文件或其 SHA-256 与新版弹窗文字，定位是否存在未登记扩展数据。M00 时间戳采集问题在其后续工作中单独处理；M01 现场与用户签核仍按 `docs/待人工完成事项.md` 保留。

## 2026-09-15 — 用户现场证据审计 / M00—M02

- 已做：按用户明确映射审计 `cold_start_03/case.json`、原名 `M02.mp4` 的 M01 录屏和 M02 四个前后 ROM。M00 现场用例确认两个不同 PID、70→71、未知偏移为空、12.312 秒，完整 verify 为 62/62 pass；将录屏压缩归档并生成联系表，确认新版启动器→未载入主窗口→系统打开对话框→载入地图动线；对两组 M02 ROM 计算大小、SHA-256、变化字节数与区间数。新增 `output/verification/manual-acceptance-2026-09-15/README.md`，同步更新人工清单和 M00/M01/M02 第 8 节。本轮未修改产品代码、EXE、ROM 或 IPS，原始 NES 没有加入证据归档。
- 未做：M00 第 7 节与 H-06 最终用户签核未填写；M01 缺参考版同流程、14 项菜单逐项记录、两级提交两路径及双环境备份观察；M02 两版修改前文件大小与 SHA 不同，且未标明同一关卡/坐标或字段/原值/请求值，现有四个 ROM 不能形成最小单字段黄金对照，H/公用颜色表只读证据也未收到。
- 验证：`report_golden_coverage.py verify` exit 0，62 cases / pass 62 / fail 0 / missing 0 / known-exception 3；录屏归档副本 66.97 秒并完成 10 帧目视联系表；新版 ROM 对变化 53,177 字节/2,398 区间，参考版 ROM 对变化 6,367 字节/315 区间；文档改动执行 `git diff --check`。
- 风险与阻塞：不同容量/哈希基线和大范围写入会把布局差异、保存归一化与用户实际操作混在一起，不能从“文件有变化”推断 M02 两版一致；录屏存在不等于覆盖全部验收步骤。用户未明确最终签核前，M00—M02 仍保持部分实现。
- 下一步：M01 补录参考版和菜单/两级提交缺口；M02 每次只做一个已记录动作并提供两版可比前后样本，若容量格式天然不同则先定义逻辑基线和归一化规则。完成差异审计与返工后，再由用户执行 H-06。

## 2026-09-15 — 用户补充现场证据 / M01 参考版菜单与保存重开

- 已做：接收两段用户指定的 M01 补充录屏（92.95 秒、17.11 秒），计算原始 SHA-256，压缩归档为 `m01-supplement-01.mp4`、`m01-supplement-02.mp4` 并生成联系表。逐帧核对确认参考版标题显示 ROM 绝对路径，文件 3 项、数据 10 项、帮助 1 项及可见快捷键均被展开；确认参考版修改地图、保存、关闭后在全新窗口重开 `M01-参考版-after.nes`，修改仍存在。结合先前新版录屏，两版保存后重开路径均有画面证据。同步更新证据 README、H-02/H-03 和 M01 第 8 节；未修改产品代码、EXE、ROM 或 IPS。
- 未做：新版载入后的 14 项菜单仍未逐项展开；参考版与新版均缺“修改并确定、主窗口不保存关闭、重开后修改消失”路径；保存前后目录和连续保存两次的 `.bak` 唯一性未完整展示。原生命令 ID 不能由录屏证明，仍依 B03 探针；第 7 节及 H-06 用户签核未代填。
- 验证：两段原视频可解码，时长分别 92.95/17.11 秒；原始 SHA-256 分别为 `047638DDF419F4044E105A561A419F66B14A70C426BA5DAC241D8165ABE6BAF0`、`BC1F32AC56F5E12C345932E487E4C394AB9ED0B379A7C738047AD17F4A7C16A3`；归档副本和 3 张联系表生成成功，执行 `git diff --check`。
- 风险与阻塞：录屏能证明可见操作结果，但没有单独记录请求值/重开值文本，也不能取代命令 ID 探针；缺失的不保存路径和目录列表仍可能掩盖两级提交或备份行为差异。
- 下一步：补录新版载入后依次展开文件/数据/帮助菜单；两版各用明显单格变化执行一次“不保存关闭→重开”；保存前先展示目录，连续保存两次后再展示目录和 `.bak` 文件。完成后由实现侧复核，再交用户 H-06 签核。

## 2026-09-15 — 用户第三段补充现场证据 / M01 基本收齐

- 已做：接收 245.85 秒 M01 补充录屏，计算原始 SHA-256，压缩归档 `m01-supplement-03.mp4` 并生成联系表。逐帧确认新版数据 10 项和帮助 1 项菜单、地图草稿关闭重开丢弃、连续保存时目录 14→15→16 且 `.bak` 时间戳不同；确认参考版修改后不保存重开恢复，常规桌面目录不产生自动备份，与既有探针 NEG-3 一致。更新证据 README、H-02/H-03、M01 第 8 节和主需求第 11.1 节；未修改产品代码、EXE、ROM 或 IPS。
- 未做：新版文件菜单切换过快，未留稳定的打开/保存/退出 3 项展开画面（但三个动作均已实际记录）；新版不保存路径在“应用地图、部署与事件”之前关闭，只证明页签草稿丢弃，尚未证明“应用到项目后、不按 Ctrl+S 关闭”仍不落盘。M01 第 7 节和 H-06 用户签核继续留空。
- 验证：原视频 SHA-256 `028661772F7CE29E113E8634154606F9220DEA915022B9784EC3CC9C7BB1542B`；归档视频 SHA-256 `97AE840E226A90C51529CDFBDF3331193A167D479238907A7667DC43FFB8CCBF`；联系表 SHA-256 `27E4AAFB42CA7AD9551C1D77038697FD15DBEAD590B3698FF3D56BD9920A69D2`；执行 `git diff --check`。
- 风险与阻塞：若把“未应用草稿”当作“已应用但未保存”，会遗漏两级提交的第二层门禁；因此 M01 只标现场材料基本齐备，不提前标已对齐。
- 下一步：用新版稳定展开一次文件菜单；地图改一格后点击“应用地图、部署与事件”，不要 Ctrl+S，关闭主窗口并选择不保存，再启动打开同一 ROM，确认该格恢复。完成后可进入 M01 用户签核。

## 2026-09-15 — 用户第四段补充现场证据 / M01 缺口闭环

- 已做：接收 64.92 秒补充录屏，计算原始 SHA-256，压缩归档 `m01-supplement-04.mp4` 并生成联系表。逐帧确认新版文件菜单稳定显示打开/保存/退出及快捷键；确认地图改动后点击“应用地图、部署与事件”，状态切换为已更新项目，未 Ctrl+S 关闭并全新进入后重开同一 `新修改器work.nes`，改动消失。H-02/H-03 现场证据至此通过审计；同步更新证据 README、人工清单、M01 第 8 节和主需求第 11.1 节。未修改产品代码、EXE、ROM 或 IPS。
- 未做：用户尚未明确 M01 最终验收结果和日期，因此模块第 7 节通过栏、H-06 及 README 签核区保持未填写；M00/M02 的独立缺口不因 M01 证据收齐而改变。
- 验证：原视频 SHA-256 `AB86B6466A0702188EE2487AF2B1E8D54C7EADFD473B15255276FFCB5A5EADBF`；归档视频 SHA-256 `B53C4988DF018BB7910372FAE465D4C896E9D95FE50F3A53E9CB632B7684FE7C`；联系表 SHA-256 `E45FB4895FBC7FEB8F669CF601DC25780EDCC6DDA035B0E5D1269713A4ADDE99`；执行 `git diff --check`。
- 风险与阻塞：现场录屏及 B03 探针已覆盖 M01 证据门禁，但“用户通过”属于验收决定，不能由实现侧自行代签；分支中仍有用户采集产生的其他未提交文件，提交时需保留来源与版权边界。
- 下一步：请用户明确确认 M01 是否验收通过；若确认，填写 M01 第 7 节日期、H-06 对应状态和需求拆分 README 签核记录，再处理 M02 重新采集。

## 2026-09-15 — M02 现场代操作尝试 / 原生桌面接口不可用

- 已做：按用户要求尝试使用已安装的 Windows Computer Use 能力接管本机修改器；完成能力规范、确认策略与接口说明检查，并读取当前可用表面。当前任务只返回 Codex 内置浏览器，Windows 原生应用列表为空；按明确路径请求启动 `output/app/新DC篇完整修改器.exe` 时，运行时不提供原生应用 `getApp` 接口。未对修改器、ROM、证据文件或桌面应用执行任何输入操作。
- 未做：未能启动或接管新版/参考版 EXE，M02 单格、宽高、容量、属性与只读现场证据均未新增；现有四个 ROM 的不同基线问题仍保持原判定。
- 验证：Computer Use 状态返回 `apps: []`，仅有 in-app browser；调用原生应用入口明确返回 `cua.getApp is not a function`。按能力安全规范未改用 PowerShell UI Automation、键鼠脚本或 Windows Run 绕过。
- 风险与阻塞：当前会话未启用 Windows 原生应用控制表面，属于工具能力阻塞；即使用户手动打开 EXE，也可能仍不会被该任务枚举，不能宣称已代操作。
- 下一步：在启用了 Windows 原生应用控制的 Codex 任务中重新执行 M02 现场代操作；若暂时无法启用，则由用户操作、实现侧继续按录屏与前后 ROM 审计。

## 2026-09-15 — M02 现场代操作重试 / 窗口仍不可枚举

- 已做：用户手动打开两个修改器后，重新扫描当前 Computer Use 表面；结果仍为 `apps: []`，仅返回 Codex 内置浏览器标签页。未向不可见窗口发送坐标、按键或文本，未改动 ROM。
- 未做：M02 现场操作和新证据未开始，上一条原生桌面接口阻塞未解除。
- 验证：第二次 `cua.getState()` 仍无任何 Windows 原生应用或窗口，证明阻塞不由“修改器尚未打开”导致。
- 风险与阻塞：在没有目标窗口句柄、截图和可访问性状态时盲发输入可能误操作其他应用，按 Computer Use 规范禁止继续。
- 下一步：切换到已启用 Windows 原生应用控制的任务/会话后再接管；当前任务只能继续处理用户提供的录屏和 ROM 文件。

## 2026-09-16 — M02 新修改器现场代操作 / 取消旧修改器对照

- 已做：用户明确确认旧修改器无法兼容新底板，M02 本轮不再测试旧修改器。使用新修改器打开独立工作副本，基线与 `before.nes` 的 SHA-256 均为 `82C218275459D53C0F306F6BC036C4797316976E0FA7AD1D5A8247E338995B8E`。现场录制宽高 `1`/`32` 边界；右键在 `(8,17)` 单格绘制使 RLE 达到 `326 / 324 B` 并显示“超出固定容量”，未应用超限草稿，随后左键恢复；中键在 `(13,5)` 吸取左画笔且地图状态不变；在 `(7,18)` 做可保存单格修改，RLE 为 `322 / 324 B`，应用并导出 `work_modified.nes`，SHA-256 `802DA5EB2C3F422AD3C4537923A5221B23BC279DAD2120C5479FB7F4ED200735`。录制还展示图库 D/G 图块字段、公用颜色表 2/3 只读说明和图库 H 仅预览状态。两段视频、前后 ROM 及说明已归档到 `output/build/legacy-diff-audit/cases/manual_m02_new_only_20260915_01/`。
- 未做：没有逐字段改写图库 A 的六类字段及 B—G 各一字段并生成独立最小 ROM；用户尚未签核 M02。此前含旧修改器的 `manual_m02_same_baseline_20260915_01` 因底板不兼容不纳入证据，但未删除以免破坏原始失败材料。
- 验证：`before.nes` 与 `work.nes` 大小均为 1,310,736 字节且 SHA-256 相同；`work_modified.nes` 同大小但哈希不同。关闭修改器并启动全新进程后重开 `work_modified.nes`，地图 `$00` 仍显示 `322 / 324 B`、尺寸 `26 × 27`，状态为“已载入 · 无未保存修改”。主录屏 65,035,746 字节、SHA-256 `B7E0C949D4CAA0D93B0207C0700663A5A55886CA8BE433ECBBC050DACE754BEB`，补充录屏 15,753,546 字节、SHA-256 `510D4775F8394A8A7F746C8763558FD7C4BD94A895338BE70C5FCEDAA0489B1A`。本轮是现场证据采集，没有修改产品代码、构建 EXE 或运行自动测试。
- 风险与阻塞：录屏能证明当前 UI 动线、容量门禁和一次导出，但不能代替 A—G 每字段字节差分与保存重开；用户取消旧修改器对照后，M02 只能按新修改器自身规格与数据不变量验收，不能再宣称“与旧版 1:1 对齐”。
- 下一步：按每次只改一个字段的方式补 A 与 B—G 最小样本，完成差分审计后交用户 H-06 签核。

## 2026-09-16 — M02 H-05 图块属性字段证据补齐

- 已做：继续接管新修改器，基于同一 `before.nes` 逐项修改并保存图库 A 的颜色表 1、防御、海属性、空中/陆地/海上移动补正；六个结果均形成独立 ROM。随后在界面修改图库 B 颜色 1，并保留实际导出。新增 `tools/generate_m02_field_samples.py`，用新修改器同一 `MapTileAttributeCodec` 重建 B—G 严格单字段样本、反解并生成 `field-diff-manifest.json`。证据统一放在 `output/build/legacy-diff-audit/cases/manual_m02_new_only_20260915_01/fields/`。
- 未做：用户尚未执行 H-06，因此 M02 第 7 节通过栏仍为空，模块状态保持“部分实现”；不恢复已取消的旧修改器/新底板兼容性测试。
- 验证：图库 A 六类样本分别仅变化预期的 1 字节；B—G 严格样本各仅变化对应 84 字节记录的首个颜色字节，全部可由编码器反解。图库 B 的真实 UI 导出额外变化位图 5 防御/海属性字节 `$50→$D0`，与界面提示的缺失水面海标志自动修复一致，已单列为 `B-color1-ui.nes`，未伪装成单字节样本。字段补充录屏压缩后 19,930,531 字节，SHA-256 `4EF8A8D05458A9DDECB8D1132CFE5972350DA7AC68AE9104BABE14CF8A727FE0`。
- 风险与阻塞：现场录屏与实现侧差分能证明当前新修改器的写入协议，但用户验收结论仍不能由实现侧代签；ROM 样本只应作为仓库内验收材料使用，不对外分发。
- 下一步：完成补充录屏压缩与哈希、运行图块属性/地图 UI 回归和 `git diff --check`，然后请用户决定 M02 的 H-06 签核结果。

## 2026-09-16 — 当前分支合并冲突处理 / M00—M02 证据与修改器功能整合

- 已做：在 `legacy-ui-research` 合入 `4072c52` 的合并中处理 15 个冲突文件。地图页保留未载入 ROM 时的空图块提示，并采用 40×40 单图块笔刷框；保留派生 ROM 无规划表重开的安全校验，同时纳入 M00—M02 的最新现场证据、M17 冷启动黄金用例、字段注册表及 62 例覆盖率报告。数据库图片地址下拉框的首选宽度不再因地图标题字体载入而缩小，窄窗口仍可收缩；机体拼图编号区固定高度，使图库与效果图顶部对齐。M00 参考版采集器保存后检查稳定字节变化，避免 Windows 修改时间不变时误报。重新打包正式 EXE，SHA-256 从 `F42E9A064CF0CEB1402BE3AF2F7B29B07E1BD71FBDD7D7F9E5E30F453A1871FD` 更新为 `720EA113BA84209C53FB623AD9BAA9C0404E80AE55C57E6225BEBBD5A1946A9C`；使用说明、模块第 8 节、主需求第 11 章及人工清单同步更新。
- 未做：没有代填 M00/M01/M02 的 H-06 用户验收签核，也没有恢复用户已取消的 M02 旧修改器与新底板直接对照。未推送分支；本轮未改变 ROM 布局或音频逻辑。
- 验证：地图/数据库/拼图联合回归 77/77 通过；时间戳不变的 M00 采集器回归 10/10 通过；完整 `unittest discover -s tests -t . -p "test_*.py"` 最终 528/528 通过。此前两次完整运行分别暴露拼图预览 5 像素错位和 M00 修改时间判断超时，均在本轮修复后重跑，不记作通过结果。黄金档案 `report_golden_coverage.py verify` 为 62 pass、0 fail、0 missing，3 个既有 known-exception；完整测试生成推荐 ROM SHA-256 `82C218275459D53C0F306F6BC036C4797316976E0FA7AD1D5A8247E338995B8E`，与既有输出一致。`build_modifier_exe.ps1` 构建及隐藏 `--self-test` 通过，EXE 校验文本与实际文件 SHA-256 一致；`git diff --check` 无内容错误。
- 风险与阻塞：合入分支附带大量本机验收视频、ROM 工作副本和打包中间产物，应继续留在仓库内部，避免对外分发 ROM。证据审计通过仍不是用户签核；M00 全量字段覆盖和 M01/M02 结论由后续验收流程决定。
- 下一步：完成本次合并提交；随后请用户按 H-06 决定 M00/M01/M02 验收结果，再依 `docs/需求拆分/README.md` 4.1 继续 M17 与 M05 等队列工作。

## 2026-09-16 — M00—M02 用户签收登记

- 已做：用户在本轮会话确认“已经签收完毕”，据此填写 `docs/需求拆分/README.md` 第 6 节 M00—M02 的验收日期和结论、三个模块第 7/8 节、H-01—H-06 人工事项，并同步主需求第 11 章 F-001—F-009 的签核状态。按归档证据中的遗留项将三个模块登记为“有条件通过”，产品实现状态继续为“部分实现”；没有改动产品源代码、ROM 或使用说明。
- 未做：M00 全量可编辑字段分母与 G1 全覆盖尚未补齐；M01 字段级黄金档案尚未补齐，D1 派生保存差异仍保留；M02 D5 标题/布局同基线比对和画布右键超集菜单核对尚未补齐，按用户此前决定不恢复旧版与新底板的直接对照。没有将任一模块升级为“已对齐”，本轮未推送分支。
- 验证：本轮仅做文档一致性和 Git 差分检查，未重跑修改器、ROM builder 或自动测试。签收依据为此前归档的 M00 `cold_start_03` 在线 12.312 秒闭环及 62/62 verify、M01 两版现场录屏和保存/不保存重开、M02 新版现场与字段样本；前一合并工作轮的完整单测 528/528 通过，不记为本轮运行结果。
- 风险与限制：“签收完毕”是用户明确确认，具体“有条件通过”分类则由现存未补证据项推定并在验收表公开；它不代表 M00—M02 已达到全字段黄金对照或旧版 1:1 对齐。验收用 ROM 样本留在仓库内部，避免对外分发。
- 下一步：按 `docs/需求拆分/README.md` 第 4.1 节继续执行队列，同时分别补齐 M00 全量字段覆盖、M01 字段档案和 M02 两项界面证据；证据齐备后再更新模块实现状态。

## 2026-09-17 — M03—M05 继续实现 / 页签取证、D3 写入门禁与共享记录保护

- 已做：按用户本轮指定将 M03—M05 临时提前，并在 `docs/需求拆分/README.md` 记录顺序调整为 M05→M03→M04。参考版探针改用真实鼠标点击扫描易语言 `CPageControl`，增加独立证据标签和陈旧 PID 防护；使用配套默认配置、`测试.nes` 与同名 CDL 成功取得战场地图、初始配置、商店事件三个不同页签。证据归档至 `output/verification/legacy-ui-probe-m03-m04-20260916-paired-v5/`：确认初始配置地址下拉 140/160/180、机体图标组 200、关卡选择 110；确认商店事件页显示原始记录和地图标记，但部署/事件字段仍位于自绘区域。产品侧据 D3 将部署和事件表格、增删、拖动、设置弹窗确定及项目写回全部锁定，同时保留列表选择、地图定位、悬停和只读弹窗；地形编辑继续可用。F5—FE 显示无效指针原因并禁选，F0 起始显示编号修正为 00。M05 普通属性表单和高级 16 字节原始写入在记录共享时都会列出全部受影响 ID，并默认取消。使用说明、M03/M04/M05 第 8 节、队列表和主需求第 11 章均已同步。
- 未做：M03 部署弹窗、M04 商店设置弹窗仍未取得单字段保存前后 ROM，D3 写入门禁没有解除；M05 的 11 个属性、名称、特技、地形/变形、配色、图库、图标和类型转换尚未完成参考版逐字段黄金对照，F-026 新增机体仍禁用。没有填写 M03—M05 用户验收表，没有运行 ROM builder、模拟器冒烟、EXE 打包或全量 528 项 discovery；本轮未修改 ROM 布局、音频或参考区文件。
- 验证：`py_compile` 检查 `map_page.py`、`pages.py` 和 `legacy_ui_probe.py` 通过。相关回归拆分后合计 158/158 通过：`test_map_feedback_ui` 25 项、`test_dc_modifier_ui` 56 项、`test_legacy_map_ui`+`test_database_feedback` 41 项、`test_screenshot_unit_feedback` 36 项；覆盖默认门禁拒绝程序化事件写入但允许地形提交、共享属性取消不写入/确认同步写入、共享原始记录提示，以及既有图标/图库/类型迁移。第一次合并运行在基础金钱用例等待新增模态确认，已中断并改为测试显式确认，随后该文件 36/36 通过。三张参考版页签截图 SHA-256 分别为 `C81B091C04107D36AA3C53CE9CD6245EEC6D19D7491FE975BFAB5413FA12B986`、`14462956C0F8BC0BC39F17F5B8E6FB3F25607F21EF8688D09E391BC68A8FF30F`、`A3F302980FE2E8286821F80C2B695428E3AA7AA00052779D3E7BD12073507462`；`git diff --check` 通过。
- 风险与阻塞：参考版初始配置和商店事件的列表/地图是自绘区域，普通子控件枚举无法给出字段 ID，必须继续操作二级设置弹窗并生成最小 ROM 差分。当前产品中相关编辑能力虽已有实现，但正式入口有意保持只读；在黄金证据完成前不得仅凭实现侧测试解锁。M05 多项能力已可用但仍属“未验证”，不能登记为 1:1 已对齐。
- 下一步：沿 B1 已知坐标进入参考版“配置设置”和“商店设置”二级窗口，枚举弹窗控件并为部署坐标/等级/行动、事件坐标/限定人物/商店号建立最小黄金样本；随后先完成 M05 11 属性字段及类型位域的单变量差分，按通过字段逐项更新状态，再决定 M03/M04 的写入开关是否可以解除。

## 2026-09-17 — M05 移动力采集收束 / 四要素正确但 30 秒门禁未过

- 已做：参考版探针新增只加载兼容 ROM 并聚焦打开数据库的 `M05` 阶段，避免主页签扫描触发数组越界弹窗。M00 在线采集器增加旧版固定菜单命令、启动器关闭同步、无中文标题依赖的文件框/数据库识别、按 PID 原生窗口枚举、ListBox 行选择及同名 CDL sidecar。使用参考版配套 `测试.nes` 先做无修改保存，将 38 处首次保存归一化与字段写入分开；随后选中列表第 1 行“弓天使”，移动力 8→7 仅变化 ROM `0x48A4F`，全新进程回读 7。探索摘要归档 `output/verification/legacy-ui-probe-m05-20260917-diff/movement-discovery.json`；正式配方、before/after、差分和新旧 PID 归档 `output/build/legacy-diff-audit/cases/legacy_live/M05/movement/cold_start_01/`。参考程序运行时生成的 `references/legacy_modifier/自动备份/2026年9月17日/` 已删除，参考区恢复零改动。
- 未做：正式用例总耗时 62.272 秒，超过 M00 的 30 秒硬门禁，因此档案明确 `passed=false`，未把移动力或 F-015 升级为“已对齐”；速度、强度、防御、HP、经验、金钱和四成长仍未采集。M03/M04 二级设置弹窗和逐字段差分仍未取得，两个写入门禁继续保持。没有填写 M03—M05 用户验收表，没有运行 ROM builder、模拟器冒烟或 EXE 打包。
- 验证：探索闭环请求值 7、唯一变化偏移 `297551 (0x48A4F)`、全新 PID 回读值 7；正式闭环同样无意外偏移且重开匹配，但 `within_budget=false`。相关黄金、地图、数据库及截图 UI 回归 227/227 通过（含采集器 11 项）；`py_compile` 检查 4 个改动 Python 文件通过；黄金快照复算 63/63 一致、0 fail、0 missing，但覆盖率报告按用例门禁正确记录 golden 59 通过/1 未通过，G1=58/62、G2=60/60；`git diff --check` 通过，`git status -- references` 无改动。
- 风险与阻塞：参考版 32 位 GUI 在当前 64 位 Python/桌面环境下两次冷启动耗时超过门禁；不能通过放宽预算或只保留探索结果规避 M00 约束。配套测试 ROM 首次保存存在 38 处格式归一化，后续字段样本必须从已规范化基线复制，避免把归一化噪声误判为字段差分。
- 下一步：先把正式采集的窗口控制改为更轻量的 32 位驱动或复用常驻观察进程，在仍保留两个独立参考进程的前提下压到 30 秒内；通过后以同一规范化基线补完其余 10 个数值字段，再返回 M03/M04 二级弹窗差分。

## 2026-09-18 — M06/M07 并行实现 / 共用人物、武器与动画写入保护

- 已做：按用户要求将 M06、M07 一并推进，并在正式队列记录本轮顺序调整。人物页为显式共用属性记录、共用头像记录及正面/背景 CHR 上传增加提交前影响审计：按实际指针或物理图块反查人物 ID，弹窗列出全部受影响项并默认取消；头像上传暂存状态也直接显示实际使用者。武器页在属性指针旁显示共用六字节记录 ID，直接修改、复制、还原属性以及我方/敌方共享动画参数时统一列出受影响武器并默认取消；确认后仍与名称、特技、距离补正和动画写入组成单一事务。使用说明、M06/M07 第 8 节、需求拆分队列及主需求第 8.1/11.5/11.6 节同步更新。
- 未做：M06 人物名称与战斗名称的独立语义、战斗/防御/变形台词绑定和新增人物仍未实现；人物属性、精神、头像各字段尚未形成参考版逐字段黄金档案。M07 的动画播放、武器模式、新增武器/脚本和规律四页签仍未实现，六字节字段与双方动画仍缺正式黄金对照。没有填写 M06/M07 用户验收表，没有构建 ROM、运行模拟器冒烟或重新打包 EXE；本轮未修改 ROM 布局、音频逻辑、生成 ROM 或参考区文件。
- 验证：`py_compile` 检查 `pages.py`、`database_records.py`、`character_editor.py` 通过；`tests.test_character_weapon_feedback` 19/19 通过，新增覆盖人物共用属性/头像默认取消、头像上传影响提示、武器共用六字节记录默认取消/确认同步写入、共享动画默认取消；数据库/动画/旧版窗口/数据浏览器/主界面联合回归 137/137 通过。`git diff --check` 通过。自动回归合计 156/156 通过。
- 风险与限制：影响确认能阻止静默联动，但不能替代参考版单变量保存、差分和全新进程回读；现有可编辑字段继续标记“已实现（未验证）”。人物 CHR 使用者按当前工程的正面和背景记录反查；确认后修改物理图块会按提示同步影响所有引用者。武器共享动画按双方各自的真实指针表计算，确认只授权本次等长参数变更，不解锁指针或控制流编辑。
- 下一步：从参考版兼容的已归一化基线分别采集 M06 精神值/成长/单补正/精神位/消耗/头像颜色和 M07 射程/命中/三地形火力/特技/距离补正的最小样本；将通过 30 秒门禁的样本登记为黄金用例，再按证据决定是否补台词绑定和规律入口。

## 2026-09-18 — M06/M07 当前底板实现收口 / 双名称、完整槽位、人物台词与武器动画入口

- 已做：人物目录扩至真实 `$01—$C8` 200 槽，武器目录扩至 `$01—$FF` 255 槽；人物目录名称与战斗名称按两套指针表在 395 字节共用池内去重重排，分别编辑、复制、还原并对超容执行零写入。新增 `CharacterDialogueCodec`，验证 0xD119 的 200 项指针、bank `$06` 内 59 条唯一人物台词记录、8 个直接绑定及三组 `FF` 结束的四字节特殊攻击规则；人物页接入 5 攻/6 防编辑，共享台词记录写入及还原均列出受影响 ID 并默认取消。动态清空取证确认 0x1596D 是 15 个有效槽加结束项的变形起飞绑定表，产品可在原 64 字节固定区内添加、清空和重排人物/机体范围/`05` 段对话编号，不移动后续全局表。武器页补齐 `$FF` 编辑、敌我代码入口、规律窗口、使用机体反查及“暂存→导出测试 ROM→Mesen”动画测试；六字节属性和动画共享写入保持显式确认。修复名称池重排工程保存时把新指针误序列化为旧池语义引用的问题，名称及人物台词改动均已验证工程保存/重开逐字节一致。使用说明、M06/M07 第 8 节、队列表、研究记录和主需求第 11 章已同步；F-035 从未实现转为已实现未验证。
- 未做：没有代填 M06/M07 用户验收表，也没有把“已实现未验证”升级为“已对齐”。人物新增 `$C9` 仍因名称/属性/头像三固定池均满而拒绝；参考版这一路径保存会跨全局区改写 17,007 字节，未照搬。特殊攻击规则只修改现有项，不增删变长规则。武器 `$01—$FF` 已满，参考版添加无动作，产品明确提示容量；独立动画脚本新增/搬移归 M12。旧版“代码编辑”输入框默认 `0080` 的具体指针语义仍无写入证据，产品保留已验证的等长指令编辑，不猜测改写。没有重新打包 EXE，也没有执行 Mesen 人工实战播放。
- 验证：参考版动态探针确认人物列表 200、武器列表 255、两名称字段独立、台词规则窗口字段、5 攻/6 防控件、变形列表数量、变形清空差分和武器代码/规律/动画测试入口；变形清空报告保存在 `output/build/legacy-diff-audit/cases/exploratory-m06-m07/report.json`。`tests.test_character_weapon_feedback` 最终 30/30 通过，覆盖 200 条人物属性/头像/台词无损往返、双名称独立编辑与工程重开、名称池超容原子拒绝、共享记录编辑及还原默认取消、变形表固定区重排、`$FF` 武器编辑、动画规律与 Mesen 启动。完整 `unittest discover -s tests -t . -p "test_*.py"` 为 547/547 通过，用时 342.741 秒；构建过程生成推荐 ROM SHA-256 `82C218275459D53C0F306F6BC036C4797316976E0FA7AD1D5A8247E338995B8E`。`py_compile` 和 `git diff --check` 通过，`git status -- references` 无改动。
- 风险与限制：动态探针显示参考版清空四条变形绑定会连带移动后续表并产生 455 个变化字节；当前固定区重排是保持运行时格式且不移动后续数据的安全实现，仍需 Mesen 实战确认新增/清空后的选择条件。黄金对照、实战播放和用户验收是状态升级证据，不是剩余代码占位。探针脚本和隔离输出位于 `output/build/`，未纳入产品源码；公开分发时仍应排除 ROM 副本。
- 下一步：按 M06/M07 第 5/7 节采集正式逐字段黄金档案并执行 Mesen 人工播放，然后交用户验收；代码实现队列回到顺延的 M17，之后进入 M08 基础编辑。

## 2026-09-18 — 关卡切换卡顿修复 / 名称容量缓存与部署控件复用

- 已做：定位 M06 双人物名称池改造带来的性能回归：人物名称每次解码都会重新扫描普通/战斗两张指针表，关卡部署下拉首次枚举人物时形成数万次重复指针读取；同时机体/武器名称和成长说明反复复制整份 ROM，少记录关卡切到多记录关卡时还会销毁并重建上百个 Qt 单元格控件。人物名称编解码器现按两张指针表的原始字节签名缓存容量映射，签名变化即自动重算且缓存最多保留 8 版；只读名称与武器记录直接读取工程缓冲区，关卡内成长表每次工程刷新只解析一次。部署/事件表把移除行的下拉框和数值框放入列级复用池，之后切换关卡直接复用并重新绑定共享模型和值。同步更新使用说明中的关卡切换性能数据，并重建正式 EXE 与映射表。
- 未做：本轮没有运行完整 discovery、ROM builder 或模拟器实战；没有改变地图、部署、事件、人物、机体或武器的 ROM 写入协议，也没有更新模块验收状态。未代替用户在实际桌面操作节奏下做主观流畅度签收。
- 验证：可见离屏窗口使用推荐 ROM 连续切换前 12 关，首次平均 `22.2 ms/关`、最慢 `75.1 ms`，反向已访问关卡平均 `9.4 ms/关`、最慢 `12.0 ms`；代表性的空部署关与 18 敌军关反复往返由约 `50 ms` 降至约 `10 ms`。人物/武器/地图定向回归 56/56 通过，主窗口与既有性能回归 66/66 通过，合计 122/122。`git diff --check` 通过。`build_modifier_exe.ps1` 成功，正式 EXE 隐藏 `--self-test` 返回 0，SHA-256 为 `CB075CA7275B213938FD15546807C27CC6CCB8FC899B197CCBBB92E646A37775`，校验文本一致。
- 风险与限制：首次进入此前未出现过的最大部署规模时仍需创建必要的 Qt 控件，但不会在往返关卡时重复创建；缓存均以指针表签名或工程刷新为失效边界，名称重排、撤销/重做及切换 ROM 不会沿用旧容量。当前测量来自同机 Qt 可见离屏页面，实际 Windows 绘制和安全软件扫描可能增加固定耗时。
- 下一步：由用户使用新打包的 `output/app/新DC篇完整修改器.exe` 复测关卡切换；若仍出现秒级停顿，记录发生卡顿的两个关卡 ID、是否刚编辑过地图及是否展开图标库，再针对该特定路径补采样。

## 2026-09-18 — M03—M07 集中人工验收清单

- 已做：新增 `docs/M03-M07验收清单.md`，按当前交付边界整理验收准备、M03—M07 分模块检查、跨模块事务检查、问题登记模板和最终签收表，共 53 项可执行检查；清单固定记录当前验收 EXE 与推荐 ROM 的 SHA-256，并要求使用隔离副本、保存后重开及证据归档。M03/M04 明确按只读范围验收，M05 明确为部分实现验收，M06/M07 按当前底板已实现范围验收；在 `docs/需求拆分/README.md` 增加集中入口，并声明只有用户明确签收后才登记模块结论。
- 未做：用户尚未执行或签署本清单，没有填写任何通过、条件通过或不通过结论；没有据此更新 M03—M07 模块状态、模块第 8 节或主需求第 11 章。本清单工作未新增产品代码、ROM 写入、EXE 构建或功能测试，工作区中此前的关卡切换性能修复及其产物仍保持未提交状态。
- 验证：检查清单共 143 行，M03、M04、M05、M06、M07、跨模块检查、问题记录和签收章节均存在，A/M03/M04/M05/M06/M07/X 编号完整；`git diff --check` 无空白错误，仅报告工作区既有 LF/CRLF 转换提示。本轮为文档整理，未重跑自动测试；清单引用的当前构建仍是上一工作轮自检通过、SHA-256 为 `CB075CA7275B213938FD15546807C27CC6CCB8FC899B197CCBBB92E646A37775` 的 EXE，以及 SHA-256 为 `82C218275459D53C0F306F6BC036C4797316976E0FA7AD1D5A8247E338995B8E` 的推荐 ROM。
- 风险与限制：M03/M04 的人工通过最多证明只读观察和门禁行为符合当前交付，不能据此解除 D3 写入门禁；M05 仍包含“新增机体禁用”等未完成范围；M06/M07 的人工通过不能替代逐字段黄金档案，也不会自动升级为“已对齐”。后续若重新打包 EXE，清单中的固定哈希必须同步更新后再验收。
- 下一步：由用户按 `docs/M03-M07验收清单.md` 逐项执行并记录证据；收到明确签收结论后，再同步 `docs/需求拆分/README.md` 第 6 节、各模块第 8 节和主需求第 11 章。
