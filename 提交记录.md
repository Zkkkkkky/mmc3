# 提交记录

## 2026-09-10 18:12:08 +08:00 — 重组项目为核心五目录并保留 references

- 分支：`expanded-modifier`
- 基线提交：`8e4eb775296e35d479fefb5e43e15f3d55aaca9a`
- 改了什么：
  - 将项目整理为 `src/`、`tools/`、`tests/`、`output/`、`docs/` 五个核心目录，并将不可变素材与历史证据集中到独立的 `references/`。
  - 迁移修改器源码、ROM 构建代码、ASM、测试、第三方工具、文档和交付产物，统一修正启动、构建、打包、研究与清单脚本中的路径。
  - 为 GUI 和底层 ROM API 增加统一输出目录策略，默认导出到 `output/exports/`，并阻止向 `references/` 写入。
  - 同步更新 README、项目规范、修改器使用说明、交接文档、PDF 手册以及 SHA-256 文件清单。
- 怎么改的：
  - 使用 `src` 布局和 `pyproject.toml` 管理可安装包，启动器负责将 `src/` 加入导入路径。
  - 将活跃程序、测试、生成物和只读参考资料按职责重新归位，并保留历史提交记录原文。
  - 修复 FamiStudio ASM 音符表、PyInstaller 配置和各类资源映射的迁移后引用。
  - 重新生成 464K ROM、IPS、映射表、修改器 EXE、PDF 手册及交接清单。
- 影响范围：
  - `src/`
  - `tools/`
  - `tests/`
  - `output/`
  - `docs/`
  - `references/`
  - 根目录项目配置、启动脚本与说明文件
- 验证：
  - `.\.venv\Scripts\python.exe -m unittest discover -v -s tests -t . -p "test_*.py"`：184 项测试全部通过。
  - `.\.venv\Scripts\python.exe tools\run_dc_modifier.py --self-test`：通过。
  - `.\.venv\Scripts\dc-modifier.exe --self-test`：通过。
  - `.\.venv\Scripts\python.exe tools\build_dc_expanded_dual_audio.py`：构建通过；ROM SHA-256 为 `82C218275459D53C0F306F6BC036C4797316976E0FA7AD1D5A8247E338995B8E`，IPS SHA-256 为 `EFAEDAF6E474CDE676ECEC2AC119F5C5EB1C4B01C851A5C997FFBD01218DF823`。
  - `.\tools\build_modifier_exe.ps1` 与 `output\app\新DC篇完整修改器.exe --self-test`：通过；EXE SHA-256 为 `08F6D5A6371D9D8EB77CC66E025978DF5F53EB9D18A5256FCB368EC65DE3D301`。
  - 映射表双目录 5 个文件逐项一致；PDF 手册共 15 页并完成渲染检查。
  - `.\tools\更新文件清单.ps1` 与 `.\tools\验证交接包.ps1`：2283 个文件全部通过校验。

## 2026-09-10 22:44:06 +08:00 — 接入 SRW2 已验证全局表并强化无损编辑

- 分支：`expanded-modifier`
- 基线提交：`bb7f342578f43e8f8821a40a1a1c689481c94e8d`
- 改了什么：
  - 按参考 `SRW2_patched.exe` 的可观察行为建立一比一复刻差异矩阵，接入 99 项累计经验、4×16 距离命中补正、双击/伤害/命中公式、11 项道具效果和 6 组初始阵容。
  - 开放 24 项道具名称与价格编辑，保留未改 Token 别名、合法非规范名称池布局和精确还原语义。
  - 修复剧情 Unicode/Token、跨页事件、无效原始字节、CHR 画布及机体包导入导出中的草稿丢失和静默覆盖路径。
  - 地图标题和字库预览改用 ROM 内 12×12 点阵，新增符合参考导出协议的确定性 24-bit BMP 编码器。
  - 新增《机体碎片与物理武器图库解绑方案》并在文档索引加入入口；该文档是尚未实施的证据、分阶段方案、验证门槛和回退设计。
  - 同步更新使用说明、逆向研究规格、操作截图和可执行交付物。
- 怎么改的：
  - 新增 `LegacyGlobalDataSpec`/`LegacyGlobalDataCodec` 及 `RomProject` 读写、还原和撤销 API，对 23 个公式/道具立即数在载入和写入前校验 opcode、存储、调用与跳转上下文，并对双击公式同步写入双镜像。
  - 道具页仅写实际改动的名称或价格；名称局部编码复用原始 Token，还原时逐字节复制指针表和 184 字节文本池。
  - 剧情页独立跟踪 Unicode 与原始 Token 草稿，对真正双源改动和跨页同地址事件在任何写入前阻止；CHR 和 `.dcunit` 导出则先在内存叠加合法画布，只在文件写入成功后提交。
  - 根据参考窗口、静态字符串/调用点和 ROM 单变量差分更新截图脚本与文档，未获得可重复证据的字段继续保持只读。
  - 图库解绑方案选择“编辑层逻辑解绑、构建期全局静态链接、运行时保持原机制”为首选，并以攻击期组合页作为后备，本次不改动 ROM 运行时。
- 影响范围：
  - `src/dc_modifier/`
  - `src/fc_editor/`
  - `src/fc_rom_editor_core.py`
  - `tests/`
  - `docs/`
  - `tools/capture_dc_modifier_guide.py`
  - `output/app/新DC篇完整修改器.exe`
- 验证：
  - `.\.venv\Scripts\python.exe -m unittest discover -s tests -t . -p "test_*.py"`：255 项测试全部通过，耗时 269.519 秒。
  - 全局代码上下文签名已交叉验证 `references/rom/source/新DC.nes`、`references/rom/baselines/DC_kuorong.nes` 和 `output/rom/DC_kuorong_464K.nes`；16 项专项测试通过。
  - `.\tools\build_modifier_exe.ps1`：PyInstaller 构建、归档检查和隐藏窗口 `--self-test` 通过；EXE SHA-256 为 `BAF9F976D5CFA87D4BEDF3EA6B4368F35616ECB3779E69978C1F94F7743827E0`。
  - `git diff --check`：通过（仅显示 Git 行尾转换提示）。

## 2026-09-11 09:00:42 +08:00 — 按视频反馈优化修改器工作流与字库编辑

- 分支：`expanded-modifier`
- 基线提交：`3e32ae6a15ce6714eb3d953589bcde7e84264d62`
- 改了什么：
  - 优化启动页、主窗口、数据库、剧情和字库的小屏布局、滚动与状态提示，减少关闭编辑窗口后的重复刷新。
  - 地图加入具名部署/商店/事件列表、地图与表格选中联动、非模态编辑、拖动坐标和超容量草稿直达规划入口。
  - 数据库展示经运行时代码签名核验的实际外观记录与原始图库，新增武器使用机体反查、双向跳转和当前机体 `.dcunit` 导入导出。
  - 修正 ROM 字模解码，开放固定槽位点阵绘制、系统字体按页替换、清页及 `.dcfont` 导入导出，并同步修正地图标题显示。
  - 剧情增加全文检索、中文摘要、直接文字编辑与等长单指令复制粘贴，纠正全局行动/劝降和关卡上下文的关系。
  - 明确音乐分配与曲槽数据替换的区别；地图动画参考示例默认隐藏，未验证功能继续明确标注。
  - 更新使用说明、功能差异矩阵、验收记录、界面截图、EXE 和验证产物；纳入用户新增的 11 张并排功能差异截图，保留尚未实现功能的证据。
  - 按用户要求，仅将 `录制_2026_09_10_18_51_54_457.mp4` 从暂存区移出并加入根目录 `.gitignore`，视频文件保留本地，其余改动纳入本次提交。
- 怎么改的：
  - 隐藏页面标记为待刷新并在下次访问时更新；差异统计跳过相同的 4096 字节块并复用快照计数；地图/事件表批量加载时暂停绘制和冗余通知。
  - 记录 ID 集合不变时原位更新列表，刷新后仅加载一次选中记录；跳转前复制目标 ID，避免跨同步提交使用已失效的列表项。
  - 字模按三条 4×12 竖条、每字节两行四位像素及 0 为前景的格式编解码；只写选中的 18 字节槽位，保护行保留字节、E/F 别名及并发编辑基线。
  - 使用窗口事务、页内草稿和同地址冲突校验保护取消/撤销；新增 Qt 测试清理基类，显式处理无事件循环测试中的延迟删除。
  - 新增可重跑的字模派生 ROM 验证与真实 Qt 事件循环压力工具，保留正常 GC 测试、Mesen 日志和未解决压力场景的边界说明。
- 影响范围：
  - `src/dc_modifier/`
  - `src/fc_editor/codecs/dc_font.py`
  - `src/fc_rom_editor_core.py`
  - `tests/`、`tools/`、`docs/`
  - `output/app/`、`output/verification/`、`output/manifest/`
  - `功能差异截图/`、`.gitignore`
- 验证：
  - `.\.venv\Scripts\python.exe -u -X faulthandler -m unittest discover -v -s tests -t . -p 'test_*.py'`：正常 GC 下 308 项通过，耗时 290.560 秒，退出码 0。
  - `tools/verify_feedback_dialog_lifecycle.py`：真实 Qt 事件循环 20 轮通过，耗时 27.067 秒；10 次取消回滚、10 次确认后撤销，每轮窗口销毁且无顶层窗口残留，ROM 文件哈希不变。
  - 字模验证派生 ROM 仅修改 `DAC2` 字模一位；Mesen 17 项启动/音频 smoke 全部通过，运行 11309 帧。附加回放未读取目标字模，不作为游戏内字形黄金对照。
  - `.\tools\build_modifier_exe.ps1`：构建及 EXE 启动自检通过；EXE SHA-256 为 `806807CBA487972D84A2BBF97CF82AF87297762A98250E7E28B8252E9CB2C959`。
  - 仍有未定位风险：重复数据库测试中，强制 `QListWidget.clear()` 重建列表曾发生原生访问异常；不宣称全部崩溃已解决，也不把关闭 GC 的诊断运行当正式验收。
  - 本次 Git 交付阶段未重新运行测试或构建；上述结果来自本轮优化时已完成并保留的验证记录。人物头像/精神、完整动画、事件插入重定位等功能仍未补齐。

## 2026-09-11 12:10:30 +08:00 — 按功能差异截图补齐修改器编辑能力

- 分支：`expanded-modifier`
- 基线提交：`bee3199c6d28f885c45ca7acb291629e058173eb`
- 改了什么：
  - 按 `功能差异截图/` 的 11 张对照图补齐机体、人物、武器、文字、成长、商店、关卡和地图动画的已验证编辑能力，并继续保护尚未确认的变长、重定位和追加记录操作。
  - 机体页增加基础经验、特技、地形低位编辑和外观事务窗口；按真实脚本合成主体预览，分开显示机体与碎片图库，并用两位 NES 色号、实时色块和运行时地址明确两组配色。
  - 人物页增加精神值与成长、五项修正、24 种精神、头像编辑及旧版目录格式的正反面 BMP 导出；武器页增加特技、距离修正和双方实际动画脚本的等长参数编辑。
  - 接入战斗/系统/道具文字、成长方式、有效商店和三阶段真实关卡指令；地图动画窗口读取实际动画、规律与调用记录，并支持已确认字段的有界修改。
  - 默认展开三组地图图标图库，同步更新使用说明、差异矩阵、逆向记录、界面截图、验证证据、可执行文件和 SHA-256 清单。
- 怎么改的：
  - 新增人物属性、动画、旧版文字、成长、商店和分 Bank 关卡指令编解码器，通过原码签名、固定容量、等长指令和共享记录检查生成补丁。
  - 将新增页面接入统一事务、取消/撤销、项目保存重放和同物理地址草稿冲突检测；武器射程与特技、机体类型与地形等共用字节采用掩码编码并保留其余位。
  - 机体主体预览解析 `F3`、`FD 20`、`F9`、原始图块和 `FF` 指令，按活动 CHR 与 `FCEUX.pal` 渲染；配色字段按 `$0476～$047B` 的运行时装载顺序复核。
  - 头像导出按人物头像记录读取 4×4 图块、位置和素材四色，生成确定性的 32×32、24 位正反面 BMP；不修改当前 ROM。
- 影响范围：
  - `src/dc_modifier/`
  - `src/fc_editor/`
  - `src/fc_rom_editor_core.py`
  - `tests/`、`tools/`、`docs/`
  - `output/app/`、`output/verification/`、`output/manifest/`
- 验证：
  - `.\.venv\Scripts\python.exe -m unittest discover -s tests -t . -p "test_*.py"`：369 项测试全部通过，耗时 247.113 秒。
  - Qt 真实事件循环连续 8 轮数据库跨页暂存、取消、重开和销毁通过，输入 ROM 哈希保持不变。
  - `.\tools\build_modifier_exe.ps1`：PyInstaller 构建通过；EXE SHA-256 为 `EF2D8365C0109DA125FA20D98CB16C875CCB1D595E01F7F178E7E19A08FACEB2`。
  - `.\tools\更新文件清单.ps1` 与 `.\tools\验证交接包.ps1`：2404 个文件全部通过校验。
  - 推荐 ROM SHA-256 仍为 `82C218275459D53C0F306F6BC036C4797316976E0FA7AD1D5A8247E338995B8E`；本轮未执行修改后动画和商店的逐项模拟器播放验收。

## 2026-09-11 13:12:20 +08:00 — 接入完整 ROM 数据读取

- 分支：`expanded-modifier`
- 基线提交：`15de10587300b9816ba19f65cd94468211a5afaf`
- 改了什么：
  - 新增“数据 → 完整ROM数据读取”，集中列出机体、人物、武器、地图与部署、文字、动画和两套关卡事件指令，并提供覆盖当前内存镜像全部字节的 HEX 查看与地址跳转。
  - 数据库默认定位到有实际属性和可见图库的机体 `$09`、有实际人物属性的 `$04`，同时常驻显示机体 16 字节、人物属性/头像和武器属性/名称 Token 原码。
  - 属性计算器默认载入人物 `$04`、机体 `$02`、武器 `$01`，显示人物与武器记录地址、原码、精神、成长、补正、特技和距离补正，避免初始界面只出现占位零值。
  - 同步更新使用说明、差异矩阵、说明截图、可执行文件和 SHA-256 清单。
- 怎么改的：
  - 使用只读 `QAbstractTableModel` 和筛选代理索引 255 条机体、199 条人物、254 条武器、100 张地图、1258 条文字、1177 条动画和 6911 条事件指令；双击结构化记录按文件偏移跳到分页 HEX 视图。
  - 为人物属性编解码器补充已验证记录偏移与完整原始字节读取接口；未确认语义的位继续按原码展示，不开放猜测写入。
  - 为可搜索记录页增加仅在首次打开时生效的推荐记录钩子，保留用户当前选择和所有全零占位槽。
- 影响范围：
  - `src/dc_modifier/`
  - `src/fc_editor/codecs/character_attributes.py`
  - `tests/`
  - `tools/capture_dc_modifier_guide.py`
  - `docs/`
  - `output/app/`、`output/manifest/`
- 验证：
  - `.\.venv\Scripts\python.exe -m unittest discover -s tests -t . -p "test_*.py"`：373 项测试全部通过。
  - `.\.venv\Scripts\python.exe tools\capture_dc_modifier_guide.py`：全部说明截图生成通过，并目视核对数据库、完整 ROM 数据读取和属性计算器页面。
  - `.\tools\build_modifier_exe.ps1`：PyInstaller 构建通过；EXE SHA-256 为 `2317A0D14B6B27421D89E2EB0F6A8B50999E1935652F4CA7FB1E3E6F19BB8B39`。
  - `.\tools\更新文件清单.ps1` 与 `.\tools\验证交接包.ps1`：2409 个文件全部通过校验。
  - 推荐 ROM SHA-256 保持 `82C218275459D53C0F306F6BC036C4797316976E0FA7AD1D5A8247E338995B8E`；`git diff --check` 通过，仅有行尾转换提示。

## 2026-09-11 17:57:19 +08:00 — 修正机体拼图显示

- 分支：`expanded-modifier`
- 基线提交：`a12e2c36976d298aaa17bc0019f086a0676e1bf7`
- 改了什么：
  - 数据库机体页改为显示由主体脚本、主体图库和机体三色合成的完整机体，不再把原始 CHR 图块条作为主图；原始机体与碎片图库移入折叠技术详情。
  - “机体拼图与配色”窗口分为主体拼图、碎片原始图库和拼图脚本原码三个页签，主体图库按 8×8 图块纵向排列，拼图结果增加网格，并清楚分开两套配色。
  - 补充“功能差异截图”中的参考修改器、制作中修改器对照截图和待改进点说明，共新增 45 个文件、8,787,608 字节。
  - 同步更新使用说明、逆向记录、说明截图、可执行文件与 SHA-256 清单。
- 怎么改的：
  - 数据库主预览改用已验证的主体脚本合成器；图库渲染器增加可选列数和网格覆盖，用于还原参考窗口的纵向图块库与 128×128 拼图核对布局。
  - 主体与碎片脚本完整读取后放入只读原码页签；机体与碎片继续分别使用记录中的 `$0476～$0478` 和 `$0479～$047B` 三色。
  - 新增 GUI 回归断言，确认数据库主图来自脚本合成、原始图库默认折叠、三个页签及两段脚本原码均已显示。
- 影响范围：
  - `src/dc_modifier/database_graphics.py`
  - `src/dc_modifier/legacy_windows.py`
  - `src/dc_modifier/unit_appearance_dialog.py`
  - `tests/`、`tools/capture_dc_modifier_guide.py`
  - `docs/`、`功能差异截图/`
  - `output/app/`、`output/manifest/`
- 验证：
  - `.\.venv\Scripts\python.exe -m unittest discover -s tests -t . -p "test_*.py"`：374 项测试全部通过，耗时 268.328 秒。
  - 最终布局调整后运行 `.\.venv\Scripts\python.exe -m unittest tests.test_database_feedback tests.test_screenshot_unit_feedback`：26 项测试全部通过，耗时 39.528 秒。
  - `.\.venv\Scripts\python.exe tools\capture_dc_modifier_guide.py`：说明截图生成通过，并目视核对数据库完整机体与拼图窗口布局。
  - `.\tools\build_modifier_exe.ps1`：PyInstaller 构建通过；EXE SHA-256 为 `DD1F51C99D320A8882EDA569374780302CF2E33ADA9F232234CFE07A0E26B0AC`。
  - `.\tools\更新文件清单.ps1` 与 `.\tools\验证交接包.ps1`：2454 个文件全部通过校验。
  - `git diff --check` 通过，仅有既有行尾转换提示。

## 2026-09-12 15:01:30 +08:00 — 完善修改器地图、机体与数据编辑

- 分支：`expanded-modifier`
- 基线提交：`cc22fa311ea055bcdbcbbd767d524968ea380d5e`
- 改了什么：
  - 按旧修改器和游戏内效果完善地图图块、真实 NES 配色、标题、机体拼图与机体图标预览，修正颜色表、背景色路由、碎片坐标和部署字段含义。
  - 重排数据库机体页面和地图初始配置页面，增加可视化颜色选择、数值上下箭头、紧凑图标图库、清晰部署图标及完整部署明细。
  - 将地图事件和初始配置接入地图右键操作，可在准确坐标编辑、添加或删除记录，并保留拖动、草稿、容量校验、应用和撤销流程。
  - 收录本轮差分研究脚本、模拟器探针、候选构建、验证截图、映射表及重新生成的正式修改器 EXE。
- 怎么改的：
  - 依据 ROM 属性表、活动 CHR、运行时调色板和旧版布局证据，统一地图、图块属性、机体主体/碎片和小图标的渲染路径；对透明机体图标增加高对比底板与阵营边框。
  - 修正敌军和客军部署记录为 `X、Y、驾驶员、机体、等级、标志` 的真实顺序，并用机体记录 `+2` 路由 `$34/$35/$36` 三组四图块小图标。
  - 为初始配置和商店事件分别建立画布右键菜单，将菜单操作绑定到对应表格行；空格新增时按阵营容量创建默认记录，有记录时精确联动编辑与删除。
  - 增加颜色、图块、机体预览、部署右键、响应式布局及字段级差分测试，并同步更新使用说明和复刻研究记录。
- 影响范围：
  - `src/dc_modifier/`
  - `src/fc_editor/`、`src/fc_rom_editor_core.py`
  - `tests/`、`tools/`、`docs/`
  - `output/app/`、`output/build/`、`output/mappings/`、`output/verification/`
- 验证：
  - `.\.venv\Scripts\python.exe -m unittest tests.test_map_feedback_ui tests.test_legacy_map_ui tests.test_expansion_integration tests.test_dc_modifier tests.test_legacy_windows_ui tests.test_rom_data_browser`：127 项测试全部通过，耗时 259.053 秒。
  - `.\tools\build_modifier_exe.ps1`：正式 EXE 构建通过；独立 `--self-test` 退出码为 0。
  - 正式 EXE SHA-256 为 `B0080885D3901492BBA6D178951F7C21407F6F243F32236BE9C917AB4A57A769`，与生成的校验文件一致。
  - 本轮相关文件的 `git diff --check` 无错误，仅显示既有 LF/CRLF 转换提示。

## 2026-09-12 23:01:11 +08:00 — 优化地图部署与机体拼图编辑

- 分支：`youhua_hu`
- 基线提交：`cface3f4647265d118121bd8be90e408f2f450a4`
- 改了什么：
  - 将地图事件和全关卡初始配置接入地图右键编辑，补全敌军、客军和我方出击位的新增、修改、删除、拖动、复制与明细查看，并恢复真实四图块机体图标。
  - 机体图标图库改为随关卡动态选择三组 CHR 页，移除阵营边框并压缩地址区留白；地图图块补回海地形读取，部署悬停提示和页面切换响应更流畅。
  - 数据库复用窗口实例并减少无变更关闭时的全量刷新，增加地图到机体/人物数据库跳转、属性计算和击落经验计算；机体特技、变形关系与基础金钱显示按旧修改器行为完善。
  - 机体主体和碎片拼图开放十六进制脚本编辑、即时预览、移动、清除及碎片翻转；保存时安全重排扩容资源池并更新全部指针。
  - 同步更新使用说明、差异研究、录像抽帧、差分审计、验证截图和正式修改器 EXE。
- 怎么改的：
  - 为各关卡建立动态机体图标页路由，统一真实 CHR 四图块渲染和地图部署命中测试，并缓存图标、图块、选择项标签和部署描述以降低切关延迟。
  - 为初始配置构建地图单元格编辑窗口和阵营感知的上下文菜单，空我方槽保持可编辑但不伪造地图标记；海属性同时采用已验证表值和旧版基准推断补齐显示。
  - 基础金钱在界面按 ROM 十位单位放大显示并按十位回写；拼图脚本先按已验证指令解码，随后通过扩容链接器重排全部主体/碎片记录，容量或指令异常时拒绝保存。
  - 数据库窗口仅在底层工程变化时刷新，未改动关闭不再重复构建所有表格与 CHR 预览；打包规格补充运行期所需资源。
- 影响范围：
  - `src/dc_modifier/`
  - `src/fc_editor/`、`src/fc_rom_editor_core.py`
  - `tests/`、`tools/dc_modifier.spec`
  - `docs/`
  - `output/app/`、`output/build/`、`output/verification/`
- 验证：
  - `.\.venv\Scripts\python.exe -m unittest discover -s tests -t . -p "test_*.py"`：425 项测试全部通过，耗时 1335.436 秒。
  - 拼图资源重排与基础金钱两个新增定向用例再次执行：2 项全部通过。
  - `.\tools\build_modifier_exe.ps1`：PyInstaller 构建通过；正式 EXE SHA-256 为 `BCD926B9FE3326DBE503D40DCBD222A77BD961A42775E8314837E86CD29B786B`。
  - 打包后的 EXE 实际启动检查通过；`git diff --check` 通过，仅有既有 LF/CRLF 转换提示。

## 2026-09-14 14:38:47 +08:00 — 补充旧版修改器复刻资料与探测证据

- 分支：`legacy-ui-research`
- 基线提交：`6d25d124f55fd47f90b028eceec76b35050ea6c2`
- 改了什么：
  - 新增《SRW2修改器1比1复刻需求文档》v1.1，以 L1 界面、L2 动线、L3 字节写入为验收层级，整理 114 个功能点、黄金对照要求、当前实现状态、待决策项和用户配合事项。
  - 新增旧版修改器 Win32 自动探针及 263 份验证产物，包括 131 张界面截图、130 份控件树 JSON、控件汇总和探测总结，覆盖启动器、ROM 载入、菜单、数据窗口、页签、次级对话框及否定证据。
  - 新增 Qoder 生成的中文仓库 Wiki，覆盖项目概述、用户手册、开发者指南、API 参考、构建部署与故障排除；同时加入 `git-add-commit-push.zip` 技能包。
  - 在使用说明和文档索引中补充“已实现需求规格”的入口。
- 怎么改的：
  - 探针使用 ctypes 调用 Win32 API，枚举窗口、菜单和控件树，驱动页签及对话框流程，并通过 PrintWindow 输出配对的 PNG 与 JSON；参考 EXE、ROM 和存档均使用工作副本，`references/` 保持只读。
  - 需求文档按 E1—E5 证据等级汇总探针、差分审计和现有实现，明确逐字段黄金对照闭环、超集功能保留决策及 D2—D6 后续决策入口。
  - Wiki 按源码模块生成分层架构、编解码器、资源管理、GUI 和操作说明；ZIP 内包含技能说明与代理配置。
- 影响范围：
  - `docs/`
  - `tools/research/legacy_ui_probe.py`
  - `output/verification/legacy-ui-probe/`
  - `.qoder/repowiki/`
  - `git-add-commit-push.zip`
- 验证：
  - 提交前检查确认仓库不处于合并、变基、拣选或回滚状态，基线 HEAD 为 `6d25d124f55fd47f90b028eceec76b35050ea6c2`。
  - 探测总结记录 130 个 JSON 均可解析、全部 PNG 存在且非空；本次核对验证目录共 263 个文件、17,992,193 字节。
  - `tar -tf git-add-commit-push.zip`：确认归档包含 `git-add-commit-push/SKILL.md` 和 `git-add-commit-push/agents/openai.yaml`。
  - 本轮未额外运行自动化测试；本次提交为研究脚本、验证证据和文档更新。

## 2026-09-14 22:31:00 +08:00 — 按模块拆分1比1复刻需求文档

- 分支：`legacy-ui-research`
- 基线提交：`9424fe8eb5f97c10d53215da670902e8a8c65aab`
- 改了什么：
  - 新增 `docs/需求拆分/`：README 总览 + M00 黄金对照流水线 + M01–M18 按界面模块的 19 份模块需求文档，每份含范围界定、功能点清单、参考版界面行为规格、数据写入规格、黄金对照采集清单、实现注意事项、用户验收清单与状态跟踪八节；114 个功能点编号与主文档第 11 章一一对应，全局无缺失无重复。
  - 主文档升级 v1.2：新增版本变更记录与 1.2 节拆分目录指引；同时纳入用户对 4.12 节剧情事件底部按钮 690 的口径修正（检查范围覆盖剧情、界面/回合事件、即时事件、行动/劝降/地图事件六类）与全文表格格式化（约 750 行对齐式重排，无语义变化）。
  - 新增根目录《SRW2修改器1比1复刻需求文档.md》副本（编辑会话产生的与 docs 版内容一致的副本）。
  - 新增 `output/build/m10m18/` 四个拆分自验脚本（menu_check.py、menu_check2.py、selfcheck.py、tabs_c05.py）。
- 怎么改的：
  - 两名实现代理按全局契约并行拆分（上半区 README+M00–M09、下半区 M10–M18），共用统一八节模板与模块清单；随后由验证代理做跨区一致性核验并修复四处瑕疵（README 总表 M17 名称、M08 优先级口径、M05 证据路径笔误、F-S01–S17 超集编号锚点补全）。
  - M14 采用用户 2026-09-14 对按钮 690 的第一手口径并标注待补点击行为证据；D2–D6 决策项在对应模块文档中保持“待决策”未固化。
  - 主文档仅做三处最小编辑（版本行、变更记录、指引句），用户的 4.12 修改与表格格式化全部保留。
- 影响范围：
  - `docs/需求拆分/`（新增 20 个文件）
  - `docs/SRW2修改器1比1复刻需求文档.md`
  - `SRW2修改器1比1复刻需求文档.md`（根目录副本）
  - `output/build/m10m18/`
- 验证：
  - 验证代理核验通过：20 个文件齐全、八节模板完整、F 编号 114 项全局对账无缺失无重复、D 标记正确、用户修改完好保留；56 个证据路径抽查 55 个存在，唯一无效路径已修复；四处建议性问题修复后全部关闭。
  - `git status` 确认 `src/`、`tests/`、`references/` 零改动；本轮为文档拆分与验证脚本提交，未运行产品单元测试。

## 2026-09-15 00:02:14 +08:00 — 补充旧版证据并启用三图头像导出

- 分支：`legacy-ui-research`
- 基线提交：`c070578e03807bb03b8c0e85f5d7057aa5a138f3`
- 改了什么：
  - 归档用户提供的参考版真实操作录屏、255 个机体槽位的 1,275 个 BMP 黄金样本、录屏接触表和文件哈希，补齐初始配置、商店事件及导出行为证据。
  - 将 D2 定为新修改器修复并启用头像导出，选择目录和人物后生成 `[背面].bmp`、`[正面].bmp`、`[效果].bmp` 三张 32×32、24 位 BMP；将用户录屏登记为 D3 的真实操作依据。
  - 同步更新主需求、模块拆分、差异矩阵、研究验收记录和用户使用说明，标记 D2/D3 已回答及 B1—B3 证据已到位。
- 怎么改的：
  - 头像效果图使用正面图层的非零色号像素覆盖背面图层，复用既有旧版四色和确定性 BMP 编码器；全局菜单与人物页导出入口统一检查覆盖并报告三个输出文件。
  - 原始 1920×1080 录屏转为 1280×720、30 fps 的 H.264/AAC 研究副本；机体 BMP 目录树无损压缩为 ZIP，并记录原始与归档文件 SHA-256。
  - 扩展头像导出测试，覆盖三文件命名、32×32/24 位 BMP 结构、效果合成、ROM 不变性、全局菜单和人物页入口。
- 影响范围：
  - `src/dc_modifier/portrait_export.py`、`src/dc_modifier/app.py`、`src/dc_modifier/database_records.py`
  - `tests/test_portrait_export.py`、`tests/test_legacy_windows_ui.py`
  - `docs/`、`output/verification/legacy-ui-probe/`
  - `output/verification/legacy-user-evidence-2026-09-14/`
- 验证：
  - `.\.venv\Scripts\python.exe -m unittest tests.test_portrait_export tests.test_legacy_windows_ui`：45 项测试全部通过，耗时 130.854 秒。
  - `.\.venv\Scripts\python.exe -m unittest tests.test_dc_modifier_ui`：47 项测试全部通过，耗时 204.187 秒。
  - 压缩录屏已完整解码验证；机体样本 ZIP 已核对为 1,275 个 BMP；`git diff --check` 无错误，仅有既有 LF/CRLF 转换提示。

## 2026-09-15 07:23:39 +08:00 — M00黄金对照流水线离线阶段落地与启动器规格精简

- 分支：`legacy-ui-research`
- 基线提交：`c070578e03807bb03b8c0e85f5d7057aa5a138f3`
- 改了什么：
  - 新增 `tools/golden_pipeline_core.py`（纯标准库核心库：差分引擎 diff_roms、归一化剔除 classify_case、5 个存量 JSON 适配器、字段注册表 derive_registry）与 `tools/report_golden_coverage.py`（diff/archive/stats/verify 四个子命令）。
  - 新增字段注册表 `tools/golden_pipeline/cases/field_registry.json`（61 条目：M17 32 / M10 24 / M09 5，其中 discovery 3）与黄金档案 `output/build/legacy-diff-audit/golden/`（61 个 `<模块>-<字段>-<用例>.json` + index.json，四要素齐全，含 schema_version/case_kind）。
  - 新增统计报告 `output/reports/golden-coverage-report.json/.md`：G1=58/61=95.08%（denominator_scope=registered_fields，最终口径为主文档第 4/5 章全量字段数）、G2=58/58=100%，待解释（discovery）3 例（M09/experience_level_2、experience_level_60、level_cap）。
  - 新增 `tests/test_golden_pipeline.py`：核心库与产物一致性测试（归一化常量、差分语义、passed 判定、档案四要素、命名合规、G1/G2 算术口径、CLI 边界、双击第三镜像三方一致）。
  - 启动器规格精简（依 2026-09-14 决策）：主文档 §2.1/§4.1/功能矩阵/REQ 编号重写——移除参考版 4 外部链接与 QQ/QQ 群信息、作者署名改为 `断月残心`、启动器不参与对参考版截图的 1:1 比对；M01 模块文档 §1/§2/§3/§5/§7 联动同步；功能矩阵 F-001 行更新。
  - 文档与状态联动更新：M00 模块文档（头部状态、阶段表第 2/4/5 行、§6 追加离线驱动不可行与第三镜像 extra_allowed 两条决策记录、§8 状态跟踪追加 2026-09-15 三行）与需求拆分 README（M00 状态改部分实现）。
- 怎么改的：
  - 以 6 个审计脚本的 10 个存量 JSON 为数据源经适配器生成字段级档案，差分复算全部由核心库 diff_roms 完成，不改动既有产物；双击三参数第三镜像 0x7FF03/0x7FF12/0x7FF1A 依据 profiles.py double_hit_operand_groups 三镜像预填 extra_allowed，verify 复算中以 known-exception 分类（3 例）。
  - 归档与统计均由 CLI 子命令驱动并保持确定性（重跑 archive+stats 产物字节级一致）。
  - 阶段 1（采集器）与阶段 3（全新进程重开口径）未启动：决策记录确认参考 EXE 无法离线驱动（配置文件仅为显示名列表与码表，写 ROM 唯一路径是 GUI"确定→文件→保存"，rebuild_exe.py 证实纯 Win32 GUI），后续采集须走 pywinauto GUI 自动化。
  - 启动器精简决策：参考版启动器 8 控件（4 链接+QQ 群+兼容提示）全部不复刻，本产品启动器仅保留作者署名 `断月残心`+"进入修改器"按钮；REQ-L 编号从 5 条缩减为 3 条。
- 影响范围：
  - `tools/golden_pipeline_core.py`、`tools/report_golden_coverage.py`、`tools/golden_pipeline/`、`tests/test_golden_pipeline.py`
  - `output/build/legacy-diff-audit/golden/`、`output/reports/golden-coverage-report.json`、`golden-coverage-report.md`
  - `docs/SRW2修改器1比1复刻需求文档.md`、`docs/需求拆分/M00_黄金对照流水线.md`、`docs/需求拆分/M01_启动器与主窗口.md`、`docs/需求拆分/README.md`、`提交记录.md`
- 验证：
  - unittest 全绿（核心库单测验证）；verify 复算 61 用例 pass 61 / fail 0 / missing 0，3 个 known-exception（双击三字段第三镜像）。
  - 确定性复核：重跑 archive+stats 产物字节级一致。
  - `git status` 核验：references/、src/、6 个审计脚本、10 个存量 JSON 零改动。

## 2026-09-15 14:19:53 +08:00 — 新增M00在线采集器并改进M01/M02交互

- 分支：`legacy-ui-research`
- 基线提交：`e671ac363e85c102dbf0c3037c5a9faf03a098f9`
- 改了什么：
  - M00 增加参考版单字段在线采集、保存后全新进程重开、差分及 30 秒门禁的参数化工具；黄金档案支持同字段多用例，61 个既有档案和覆盖率报告按新格式重新生成。`cold_start_01` 的桌面窗口等待失败记录保留为诊断证据，未算作通过用例。
  - M01 进入主窗口时销毁启动器，未载入态显示禁用的三个空页签，整理参考菜单的文字、分隔及 14 个逻辑命令 ID；D1 派生 ROM 保存继续保护载入基线，快速重复保存的备份不再相互覆盖。
  - M02 主地图页宽、高改为可直接编辑的 1—32 数值控件，联动高级窗口、草稿画布和 RLE 容量显示；更新空主窗口与地图页截图，并重建可执行文件。
  - 归档用户 D2—D6 决策、C1 音频验收、B1 初始配置/商店事件录屏审计及机体导出样本协议；确立开发顺序和每次开发“已做／未做／验证／下一步”的持续日志。M00—M02 仍是部分实现，现场黄金对照和用户签核未完成。
- 怎么改的：
  - `tools/golden_pipeline_collect.py` 用白名单 JSON 配方驱动隔离参考 EXE，复制工作 ROM、确认落盘与进程退出，再以不同 PID 冷启动读取；`report_golden_coverage.py collect` 接入在线结果，档案索引改为 `module/field/case_id`，失败目录不误报黄金档案孤儿。两份失败尝试的 `before.nes`/`after.nes` 与已跟踪 `audit.nes` 的 SHA-256 均为 `C0F6FB15DBCC9F04D35EC97B9558DF85D099C661C35D54D3A2AAE4966D2ED5B2`。
  - Qt 菜单动作保存参考逻辑 ID 并支持 WM_COMMAND 分发；启动器关闭后由主窗口生命周期持有会话。地图主界面数值变更复用现有草稿重编码路径，并阻断同步控件的递归信号；派生文件备份采用微秒时间戳与碰撞后缀。
  - 当前产品代码变动同步写入 `docs/DC修改器使用说明.md`、模块状态与主需求动态跟踪；用户确认的决策与证据只更新相关文档，不把未采集的字节映射标成已对齐。
- 影响范围：
  - `src/dc_modifier/app.py`、`src/dc_modifier/map_page.py`、`src/fc_rom_editor_core.py`
  - `tools/golden_pipeline_collect.py`、`tools/golden_pipeline_core.py`、`tools/report_golden_coverage.py`、`tools/golden_pipeline/cases/`、`pyproject.toml`、`tests/`
  - `docs/`、`AGENTS.md`、`output/build/legacy-diff-audit/`、`output/reports/golden-coverage-report.json/.md`、`output/verification/legacy-user-evidence-2026-09-14/`
  - `output/app/新DC篇完整修改器.exe` 及其 SHA-256 文本；EXE SHA-256 `BCD926B9FE3326DBE503D40DCBD222A77BD961A42775E8314837E86CD29B786B` → `78008B2AFF9E1101FE13F3A0FE05F397CCB30445CAB1BB880E13123E8253D16C`
- 验证：
  - 开发轮运行 `.\.venv\Scripts\python.exe -m unittest discover -s tests -t . -p "test_*.py"`：500 项通过；M00 `verify`：61 pass、0 fail、0 missing（3 个 known-exception）；`stats`：G1=58/61=95.08%（仅已登记字段分母）、G2=58/58=100%。本次提交前未额外重跑测试。
  - `.\tools\build_modifier_exe.ps1` 构建成功，新 EXE 隐藏 `--self-test` 返回 0；更新的空主窗口与地图页截图已目视检查。完整测试生成 ROM SHA-256 `82C218275459D53C0F306F6BC036C4797316976E0FA7AD1D5A8247E338995B8E` 未变；参考 ROM、推荐 ROM 和 IPS 无本次工作区改动。
  - M00 真实桌面尝试在等待参考窗口时失败，`cold_start_01/error.json` 的两个 PID 均未确认；不能据离屏测试和既有档案宣称在线四步闭环或 M01/M02 用户验收通过。

## 2026-09-15 14:35:41 +08:00 — 归档M00—M02人工验收待办

- 分支：`legacy-ui-research`
- 基线提交：`cfab31c4b7de4c6e4a6f239ab09b632993d79189`
- 改了什么：
  - 新增 `docs/待人工完成事项.md`，将仍待交互式 Windows 桌面与用户签核的 M00—M02 工作列为 H-01—H-06：在线冷启动采集、M01 启动/打开/菜单及两级保存和备份双环境、M02 同基线地图与图块属性/只读核对、最终用户签核。
  - 在开发队列 README 和持续开发记录中添加该清单的交接入口；M00—M02 仍保持“部分实现”，没有把待办登记当作已通过证据。
- 怎么改的：
  - 每项人工操作对应应留的录屏、工作副本、重开值或失败 JSON，并区分现场操作者、实现侧自动差分/归档和用户最终签核；要求参考材料只读、两版使用同基线的独立副本，新修改器保存遵守 D1 派生 ROM 例外。
  - 开发记录仅追加文档归档条目，不重写既有开发历史；需求拆分 README 仅增加指向新清单的链接，未改变正式开发队列。
- 影响范围：
  - `docs/待人工完成事项.md`、`docs/需求拆分/README.md`、`docs/开发记录.md`
- 验证：
  - 核对新清单引用的 M00/M01/M02 模块文档、B03 控件树、M17 `cold_start_02` 配方以及隔离 `audit.nes`/参考 EXE 路径，均存在；检查待提交差分只涉及上述交接文档。
  - 本轮未运行产品测试、ROM 构建或 EXE 打包（仅文档变动）；之前 500 项通过的结果未作为本轮重跑结果。

## 2026-09-15 19:47:48 +08:00 — 修复未载入地图占位与派生ROM重开

- 分支：`legacy-ui-research`
- 基线提交：`b9c6fa6287f7e15055de0936176dfbde243fa38d`
- 改了什么：
  - 未载入 ROM 的主窗口不再显示虚构的绿色单格与纯色图块，保留禁用编辑和 `Ctrl+O` 打开入口；载入后仍显示 ROM 中的真实图块与地图。
  - 新 DC 扩容布局保存出的无容量规划表派生 ROM 可在全新窗口重新打开并读回字段、地图图块；固定代码损坏或预留扩展区含未登记数据仍被拒绝，带规划表的输出仍按规划校验。
  - 同步使用说明、需求状态、M01/M02 跟踪和两轮开发记录，更新空状态截图并重建 EXE；已提交的参考 ROM、推荐 ROM 与 IPS 未改动。EXE SHA-256 `78008B2AFF9E1101FE13F3A0FE05F397CCB30445CAB1BB880E13123E8253D16C` → `F42E9A064CF0CEB1402BE3AF2F7B29B07E1BD71FBDD7D7F9E5E30F453A1871FD`。
- 怎么改的：
  - `MapCanvas` 无项目时从 0×0、空图块开始，笔刷与图块按钮显示中性空位，地图页和状态栏提示载入 ROM；主窗口载入成功后恢复现有 ROM 绘制路径。
  - `MainWindow.load_rom` 对非基准哈希的新 DC 扩容无规划输出检查已认证固定代码、预留 Bank 全零与项目完整性，不再一律要求自动容量表；补保存后新窗口重开及拒绝损坏输入的离屏 UI 回归。
- 影响范围：
  - `src/dc_modifier/app.py`、`src/dc_modifier/map_page.py`、`tests/test_dc_modifier_ui.py`
  - `docs/DC修改器使用说明.md`、`docs/SRW2修改器1比1复刻需求文档.md`、`docs/开发记录.md`、`docs/需求拆分/M01_启动器与主窗口.md`、`docs/需求拆分/M02_战场地图.md`、`docs/images/dc_modifier/00b-empty-main.png`
  - `output/app/新DC篇完整修改器.exe` 及其 SHA-256 文本
- 验证：
  - 未载入态及实际图块的定向 UI 测试、地图相关 39 项测试通过；派生 ROM 字段/地图保存重开、固定代码与未登记资源拒绝等 5 项定向 UI 测试通过。
  - 完整 `unittest discover` 在新增地图单格回归前运行 502 项，501 项通过、1 项失败；失败用例为 M00 `test_win32_adapter_waits_for_disk_save_and_process_exit`，独立重跑仍失败。Windows 快速写入后修改时间可不变，既有 M00 适配器仅以该时间判断落盘；本轮未修此独立问题，不宣称全套通过。
  - `./tools/build_modifier_exe.ps1` 构建成功且隐藏 `--self-test` 返回 0；`git diff --check` 通过。截图所示 `D:\GIT\mmc3\output\rom\新修改器work.nes` 当前环境不存在，未对用户实际文件做验收。
## 2026-09-13 17:24:07 +08:00 — 完善机体拼图与图标编辑界面

- 分支：`youhua_hu`
- 基线提交：`6d25d124f55fd47f90b028eceec76b35050ea6c2`
- 改了什么：
  - 参照旧修改器重排数据库机体页和机体拼图窗口，修复拥挤、控件遮挡、地址截断、武器栏需要拖动及机体碎片位置偏差。
  - 开放机体类型、主体/碎片图库地址、机体图标绑定和真实 CHR 像素编辑；图标支持严格 16×16 BMP/PNG 导入、旧版 BMP 导出及关卡动态图库选择。
  - 机体拼图支持上下两个 8×8 图库连续预览、主体代码编辑、自动压缩导入与脚本生成、图块编辑/删除和四向移动，并固定窗口及按钮位置。
  - 收录录像抽帧、界面对照与验证截图，重新生成正式修改器、校验文件和映射资料。
- 怎么改的：
  - 统一机体类型和 CHR 地址为旧版样式下拉框，地址同时呈现十六进制、十进制及文件偏移；将主体图库与效果图按固定逻辑像素布局，并使用独立按钮通道避免覆盖画布。
  - 修正 NES OAM 的 Y 坐标补偿和敌方碎片右侧锚点；拼图编辑使用固定工作色预览，主体效果不再叠加碎片层。
  - 新增机体图标绑定与像素编辑窗口，把图标关卡路由、四色画笔、导入导出和事务回滚接入现有工程模型。
  - 扩充数据库、地图、图标、拼图脚本、资源迁移和界面几何回归测试，并同步更新使用说明与差分研究记录。
- 影响范围：
  - `src/dc_modifier/`
  - `src/fc_editor/`、`src/fc_rom_editor_core.py`
  - `tests/`、`docs/`
  - `output/app/`、`output/build/`、`output/verification/`
- 验证：
  - `.\.venv\Scripts\python.exe -m unittest tests.test_screenshot_unit_feedback -v`：36 项测试全部通过，耗时 62.968 秒。
  - `.\tools\build_modifier_exe.ps1`：正式 EXE 构建通过；SHA-256 为 `4555D6930ACF9B4086DB8169C6900180E4CF0A24E82314EE1444ADF5BFA5BD32`。
  - 正式 EXE 使用 `--self-test` 自检，退出码为 0。
  - `git diff --check` 无内容错误，仅显示既有 LF/CRLF 转换提示。

## 2026-09-16 08:50:29 +08:00 — 归档M00—M02与M17验收证据

- 分支：`youhua_hu`
- 基线提交：`bca9e3ee81664b84abb93cecd9530d017dced0e1`
- 改了什么：
  - 归档 M00 现场采集、M01 启动与保存流程、M02 地图和图块属性、M17 命中阈值冷启动等人工验收材料，并更新黄金覆盖报告与字段注册表。
  - 为 M02 新修改器保存地图尺寸、容量门禁、画笔/吸取、图库只读边界及图块属性字段写入录屏和 ROM 样本；图库 A 六类字段与图库 B—G 单字段差分均有独立清单。
  - 记录图库 B 界面保存时自动补写缺失水面海标志的两字节行为，同时保留严格单字段样本，避免把自动修复误判为异常写入。
- 怎么改的：
  - 使用同一基线 ROM 采集并压缩现场录屏，对每个前后 ROM 计算 SHA-256 和字节偏移差分，生成 `field-diff-manifest.json` 作为机器可读入口。
  - 新增 M02 字段样本生成器，复用新修改器的 `MapTileAttributeCodec` 生成 B—G 最小样本并立即反解校验。
  - 同步更新 M00—M02 模块状态、人工待办、开发记录、黄金索引及覆盖报告；按用户决定取消旧修改器与新底板的不兼容对照，保留原失败材料但不纳入 M02 判定。
- 影响范围：
  - `docs/`
  - `tools/golden_pipeline/`、`tools/generate_m02_field_samples.py`
  - `output/build/legacy-diff-audit/`
  - `output/reports/`、`output/verification/`、`output/exports/`、`output/rom/`
- 验证：
  - `.\.venv\Scripts\python.exe tools\generate_m02_field_samples.py`：成功重建 B—G 样本并生成总差分清单。
  - `.\.venv\Scripts\python.exe -m unittest tests.test_map_tile_attributes tests.test_legacy_map_ui`：24 项测试全部通过，耗时 8.780 秒。
  - `git diff --check`：通过，无内容格式错误。

## 2026-09-18 22:30:00 +08:00 — 勾选M03—M07人工验收结果并归档证据

- 分支：`youhua_hu`
- 基线提交：`4072c52311940ba45b8727389d5cd71d624178bc`
- 改了什么：
  - 在 M03—M07 验收清单中勾选人工验收结果：M03(7/7)、M05(10/10)、M06(10/10)、M07(10/10) 和跨模块 X-01~X-05(5/5) 标记通过；M04 标注 2 项通过、4 项待功能完善验收。
  - 对验收清单表格做列宽对齐格式化。
  - 新增 M03-01 三关（第 1/4/28 关）验收截图证据。
  - 新增武器动画测试 ROM（$95/$9D）及其 Mesen 模拟器运行记录。
  - 更新 Mesen 手柄按键映射（A/B/方向键/Turbo）和窗口配置。
- 怎么改的：
  - 依据实际操作修改器逐项核对验收清单，将 `[ ]` 改为 `[通过 ]`，M04 未完成项标注"待功能完善验收"。
  - 验收截图保存至 `output/verification/manual-acceptance-m03-m07-20260918/`。
  - 武器动画测试 ROM 保存至 `output/verification/`，Mesen 存档和最近游戏记录保存至 `tools/vendor/mesen-0.9.9/`。
- 影响范围：
  - `docs/M03-M07验收清单.md`
  - `output/verification/manual-acceptance-m03-m07-20260918/`
  - `output/verification/weapon-animation-95.nes`、`weapon-animation-9D.nes`
  - `output/exports/working_modified.nes`、`新修改器work_modified_modified.nes`
  - `output/rom/working.nes`
  - `tools/vendor/mesen-0.9.9/settings.xml`、`RecentGames/`、`Saves/`
- 验证：
  - 未运行自动化测试；本次为人工验收结果记录和证据归档。
  - `git diff --check`：通过。
## 2026-09-18 13:21:55 +08:00 — 完成 M06 人物与 M07 武器编辑

- 分支：`legacy-ui-research`
- 基线提交：`d60e4bea8c07cebb91952bad35b1033ee49ba76e`
- 改了什么：
  - 完成 M06 人物 01—C8 全槽位编辑，支持普通/战斗双名称、对白指针、对话记录及变身关系，并为共用名称和对白建立明确的保存确认与默认取消保护。
  - 完成 M07 武器 01—FF 全槽位编辑，补齐名称、威力、命中、射程、消耗、地形适应、动画与模式等字段，接入旧版规则校验、容量提示和 Mesen 验证用例。
  - 同步人物/武器界面、工程保存重开、需求状态、差异矩阵、使用说明、探针脚本及旧修改器动态证据。
- 怎么改的：
  - 新增人物对白编解码器，解析 200 项指针表和 59 条唯一记录；使用固定容量安全重排写回对白数据，并按 64 字节边界重建变身表，拒绝越界或结构不完整的数据。
  - 扩展人物名称编解码与工程序列化，在名称池已重排时保存原始池数据，避免工程重开后按旧偏移错误解码；人物和武器界面统一使用事务提交、共享记录确认及恢复默认确认。
  - 扩展武器记录、校验服务和数据库页面，使全部 255 个槽位及规则反馈可编辑、可保存并可回读；保留旧版探针生成的 ROM、CDL、报告和自动备份作为本轮复刻证据。
- 影响范围：
  - `src/dc_modifier/`、`src/fc_editor/`、`src/fc_rom_editor_core.py`
  - `tests/`、`docs/`
  - `output/build/m06_m07_legacy_probe.py`、`output/build/legacy-diff-audit/`
- 验证：
  - `.\.venv\Scripts\python.exe -m unittest discover -s tests -t . -p "test_*.py"`：547 项测试全部通过，耗时 342.741 秒。
  - `.\.venv\Scripts\python.exe -m unittest tests.test_character_weapon_feedback`：30 项测试全部通过，耗时 23.387 秒；Python 编译检查与 `git diff --check` 通过。
  - 推荐 ROM SHA-256 保持 `82C218275459D53C0F306F6BC036C4797316976E0FA7AD1D5A8247E338995B8E`；`references/` 工作区无改动。

## 2026-09-18 19:21:27 +08:00 — 优化关卡切换并补充M03—M07验收清单

- 分支：`legacy-ui-research`
- 基线提交：`7271915188e86aae2408e517600c1b1fa83de2f7`
- 改了什么：
  - 修复 M06 双人物名称表接入后关卡切换需要等待数秒的性能回归，降低首次切换及已访问关卡往返耗时。
  - 新增 M03—M07 集中人工验收清单，共 53 项可执行检查，并按当前交付边界区分 M03/M04 只读验收、M05 部分实现验收和 M06/M07 当前底板验收。
  - 更新使用说明、研发记录、需求拆分入口、测试和映射资料，并重新生成正式修改器及 SHA-256 校验文件。
- 怎么改的：
  - 按普通/战斗名称指针表签名缓存人物名称容量，签名变化时自动失效，并将缓存限制为最多 8 版。
  - 名称与武器只读路径直接使用工程缓冲区，关卡刷新周期内复用成长表解析结果，避免重复复制整份 ROM 和重复构建编解码器。
  - 部署/事件表按列回收并复用下拉框和数值框，切换关卡时重新绑定共享模型和值，避免反复销毁和创建大量 Qt 控件。
  - 验收清单加入固定构建哈希、隔离副本、保存重开、跨模块事务、问题记录和最终签收规则；用户明确签收前不改写模块验收结论。
- 影响范围：
  - `src/dc_modifier/map_page.py`
  - `src/fc_editor/codecs/`、`src/fc_rom_editor_core.py`
  - `tests/`、`docs/`
  - `output/app/`、`output/mappings/`
- 验证：
  - 人物、武器和地图定向回归 56/56 通过，主窗口与性能回归 66/66 通过，合计 122/122。
  - 推荐 ROM 前 12 关首次切换平均 22.2 ms、最慢 75.1 ms；反向已访问关卡平均 9.4 ms、最慢 12.0 ms。
  - `build_modifier_exe.ps1` 构建成功，正式 EXE `--self-test` 退出码为 0，SHA-256 为 `CB075CA7275B213938FD15546807C27CC6CCB8FC899B197CCBBB92E646A37775`。
  - 验收清单共 143 行、53 项编号检查；`git diff --check` 无内容错误，仅显示 LF/CRLF 转换提示。

## 2026-09-18 19:58:31 +08:00 — 修复关卡切换并增加一键打包入口

- 分支：`legacy-ui-research`
- 基线提交：`86ffbfb2a8383531bfac82ca28b1a61dd8647562`
- 改了什么：
  - 修复从“幕后浮现”切换到其他关卡时部署表控件失效、界面不再响应并抛出 `Internal C++ object already deleted` 和 `NoneType.value` 的回归。
  - 新增仓库根目录双击打包入口，兼容 Windows PowerShell 5 与 PowerShell 7，并同步打包说明和脚本编码。
  - 更新 M02 状态说明、M03—M07 人工验收步骤、使用说明和开发记录，并重新生成正式修改器及 SHA-256 校验文件。
- 怎么改的：
  - 移除 `QTableWidget` 单元格编辑器对象池；行数缩减后让 Qt 正常销毁控件，重新扩展时创建新控件，同时继续复用共享下拉模型和数据缓存。
  - 增加“幕后浮现→首关→幕后浮现”等精确切换回归，逐格验证部署表控件、当前地图和选择行，防止已删除控件或空单元格再次进入草稿签名计算。
  - 根目录批处理固定 UTF-8 环境、检查虚拟环境和构建脚本，优先调用 `pwsh` 并回退至 `powershell`；构建脚本保存为带 BOM 的 UTF-8，避免中文路径乱码。
- 影响范围：
  - `src/dc_modifier/map_page.py`、`tests/test_map_feedback_ui.py`
  - `tools/build_modifier_exe.ps1`、`打包DC修改器.bat`
  - `docs/`、`README.md`、`AGENTS.md`
  - `output/app/新DC篇完整修改器.exe` 及校验文件
- 验证：
  - 地图界面 26/26、相关界面与图块属性 81/81、全量测试 549/549 通过；Python 编译检查和 `git diff --check` 通过。
  - 推荐 ROM 前 12 关首次切换平均 31.9 ms、最慢 77.9 ms；反向切换平均 18.9 ms、最慢 68.9 ms，均低于 100 ms 目标。
  - 当前 EXE 已完成隐藏自检，SHA-256 为 `A80366B68C357E2C2327F9C81CD792E6B4E5B6EE50BCC9FF64127D3A10DD0D1C`；打包末尾复制映射表时曾因一个目标 CSV 被占用而返回非零，但全部源/目标映射文件哈希一致。
  - 推荐 ROM SHA-256 保持 `82C218275459D53C0F306F6BC036C4797316976E0FA7AD1D5A8247E338995B8E`；`references/`、`output/rom/` 和 `output/patches/` 无改动。

## 2026-09-18 20:32:00 +08:00 — 完善 M08、M09 与 M13 兼容功能

- 分支：`legacy-ui-research`
- 基线提交：`67f00ed43218580f713215d2f25a751dbe4d6868`
- 改了什么：
  - 为战斗对话补齐“按列表/按内容”双视图、随机分支同步和四组末条边界回归。
  - 为其他修改1补齐系统文字转义注释、累计经验单调校验和 60 位成长十六进制快捷编辑，等级 61 入口继续禁用。
  - 文字转换器切换为参考版主码表的精确非空映射，并同步使用说明、主需求、模块状态与开发记录。
- 怎么改的：
  - 在现有固定容量文本编辑器上增加只读汇总视图，每次草稿变更同步更新三个可见列表；新增成长对话框只写前 60 个半字节值并保留尾部。
  - 将旧版精确码表构建为独立缓存源；ROM 文本编辑在其上增加可见占位符，转换工具则保持 F2 等参考版显示。
  - 两份旧版审计进度文件保留 Windows 错误 740 的失败轨迹，明确不把未能启动参考 EXE 记作回归通过。
- 影响范围：
  - `src/dc_modifier/legacy_text_pages.py`、`src/dc_modifier/legacy_tools.py`、`src/dc_modifier/legacy_windows.py`
  - `src/fc_editor/dc_text.py`、`tests/`
  - `docs/`、`output/build/legacy-diff-audit/`
- 验证：
  - `.\.venv\Scripts\python.exe -m unittest tests.test_legacy_text_scenario tests.test_legacy_windows_ui`：61 项测试全部通过。
  - `.\.venv\Scripts\python.exe -m unittest tests.test_legacy_tools_ui.LegacyToolDialogTests.test_text_converter_uses_all_nonempty_legacy_table_entries`：1 项码表全量映射测试通过；核对 2713 条非空映射。
  - `audit_legacy_initial_roster.py` 和 `audit_legacy_other1_tables.py` 未能启动参考 EXE，均因 Windows 错误 740（需要提权）失败；不记为通过。
  - `git diff --check` 无内容错误，仅有 LF/CRLF 转换提示。

## 2026-09-18 21:31:16 +08:00 — 推进 M10—M12 兼容界面与 M18 导出

- 分支：`legacy-ui-research`
- 基线提交：`1f7cf9facd8374ce4c8691597ca9f38b55a3c138`
- 改了什么：
  - 将其他修改2恢复为道具与商店同屏布局，补齐道具名称/价格/说明联动、F0—FE 商店列表和七段对话的可见结构及回归。
  - 将字库窗口收紧为参考版可见按钮结构，并为地图动画增加指针输入、规律名称和物理拼图等只读门禁入口。
  - 启动旧版 255 机体、每机体五 BMP 的导出协议实现和完整样本探查；纳入工作区已有的两份 15 曲音频时序修复 ROM 产物。
- 怎么改的：
  - 重组现有 Qt 页签和编辑控件，保留等长/固定容量写回；把增强型字模导入导出移至右键菜单，并用只读或禁用控件隔离尚未验证的动画协议。
  - 新增独立机体导出模块与样本探针，按旧版目录/文件命名、24 位 BMP 编码、战斗图层和图标 CHR 分页反推输出；当前仍明确保留未对齐项。
  - 更新使用说明、主需求、模块进度、开发记录和产品截图；截图脚本增加 Qt 对象有效性检查。
- 影响范围：
  - `src/dc_modifier/`
  - `tests/`、`tools/capture_dc_modifier_guide.py`、`output/build/m18_probe.py`
  - `docs/`、`output/rom/`
- 验证：
  - M10+M11 相关组合回归 104/104 通过；M12 动画与旧版工具定向回归 15/15 通过；21 张说明截图已重新生成。
  - M18 对 1275 个参考 BMP 的逐文件比对仍有 58 个不一致，尚未接入菜单或完成验收。
  - 本次提交工作流未额外运行测试；两份新增 ROM 仅记录 SHA-256，未重新构建或执行模拟器验收。

## 2026-09-18 22:05:11 +08:00 — 完成 M18 旧版机体五图导出

- 分支：`legacy-ui-research`
- 基线提交：`01ec20faefedd837fb4743248b671df11b36be29`
- 改了什么：
  - 完成旧版 001—255 机体五 BMP 批量导出，并让全部 1275 个文件与用户提供的完整参考样本逐字节一致。
  - 将数据菜单 `Ctrl+F` 接入原生保存对话框与固定导出目录；按 D2 保留置灰的参考头像入口，并把既有增强头像导出移到扩展功能。
  - 新增 M18 独立验证工具、自动化测试、JSON 证据和人工验收清单，同步需求状态、使用说明、开发记录及正式 EXE。
- 怎么改的：
  - 按完整样本建立固定图标敌我调色板槽位，复刻负坐标碎片的整块裁切和黑色碎片透明叠加规则，并保持导出过程不修改 ROM 缓冲区。
  - 批量导出逐机体隔离失败、报告进度并仅覆盖同名 BMP；独立验证器在内存生成全部文件，核对路径、字节、聚合哈希和 ROM 前后哈希。
  - 用界面回归覆盖原生保存路径、取消操作、禁用快捷键及增强入口，用全量样本回归锁定目录名、BMP 头和五类像素结果。
- 影响范围：
  - `src/dc_modifier/app.py`、`src/dc_modifier/legacy_unit_export.py`
  - `tests/`、`tools/verify_legacy_unit_export.py`、`output/build/m18_probe.py`
  - `docs/`、`output/verification/`、`output/app/`
- 验证：
  - `.\.venv\Scripts\python.exe tools\verify_legacy_unit_export.py`：1275/1275 文件匹配，0 个差异，生成与参考聚合 SHA-256 均为 `FB2122766C647507D357B62B5799B0856FCAB01F4EC649A889793F63A9FAC102`。
  - M18/UI 定向回归 14/14 通过；完整 `unittest discover -s tests -t . -p "test_*.py"` 为 567/567 通过，用时 289.707 秒。
  - `.\tools\build_modifier_exe.ps1` 构建成功，正式 EXE 隐藏自检退出码为 0，SHA-256 为 `246426B4B55D15C02E03AB536E340A6BBB7197D334EAF377C9FA3DA890734AFB`；推荐 ROM 导出前后哈希不变。

## 2026-09-18 22:57:53 +08:00 — 完成 M15 属性计算器

- 分支：`legacy-ui-research`
- 基线提交：`8299b7ff0930ed8c03633c9a2c12549d190b6e67`
- 改了什么：
  - 将属性计算器改为参考式敌我双面板和逐行结果列表，补齐机体实际武器装配、1—60 级成长与人物补正。
  - 实现命中临界、距离补正、严格双击边界、空陆海火力、伤害倍率、防御特技、剩余 HP 与击落次数计算，并在每次计算时读取当前 M17 参数。
  - 按 D6 保持两侧“更改倍数”按钮禁用，新增 M15 自动回归、使用说明、需求状态、截图证据和独立人工验收清单。
- 怎么改的：
  - 新增纯计算模块，以不可变状态对象隔离公式逻辑；界面负责从当前工程装配和参数构造输入，不向 ROM 写回。
  - 机体切换时仅列出两个实际武器槽；人物、等级和机体变化会重新计算成长值及上限，武器火力按参考显示规则与当前武器乘数同步。
  - 用纯公式边界测试和 Qt 界面回归覆盖距离、地形、特技编码、M17 实时联动、空武器槽及禁用入口。
- 影响范围：
  - `src/dc_modifier/battle_calculator.py`、`src/dc_modifier/legacy_tools.py`、`src/dc_modifier/app.py`
  - `tests/test_battle_calculator.py`、`tests/test_legacy_tools_ui.py`、`tests/test_rom_data_browser.py`
  - `docs/`、`output/verification/m15-attribute-calculator-2026-09-18/`
- 验证：
  - M15 公式、旧版工具界面和数据浏览器定向回归 27/27 通过。
  - 完整 `.\.venv\Scripts\python.exe -m unittest discover -s tests -t . -p "test_*.py"` 为 575/575 通过，用时 302.747 秒。
  - 说明截图脚本成功生成 21 张图片；推荐 ROM SHA-256 保持 `82C218275459D53C0F306F6BC036C4797316976E0FA7AD1D5A8247E338995B8E`。
  - 参考 EXE 动态逐行重放因当前环境需要提权尚未完成，用户验收表保持空白；本次提交工作流未额外重跑测试或构建正式 EXE。

## 2026-09-18 23:28:13 +08:00 — 收口 M14 剧情事件安全实现

- 分支：`legacy-ui-research`
- 基线提交：`7c7e296f4824fd3f890c94b1d9b4a1760017806b`
- 改了什么：
  - 将关卡设置内的界面、回合和即时事件改为参考式反汇编列表，双击指令后再进入隐藏的等长参数编辑窗口，并保持选中项与内容同步。
  - 恢复六类事件剩余空间入口的完整参考文案，在缺少动态分区证据时保持禁用，并移除会误报为六类容量的单一剧情文本配额弹窗。
  - 同步 M14 使用说明、主需求、模块状态、执行队列、产品截图和正式 EXE，新增带固定哈希、16 个步骤、问题登记与签收表的人工验收清单。
- 怎么改的：
  - 把三个真实事件页注册为隐藏控制器，主窗口为每页建立独立 `QListWidget` 投影；刷新时复制反汇编文本、Bank/地址/原码提示并同步当前行。
  - 通过禁用按钮和说明性提示固化 F-083 门禁，不在缺少参考版点击结果与六类分区算法时猜测容量。
  - 增加 Qt 回归，覆盖隐藏编辑器、主窗无裸露十六进制输入框、投影同步及按钮逐字文案与禁用状态。
- 影响范围：
  - `src/dc_modifier/legacy_windows.py`、`tests/test_legacy_windows_ui.py`
  - `docs/`、`output/app/新DC篇完整修改器.exe`
- 验证：
  - `tests.test_legacy_text_scenario tests.test_scenario_feedback tests.test_legacy_windows_ui`：71/71 通过；`tests.test_dc_modifier_ui`：56/56 通过，合计 127/127。
  - `py_compile` 通过；说明截图脚本成功重生成 21 张图片并人工核对剧情事件截图。
  - 一键打包成功，正式 EXE 隐藏 `--self-test` 返回 0，SHA-256 为 `1E6983541A07A0773B8DB1EE27498B472C669161434700187D9A1AA42DE40F77`；推荐 ROM 哈希保持不变。
  - `git diff --check` 通过，仅有既有 LF/CRLF 转换提示；本轮未运行完整 discovery 或 Mesen。

## 2026-09-18 23:51:44 +08:00 — 校正 M09 等级上限兼容判定

- 分支：`legacy-ui-research`
- 基线提交：`5fb23894551068d53f4453e705528b0b456dc79d`
- 改了什么：
  - 将当前受支持 ROM 的等级上限从错误硬编码的 60 校正为经运行时指令、经验表和成长记录三重验证的 99，并在“其他修改1”界面显示真实上限。
  - 明确隔离旧版 `audit.nes` 的 60→61、3778 字节重排样本，超过 99 级的未验证入口继续禁用。
  - 新增 M09 兼容性报告、自动回归、人工验收清单，并同步需求基准、使用说明、进度记录、截图和仍待执行清单的正式 EXE 哈希。
- 怎么改的：
  - 在成长编解码器和工程层增加等级上限一致性读取接口，以 `CMP` 立即数、99 项累计经验和 50 字节成长记录共同判定受支持布局。
  - 新增独立报告工具，对比当前 ROM 与旧审计前后样本的运行时上限、成长记录宽度、变更字节和产品载入门禁。
  - 重建正式 EXE，并用界面与编解码器回归锁定 99 级显示、禁用说明及旧样本拒绝行为。
- 影响范围：
  - `src/fc_editor/codecs/legacy_text_growth.py`、`src/fc_rom_editor_core.py`、`src/dc_modifier/legacy_windows.py`
  - `tools/report_m09_level_cap_compatibility.py`、`tests/`、`output/verification/`
  - `docs/`、`output/app/新DC篇完整修改器.exe`
- 验证：
  - M09 相关四套测试 81/81 通过，`py_compile` 通过；兼容性报告 `passed=true`，确认当前为 99 级布局、旧样本为 30→31 字节记录且存在 3778 处差异。
  - 说明截图 21 张重新生成并人工核对；正式 EXE 构建及隐藏 `--self-test` 成功，SHA-256 为 `E1FA6624851776BF4505CC3E97EC0F7E4189C14441C4216C0ECE3830DC035D3A`。
  - `git diff --check` 已通过，仅有既有 LF/CRLF 转换提示；本次提交工作流未额外运行完整 discovery 或 Mesen。

## 2026-09-19 01:14:12 +08:00 — 完成 M16 存档编辑器

- 分支：`legacy-ui-research`
- 基线提交：`5bcb7ea33066f9415ef6dfdc289a6c19a0275813`
- 改了什么：
  - 完成固定 8192 字节 DC SRAM 的三槽解析、校验、常驻队伍与活动战场编辑，并接通参考式上下两张 11 列表。
  - 打通“打开、读取、写入内存、备份后原子保存”的四阶段流程；存档工具无需先加载 ROM，载入 ROM 后补充名称、属性及可唯一推导的敌方信息。
  - 新增协议验证报告、自动化测试、M16 人工验收清单，更新需求状态、使用说明、进度记录、截图和正式 EXE。
- 怎么改的：
  - 新增独立 `LegacySaveCodec`，按活动块、备份块、三个 `$FB` 字节槽位及 16 位小端累加校验和进行无损编解码和边界校验。
  - 界面区分表格草稿、内存副本与磁盘文件，阻止未处理改动被静默重读或关闭；覆盖前生成唯一时间戳备份，并以临时文件原子替换目标。
  - 敌方机体与金钱不冒充 SRAM 字段，仅在当前 ROM 关卡部署可唯一匹配时只读显示；用实际存档锁定无操作恒等和单字段变更范围。
- 影响范围：
  - `src/fc_editor/codecs/legacy_save.py`、`src/dc_modifier/legacy_tools.py`、`src/dc_modifier/app.py`
  - `tests/`、`tools/report_m16_save_compatibility.py`、`output/verification/m16-save-compatibility.json`
  - `docs/`、`output/app/新DC篇完整修改器.exe`
- 验证：
  - M16 定向回归 30/30 通过；完整 `unittest discover -s tests -t . -p "test_*.py"` 为 588/588 通过，用时 292.518 秒。
  - 协议报告 `passed=true`；三槽校验全部有效，无操作往返逐字节恒等，EXP `3884→3885` 仅变化目标字节 `$1D5C` 与校验和字节 `$1D7C`。
  - 说明截图 21 张生成成功并完成目视核对；正式 EXE 构建及隐藏 `--self-test` 成功，SHA-256 为 `E22D66ABC6931575A046BE12B4775A25EF9C321351D3C854DC777076172A9281`。
  - `git diff --check` 已通过，仅有既有 LF/CRLF 转换提示；参考 EXE 单字段落盘、模拟器实际加载和用户验收尚未完成，未记为通过。

## 2026-09-19 01:54:14 +08:00 — 实现 M12 动画物理拼图与离线播放

- 分支：`legacy-ui-research`
- 基线提交：`5bad8089dab4013c2b1acc738a4f852cf5319375`
- 改了什么：
  - 将 M12“物理拼图”从空网格扩为真实 CHR 图库、`00/80` 图块映射、逐块属性表、最终拼图、整图翻转和帧规律播放。
  - 纠正组图首两字节的 X/Y 语义；对运行时图块令牌、循环次数和指针页保留明确占位或门禁，不以猜测结果冒充预览。
  - 新增兼容性报告、自动化测试、说明截图、M12 人工验收清单、开发记录和正式 EXE，并同步需求状态及现有验收清单哈希。
- 怎么改的：
  - 按当前 ROM Bank `$3E` 的 `$D194—$D2EF` OAM 例程实现 249 个组图记录的确定性解码，覆盖坐标步进、调色板和水平/垂直翻转。
  - 按 `$E8CC—$EA34` 帧解释器实现有限状态播放，识别结束、稳定循环和运行时依赖；81 个帧规律中 79 个可离线播放，`$12/$21` 保持门禁。
  - 预览层读取当前工程 CHR 与动画记录但不写 ROM；独立报告锁定解释器签名、表数量、资料样例、锚点修改范围和运行时分类。
- 影响范围：
  - `src/fc_editor/codecs/animation.py`、`src/dc_modifier/animation_editor.py`
  - `tests/test_animation_feedback.py`、`tests/test_m12_animation_report.py`、`tools/report_m12_animation_compatibility.py`
  - `docs/`、`output/verification/m12-animation-compatibility.json`、`output/app/新DC篇完整修改器.exe`
- 验证：
  - M12、报告与旧版工具定向回归 40/40 通过；完整 `unittest discover -s tests -t . -p "test_*.py"` 为 592/592 通过，用时 314.606 秒。
  - 兼容性报告 `passed=true`：249/249 个组图完整，81 个帧规律中 79 个可离线完成或稳定循环，运行时依赖仅 `$12/$21`。
  - 说明截图生成成功并目视核对；`py_compile` 与 `git diff --check` 通过。
  - 正式 EXE 构建及隐藏 `--self-test` 成功，SHA-256 为 `05D8ACD1701C93524649374F3DD8286123449CF11A5C455DAF227A15B87648C8`；本轮未运行 Mesen，参考 EXE 动态黄金和用户验收仍未完成。

## 2026-09-19 02:28:37 +08:00 — 实现 M08 战斗对话安全重排

- 分支：`legacy-ui-research`
- 基线提交：`48bd33d4e59cab686ac6675fffcad266f632907c`
- 改了什么：
  - 为 00/01/04/05 四组战斗对话增加同 Bank 安全文字池重排，允许先缩短正文释放容量，再增长同 Bank 的其他正文。
  - 保持控制 Token、共享正文别名和 F8 随机分支语义；超容、冲突草稿和未经验证空间在应用前拒绝，系统文字及非文字间隙不受影响。
  - 新增兼容性报告、自动回归、M08 人工验收清单和正式 EXE，同步使用说明、需求状态、开发记录及既有验收清单哈希。
- 怎么改的：
  - 从当前 ROM 的对象闭包固定 Bank `$2A` 5432 字节和 Bank `$0E` 6294 字节安全段，使用最小差异分配器优先固定未变对象，并原子更新正文、F8 子指针和两张根指针表。
  - 数据库页按 Bank 汇总全部草稿，显示占用/容量/剩余字节；重排后的 ROM 或工程重开仍从当前对象布局识别空闲区，不依赖原始地址。
  - 报告锁定两池满载基线、缩短/增长、别名拓扑、系统文字隔离、Bank `$0E` 保护间隙哈希和超出 2 字节的零写入拒绝。
- 影响范围：
  - `src/fc_editor/codecs/legacy_text.py`、`src/dc_modifier/legacy_text_pages.py`
  - `tests/test_legacy_text_scenario.py`、`tests/test_m08_battle_text_report.py`、`tools/report_m08_battle_text_compatibility.py`
  - `docs/`、`output/verification/m08-battle-text-compatibility.json`、`output/app/新DC篇完整修改器.exe`
- 验证：
  - M08 定向回归 29/29 通过；数据库/截图/道具组合回归 114/114 通过；完整 `unittest discover -s tests -t . -p "test_*.py"` 为 598/598 通过，用时 311.378 秒。
  - 兼容性报告 `passed=true`；Bank `$2A/$0E` 的容量、缩短→增长、保存重开、别名、保护区和超容拒绝检查全部通过。
  - `py_compile`、`git diff --check` 通过；正式 EXE 构建和隐藏 `--self-test` 成功，SHA-256 为 `475B0E3461E760751698A1DC56EBF75CDE958FDB7DA60B198B0B78988B2833E6`。
  - 本轮未运行参考 EXE 动态保存黄金或 Mesen 战斗烟测，用户尚未签收，未将 M08 标记为“已对齐”。

## 2026-09-19 03:16:36 +08:00 — 实现 M11 字库自动编码与全库协议

- 分支：`legacy-ui-research`
- 基线提交：`66f02909619f727d2fedd8f2ca12b1ff8b13a9a6`
- 改了什么：
  - 为字库编辑器增加保守的自动字符编码分配，以及带工程自定义映射的版本化 `.dcfontset` 全字库导入、导出。
  - 让项目码表随 `.dcmod` 持久化并参与撤销/重做，同时供文字转换和剧情文字编辑共用。
  - 新增 M11 兼容性报告、自动测试和人工验收清单，同步需求状态、使用说明、开发记录、正式 EXE 及交接清单。
- 怎么改的：
  - 仅把内置/工程码表未占用、双字节 Token 在 ROM 全文件零出现且 18 字节字模为统一填充值的槽位列为候选；字模与映射以一次事务提交并检查并发修改。
  - 定义 `DCFNTALL` v1 文件头，按 12 页固定顺序保存 2688 个独立字模和 UTF-8 自定义映射，并严格校验魔数、版本、长度、Token 与字符唯一性。
  - 参考程序可见布局保持不变，增强入口放入字模网格右键菜单；未取得的参考保存协议和运行时证据继续明确标为待验收。
- 影响范围：
  - `src/fc_editor/codecs/dc_font.py`、`src/fc_editor/dc_text.py`、`src/fc_editor/project.py`
  - `src/dc_modifier/font_edit.py`、`src/dc_modifier/legacy_tools.py`、`src/dc_modifier/story_page.py`
  - `tests/`、`tools/report_m11_font_compatibility.py`、`output/verification/m11-font-compatibility.json`
  - `docs/`、`output/app/新DC篇完整修改器.exe`、`output/manifest/文件清单_SHA256.csv`
- 验证：
  - M11/M13/M14 定向测试 48/48、扩大组合回归 170/170、完整 `unittest discover -s tests -t . -p "test_*.py"` 604/604 通过，用时 319.194 秒。
  - 兼容性报告 `passed=true`：覆盖 2688 个字模、48384 字节载荷、76 个安全槽、192 个行保留区、确定性全库往返、单事务撤销/重做与工程重开。
  - `py_compile`、`git diff --check`、正式 EXE 构建、隐藏 `--self-test`、文件清单更新和交接包验证均通过；EXE SHA-256 为 `9ADCBB0A7BF4017A0E54794FDFC43580558202070215275A0DAB23FDF1F812D4`。
  - 本轮未完成参考程序固定字模保存黄金、Mesen/FCEUX 新字符显示或用户签收，未将这些项目记为通过。

## 2026-09-19 19:08:03 +08:00 — 完成 M08—M18 实现与验证门禁

- 分支：`legacy-ui-research`
- 基线提交：`6cf4d1b21985c4491f1d41f594b6d3d3c1c0af86`
- 改了什么：
  - 完成 M08—M18 当前交付范围内的剩余实现、兼容性报告和验收材料，新增行动事件、章节标题、胜利文字等编解码与界面，并补齐 M09、M10、M12、M13、M14、M15、M17 的独立验证。
  - 完成 M12 动画参考程序动态证据、黄金样本与安全编辑协议：首图块写入可持久化，地图/精灵/移动复制受容量与唯一绑定保护，背景动画按证据分级开放；无法证明可安全写回的坐标和调用保持只读。
  - 修复关卡切换时表格编辑器生命周期异常及空单元格读取崩溃，同步使用说明、需求状态、开发记录、验收清单、截图、正式 EXE、黄金覆盖报告和交付门禁报告。
- 怎么改的：
  - 在 `src/fc_editor/codecs/` 增加行动事件、章节标题、胜利文字协议，在 `src/dc_modifier/` 增加相应编辑页并强化地图页、动画页、旧版工具和 ROM 数据浏览器的容量检查与只读防护。
  - 扩展黄金样本采集器的控件坐标点击、等待/断言与重试能力，将发现性样本与正式黄金分离，并以单元测试、兼容性报告、EXE 自检和模拟器脚本组成自动交付门禁。
  - 所有用户可见功能、操作步骤、限制和未验收项同步写入 `docs/DC修改器使用说明.md`、模块需求文档及汇总验收清单；未把实现自检当作用户验收。
- 影响范围：
  - `src/dc_modifier/`、`src/fc_editor/`、`src/fc_rom_editor_core.py`
  - `tests/`、`tools/`、`output/build/`、`output/reports/`、`output/verification/`、`output/app/`、`output/manifest/`
  - `docs/`、`提交记录.md`
- 验证：
  - 完整测试 `unittest discover -s tests -t . -p "test_*.py"` 为 647/647 通过，用时 341.058 秒。
  - 正式 `output/app/新DC篇完整修改器.exe` 隐藏自检退出码为 0，SHA-256 为 `F7784D2DE361621AC43B551B7759C8E9D5AB7FE63F8058DA7951A97CFDA6DA9D`。
  - Mesen 基线复验 17/17 通过，共 11309 帧，覆盖三首歌曲、56 个音效及旧引擎返回路径。
  - 黄金覆盖共 61 个正式样本和 5 个发现样本，G2 字段差异覆盖 61/61；60/61 个正式用例通过，唯一未通过项为 M05 移动编辑冷启动耗时 62.272 秒超过 30 秒门限，但目标偏移、写值与重开结果正确。
  - 自动交付门禁显示机器检查 11/11、实现范围 11/11；用户验收仍为 0/11，最终交付状态保持未通过，等待用户逐项签收。文件清单在追加本条提交记录前为 4616 项且交接包验证通过；暂存前 `git diff --check` 仅有换行符提示，暂存后另提示 Mesen `stdout.txt` 末尾新增空行，不影响证据内容。
## 2026-09-19 22:01:06 +08:00 — 修复 M04—M07 验收问题并优化 ROM 启动

- 分支：`legacy-ui-research`
- 基线提交：`ca717105530e9eb23a8fbbe2d0d21432b7d78d49`
- 改了什么：
  - 把启动页改为 ROM 优先动线，支持“打开ROM”、单个 `.nes` 拖入启动页和拖到 EXE 图标后直接载入；取消、无效文件或多参数时保留启动页。
  - 归档 `origin/youhua_hu@c54ab50` 的 M03—M07 人工验收与三张 M03 截图，保留 M03 只读范围通过、M04—M07 有条件通过的历史结论。
  - 修复四项有条件反馈：开放 M04 固定四字节商店/事件记录的安全编辑；恢复 M05 导图偏移与压缩上传；修正 M06 背景头像“四幅一库”换算；为 M07 增加四页签独立只读规律库。
  - 同步需求基准、模块状态、使用说明、人工待办、专项验收清单、探针报告、正式 EXE 和文件哈希清单。
- 怎么改的：
  - 启动器的按钮、文件拖放与命令行参数统一复用 ROM 身份、Mapper、受保护 Bank 和完整性校验，只在载入成功后关闭启动页。
  - M04 将部署门禁与触发器门禁拆开，事件写入继续执行坐标、F5—FE 无效商店和 300 字节托管区检查；参考版逐字段黄金仍保留为未完成项。
  - M05 对主体与碎片分别增加偏移/压缩控件和尺寸、容量规则；M06 依据参考人物 `$06` 的控件值与七字节原始记录做可逆 Bank/Slot 折算；M07 直接解析四张 ROM 指针表，展示名称、地址、共享项和代码，未知写回语义保持只读。
  - 扩展参考探针及 GUI/编解码回归，重建程序并生成机器可读的验收归档和返工报告；历史人工签核不由自动测试代填。
- 影响范围：
  - `src/dc_modifier/app.py`、`src/dc_modifier/map_page.py`、`src/dc_modifier/unit_appearance_dialog.py`
  - `src/dc_modifier/character_editor.py`、`src/dc_modifier/database_records.py`、`src/dc_modifier/weapon_rule_library.py`
  - `tests/`、`output/build/`、`output/verification/`、`output/app/`、`output/manifest/`
  - `README.md`、`docs/`
- 验证：
  - M04—M07 定向回归 167/167 通过；首次全量回归发现并修复 M05 预览顶边 3 像素的测试顺序问题，最终完整回归 659/659 通过，用时 356.841 秒。
  - `py_compile` 与 `git diff --check` 通过；后者仅报告既有 LF/CRLF 转换提示。
  - 正式 EXE 普通 `--self-test` 与 `--self-test-startup-rom output/rom/DC_kuorong_464K.nes` 均退出 0，SHA-256 为 `2127FC82D8C1A4C14F52672E90BB519A1821D0CAB755EE22A81FF32E6BF3A76E`。
  - 推荐 ROM SHA-256 保持 `82C218275459D53C0F306F6BC036C4797316976E0FA7AD1D5A8247E338995B8E`；文件清单更新为 4623 项，`验证交接包.ps1` 通过。M04—M07 当前返工仍待用户按专项清单复验，未登记为字段级“已对齐”。

## 2026-09-21 14:00:16 +08:00 — 沉淀 ROM 定制修改器建设方案

- 分支：`legacy-ui-research`
- 基线提交：`7d52ff7010a727f8f57103e74f4fb80307e852bb`
- 改了什么：
  - 新增跨 ROM 可复用的定制修改器建设方案，覆盖 ROM 身份、分层架构、风险分级、证据与黄金对照、事务安全、实施路线、测试验收、交付和新项目启动清单。
  - 把建设方案的持续维护要求固化到仓库规则；后续相关架构、验证和交付方法变化必须在同一轮同步优化方案。
  - 将方案接入文档索引、当前设计方案和用户说明，并同步开发记录与交接文件清单。
- 怎么改的：
  - 从当前 `RomImage`、`RomProfile`、Codec、`ChangeSet`、`ResourceGraph`、黄金流水线和模块验收纪律中提炼通用方法，同时明确 DC 专属 Mapper、Bank、偏移和运行时补丁不可直接套用。
  - 用 R0—R4 区分未知字段、固定字段、引用资源、变长资源和运行时补丁，并为各级定义写入策略与解禁条件。
  - 在 `AGENTS.md` 和方案第 17 章同时登记维护触发条件、通用化边界及文档联动要求。
- 影响范围：
  - `AGENTS.md`
  - `docs/ROM定制修改器建设方案.md`、`docs/README.md`
  - `docs/DC修改器设计与实现方案.md`、`docs/DC修改器使用说明.md`、`docs/开发记录.md`
  - `output/manifest/文件清单_SHA256.csv`
- 验证：
  - `git diff --check -- AGENTS.md docs output/manifest/文件清单_SHA256.csv` 通过，仅有 LF/CRLF 转换提示。
  - Markdown 代码块闭合，未发现占位符、尾随空白或冲突标记。
  - 文件清单更新并验证通过，共 11465 个文件。
- 本次仅改文档、仓库规则和清单，未运行 Python 测试、ROM 构建或模拟器验证。

## 2026-09-21 21:33:43 +08:00 — 固化 Git 身份与分支纪律

- 分支：`legacy-ui-research`
- 基线提交：`405d6d3a4a6bf9fda587e3c3d7cdf311e5adfa8d`
- 改了什么：
  - 固定本仓库访问 `origin` 时使用 `C:/Users/Administrator/.ssh/id_ed25519`，认证失败时直接报告，不再自动尝试其他身份。
  - 固化分支纪律：没有用户明确指定目标分支时必须停留在当前分支；查看、合并、比较或修复其他分支内容不构成切换授权。
- 怎么改的：
  - 在根 `AGENTS.md` 新增仓库 SSH 身份和分支纪律章节，明确 `IdentitiesOnly=yes`、只读跨分支引用及需要切换时先询问的约束。
  - 在 `docs/开发记录.md` 记录规则范围、验证结果、风险和后续执行要求，并按最终文件内容重新生成 SHA-256 清单。
- 影响范围：
  - `AGENTS.md`
  - `docs/开发记录.md`
  - `output/manifest/文件清单_SHA256.csv`
- 验证：
  - 当前分支核对为 `legacy-ui-research`，上游为 `origin/legacy-ui-research`；提交前工作区无合并或变基冲突。
  - `tools/更新文件清单.ps1` 通过 PowerShell 7 成功生成 11465 项文件哈希。
  - 本次仅修改仓库协作规则和文档，不涉及产品运行路径，未运行 Python、ROM 构建或模拟器测试。
## 2026-09-21 20:24:36 +08:00 — 记录H07-H10当前构建验收结果

- 分支：`hushanghua`
- 基线提交：`d958b27c56410508cf2fbc9d9c1b50fd96970dec`
- 改了什么：
  - 使用当前正式 EXE 和独立 ROM 副本完成 H-07—H-10 的可见界面验收，保存 8 张操作截图和验收用 ROM 副本。
  - 修正机器复验报告中与当前分支不符的结论：H-07 人物头像选择器正反面编号对调，H-08 商店事件正式入口仍只读，H-10 四类武器规则库及测试缺失。
  - 将 H-09 登记为部分通过：旧版双列布局、偏移范围、压缩选项和真实 BMP 预览正确，但保存受机体扩容绑定门禁阻止。
- 怎么改的：
  - 通过用户授权的鼠标/UI 自动化脚本操作打包后的 Windows 程序，所有导入尝试均指向独立工作副本，没有覆盖推荐 ROM。
  - 在报告中记录每项人工结果、自动回归结果、阻塞原因、证据文件路径以及验收前后 ROM SHA-256。
- 影响范围：
  - `output/verification/m03-m07-current-build-machine-recheck-2026-09-21.json`
  - `output/verification/manual-acceptance-current-20260921/`
- 验证：
  - `tests.test_portrait_export` 8/8、`tests.test_map_feedback_ui tests.test_legacy_map_ui` 41/41、`tests.test_screenshot_unit_feedback tests.test_legacy_windows_ui` 83/83 通过，合计 132 项。
  - 验收报告可被 PowerShell `ConvertFrom-Json` 正常解析，`git diff --check` 仅提示既有 LF/CRLF 转换。
  - 推荐 ROM 与工作副本 SHA-256 均为 `82C218275459D53C0F306F6BC036C4797316976E0FA7AD1D5A8247E338995B8E`，确认本轮未写入基准内容。

## 2026-09-21 22:09:41 +08:00 — 修复H07-H10并完成当前构建验收

- 分支：`hushanghua`
- 基线提交：`889cde7582c31e95bf150ea3dd8a4a94adfeb6e5`
- 改了什么：
  - 修正人物头像选择器的正面/背景字段映射与一基槽位显示，补齐头像导出的颜色与回归覆盖。
  - 开放已完成参考保存审计的商店事件编辑入口，恢复 M05 紧凑布局下的纵向图形区，并新增四页签武器规律只读库。
  - 完成 H07—H10 隔离 ROM 修复验收，更新 M03—M18 当前正式构建的原生窗口点击记录、机器报告、验收清单和正式 EXE。
- 怎么改的：
  - 将头像字段统一通过参考版选择器适配层读写，保持底层 `PortraitRecord` 协议不变；以自动测试锁定预览、导出与字段往返。
  - 商店事件仅解除已验证记录的写入门禁；部署编辑仍保持保护。武器规律按四张已验证指针表独立解析，指针异常时停止读取并保持只读。
  - 验收过程使用独立扩容 ROM，实际点击 M03—M18 窗口；文字转换完成“啊→C8 00→啊”，属性计算器产生双方命中、双击和伤害结果，全程未保存用户 ROM。
  - 报告工具对 Windows CRLF 检出采用规范化 LF 哈希，并让 M13 报告可从仓库根目录直接运行。
- 影响范围：
  - `src/dc_modifier/character_editor.py`、`src/dc_modifier/database_records.py`、`src/dc_modifier/legacy_windows.py`、`src/dc_modifier/map_page.py`、`src/dc_modifier/weapon_rule_library.py`
  - `tests/`、`tools/`、`output/verification/`、`docs/`
  - `output/app/新DC篇完整修改器.exe`
- 验证：
  - 完整测试为 728/728 通过，用时 867.458 秒；后续 M13 报告与 M08—M18 门禁定向测试 2/2 通过。
  - 正式 EXE 隐藏自检通过，SHA-256 为 `72E78D4900BAA7AF64187346A8B51EF20D4D7FF616517E5426B4F6542B98A752`。
  - M08—M18 机器门禁 11/11、实现范围 11/11，当前构建原生窗口点击烟测完成；M18 导出验证 1275/1275 精确匹配。
  - 推荐 ROM 验收前后 SHA-256 均为 `82C218275459D53C0F306F6BC036C4797316976E0FA7AD1D5A8247E338995B8E`；用户最终签收仍按模块单独记录。

## 2026-09-21 22:27:06 +08:00 — 合并 hushanghua H07-H10 验收修复

- 分支：`legacy-ui-research`
- 基线提交：`0550a65d5afcc7d3aedb4f1a523805e7982e4ec4`
- 改了什么：
  - 合入 `origin/hushanghua@ffe8eab095107da94f43c1e03b1c6ab15c83e417` 的 H07—H10 修复与当前构建验收成果，解决 5 个文件中的 11 个合并冲突，并清除合入侧历史提交记录中遗留的旧冲突标记。
  - 修正人物头像参考选择器显示，恢复 M05 机体/碎片导图双列、偏移和压缩入口，把 M04“任何人物”固定到事件人物下拉首项，并保留四类武器规律库的安全只读解析。
  - 同步正式 EXE、SHA-256、M03—M18 验收报告、原生窗口截图、隔离验收 ROM 和 H07—H10 修复报告；用户最终签收状态仍保持独立记录。
- 怎么改的：
  - 人物头像通过一基槽位适配函数往返底层 `PortraitRecord`；M05 紧凑布局将上传、清除、偏移、压缩和拼图控件接入既有 `UnitAppearanceDialog` 事务与容量校验；地图事件继续只开放已验证触发器写入，部署写入保持保护。
  - 冲突代码、测试和验收清单以 `origin/hushanghua` 为主；追加型历史记录保留双方有效条目。补充头像字段往返、机体/碎片导入边界、地图人物顺序和旧版窗口布局测试，并同步报告生成工具。
- 影响范围：
  - `src/dc_modifier/character_editor.py`、`src/dc_modifier/legacy_windows.py`、`src/dc_modifier/map_page.py`、`src/dc_modifier/weapon_rule_library.py`
  - `tests/`、`tools/accept_repairs_h07_h10.py`、`tools/report_*.py`
  - `docs/`、`output/app/`、`output/verification/`、`提交记录.md`
- 验证：
  - 合并解决后运行 `tests.test_map_feedback_ui`、`tests.test_screenshot_unit_feedback`、`tests.test_weapon_rule_library`、`tests.test_character_weapon_feedback`，共 102/102 通过，用时 47.969 秒。
  - 未合并索引为空；对 `docs/`、`src/`、`tests/` 和根提交记录扫描无冲突标记；五个原冲突文件的 `git diff --check` 通过。
  - 本轮未重新运行完整 728 项测试、正式 EXE 自检或模拟器验证；这些结果沿用合入侧已归档证据，不作为本轮重跑结论。

## 2026-09-21 23:36:00 +08:00 — 记录 M03-M17 最终验收结果

- 分支：`shanghua`
- 基线提交：`689e1eecf3a500877f682a7662a593779fb8c3c5`
- 改了什么：
  - 将用户明确给出的“M03—M17 按当前实现范围通过”回填到全局签核表、模块验收清单、主需求状态和 M08—M18 汇总。
  - 将 M08—M18 总门禁的用户签收更新为 11/11，并归档 M10 价格、M11 字模和 M17 双击参数的真实保存、完全重开与精确差分证据。
  - 保留 M12 的完成态只读门禁和 M16 的参考错误 4/安全分母 0 边界，不把当前范围签收扩大为未实现功能或完整参考对齐。
- 怎么改的：
  - 更新总门禁生成器的逐模块签收状态并重新生成汇总 JSON；同步各清单的验收人、日期、结论、未关闭边界和证据路径。
  - 使用隔离 ROM 副本执行界面保存与冷启动重开，保存原始工作副本、修改后副本和执行记录；推荐 ROM 保持不变。
- 影响范围：
  - `docs/`、`tools/report_m08_m18_delivery_gate.py`
  - `output/verification/`、`output/exports/`
- 验证：
  - `tools/report_m08_m18_delivery_gate.py` 退出码为 0；机器门禁、实现范围完成、用户签收完成和总交付完成均为 `true`，11/11 模块通过。
  - M10 价格 `4000→4010`、M11 C800 字模单像素及 M17 双击参数 `70→71` 均完成真实保存和重开；M17 仅变化三个预期镜像偏移。
  - 推荐 ROM SHA-256 仍为 `82C218275459D53C0F306F6BC036C4797316976E0FA7AD1D5A8247E338995B8E`；`git diff --check` 通过。

## 2026-09-22 20:10:52 +08:00 — 完善 M03-M07 验收与武器动画测试

- 分支：`shanghua`
- 基线提交：`3c232a3dd9b071d2185ecc62375c8a7695b520d3`
- 改了什么：
  - 完成 M03—M07 当前构建验收补证，补齐 M04 容量、坐标边界及正式 EXE 保存重开证据。
  - 将 M07 动画测试升级为所选武器的活动战场 ROM/SRAM 夹具，并归档 Mesen 实战、运行时追踪与全量回归证据。
  - 更新正式 EXE、校验文件和模块、用户、建设说明；校正 M08—M18 已签收范围的过期测试断言。
- 怎么改的：
  - 扩展 `LegacySaveCodec`，校验并镜像写入活动与备份战场块、重算校验；新增隔离测试 ROM/SRAM 生成器，导出副本绑定测试机体 `$11` 的当前武器。
  - 通过真实 Qt 控件、边界样本、正式 EXE、Mesen TestRunner 和逐字节、哈希报告完成门禁，保留参考不可达范围只读。
- 影响范围：
  - `src/dc_modifier/`
  - `src/fc_editor/codecs/legacy_save.py`
  - `tests/`
  - `docs/`
  - `output/app/`
  - `output/verification/`
- 验证：
  - 完整测试 743/743 通过，用时 850.271 秒。
  - 正式 EXE `--self-test` 退出码为 0，SHA-256 为 `111C3B3D9A5C11918DE4CF46E92899938EA948F13E40135FA8E776A616BD08E9`。
  - M07 Mesen 运行时显示所选 `$01 光束军刀`、选敌层和攻击战斗画面，结构化报告通过。

## 2026-09-23 15:06:19 +08:00 — Promote reviewed legacy golden evidence

- 分支：`shanghua`
- 基线提交：`5fab446bd7bec30eb0d1a820c8964f0045b3ce9b`
- 改了什么：
  - 将审核通过的 M05 拼图动作、M06 人物新增越界、M09 等级上限和 M12 精灵首图块证据晋级为黄金用例，并保留 11 项证据不足的 discovery 门禁。
  - 为参考操作增加显式预期无变化语义，使已验证的边界无操作可以进入黄金覆盖，同时仍拒绝将未持久化或协议不明的操作误判为通过。
  - 将在线采集结果改为引用用例报告的紧凑索引，避免重复嵌入大体积差分；更新覆盖率、M05 控件目录和 18 模块完成门禁报告。
- 怎么改的：
  - 扩展黄金用例、归档和字段注册模型以携带 `expected_noop`，并在分类、归档、统计和验证路径统一处理该语义。
  - 新增审核证据晋级工具与 M05 保存归一化配置，把已确认动作从 discovery 历史移动到正式用例；采集器使用可解析、可回写的紧凑结果引用。
  - 补充采集器、黄金流水线、M05 动作配方和控件目录测试，并把报告摘要明确拆分为 17 项已晋级黄金、8 项仍待动态证据。
- 影响范围：
  - `tools/golden_pipeline_core.py`、`tools/golden_pipeline_collect.py`、`tools/report_golden_coverage.py`
  - `tools/promote_m05_puzzle_evidence.py`、`tools/promote_reviewed_discovery_evidence.py`
  - `tools/golden_pipeline/`、`output/build/legacy-diff-audit/`、`output/reports/`
  - `tests/test_golden_pipeline.py`、`tests/test_golden_pipeline_collect.py`、`tests/test_m05_puzzle_discovery_specs.py`、`tests/test_m05_reference_control_catalog.py`
- 验证：
  - 完整测试 759/759 通过，用时 799.475 秒。
  - 18 模块最终门禁 18/18 通过，安全分母 6885；G1 为 230/241（95.44%），G2 为 233/233（100%），黄金用例无失败。
  - M05 控件目录专项测试 3/3 通过；`git diff --check` 通过；仓库内最大单文件 62.02 MB，低于 GitHub 100 MB 单文件限制。

## 2026-09-23 16:02:38 +08:00 — Close remaining legacy evidence gaps

- 分支：`shanghua`
- 基线提交：`123078b95a9c1e60f9c9dabfd6eb26b9a9f584f5`
- 改了什么：
  - 将 M09 `experience_level_2`、`experience_level_60` 与 M12 `sprite_anchor_x` 的真实保存、关进程和不同 PID 冷重开证据晋级为黄金用例，使黄金证据达到 238/238 通过。
  - 为 M05 剩余 8 项 discovery 固化动态时长、冷重开结果和明确晋级阻断原因；保留普通上传重复采集、失败重试和诊断日志，不把超时或协议不明动作误升黄金。
  - 保存工作区中并行存在的 V11 动态 R1 参考 ROM、重放脚本、截图与冰刃验证产物；本提交不据此扩大修改器参考对齐范围。
- 怎么改的：
  - 为采集器增加 `SysListView32` 键盘行选择，并让黄金报告区分请求值、原值和 `expected_noop` 的冷重开期望；使用哈希锁定的 M09 全局保存归一化画像解释参考版整库重写。
  - 扩展证据晋级脚本、字段注册表、M05 控件目录和相应测试；目录自动从黄金档案提取耗时、预算、变化数量与冷重开判定，为每个未晋级配方生成机器可检查的阻断列表。
  - 重新生成黄金索引、覆盖率报告、M05 目录和 M18 导出比对报告，并在开发记录中登记本轮证据边界。
- 影响范围：
  - `tools/golden_pipeline*`、`tools/report_golden_coverage.py`、`tools/report_m05_reference_control_catalog.py`、`tools/promote_reviewed_discovery_evidence.py`
  - `tests/test_golden_pipeline.py`、`tests/test_golden_pipeline_collect.py`、`tests/test_m05_reference_control_catalog.py`
  - `output/build/legacy-diff-audit/`、`output/reports/`、`output/verification/`、`docs/开发记录.md`
- 验证：
  - 完整测试 762/762 通过、失败 0、错误 0，用时 1006.923 秒；相关证据与完成门禁定向测试 6/6 通过。
  - 黄金档案 246 份，其中 golden 238、discovery 8；G1 为 233/241（96.68%），G2 为 238/238（100%），全局门禁 18/18 通过且安全字段分母为 6885。
  - M18 导出 1275/1275 文件一致、差异 0；推荐 ROM SHA-256 为 `82C218275459D53C0F306F6BC036C4797316976E0FA7AD1D5A8247E338995B8E`；`git diff --check` 通过。

## 2026-09-23 16:21:29 +08:00 — Refresh verified handoff manifest

- 分支：`shanghua`
- 基线提交：`3d22e17588017085db6a68d6b5d7a0d8c412ae84`
- 改了什么：
  - 为文件清单更新工具增加按工作区相对路径精确排除文件的能力。
  - 重新生成交接文件 SHA-256 清单，共登记 12391 个文件。
  - 将并行产生、尚未纳入修改器交付范围的实验诊断文件保留在工作区，但排除在本次交接清单和提交之外。
- 怎么改的：
  - 新增可选参数 `ExcludeRelativePath`，使用不区分大小写的哈希集合执行精确路径过滤。
  - 在保持现有目录、缓存及清单自身排除规则不变的前提下刷新清单，并重新执行交接包校验。
- 影响范围：
  - `tools/更新文件清单.ps1`
  - `output/manifest/文件清单_SHA256.csv`
- 验证：
  - `tools/验证交接包.ps1` 通过，共验证 12391 个文件；并行未跟踪文件进入清单的数量为 0。
  - 正式 EXE 隐藏自检退出码为 0，SHA-256 为 `E6E6184BAF3A884A4B977F993DFB80A1540DE82CA392A8EC3457E5A9138A2692`。
  - 18 模块完成门禁 18/18 通过，安全字段分母为 6885；`git diff --check` 通过。

## 2026-09-23 20:57:13 +08:00 — Complete modifier usability and archive runtime evidence

- 分支：`shanghua`
- 基线提交：`f1f22a1e01944993a1c8c023cee7d49c89c2dc9a`
- 改了什么：
  - 补齐面向零经验用户的 F1 全模块操作向导、容量诊断和可解释只读提示，完善人物、武器、剧情、字库、属性计算与地图动画的可用入口。
  - 将 M12 的 106 条背景规律和 86 个动画调用改为逐项显示安全参数或精确门禁原因；将 M05 剩余 8 个参考动作区分为参考动作未持久化与冷重开不匹配。
  - 更新正式 EXE、验收清单、兼容性报告和说明截图，并归档工作区中保留的武器跨 UI 运行时探针、ROM、Mesen 脚本及逐帧截图证据。
- 怎么改的：
  - 在动画编解码层集中维护背景边界和调用证据状态，界面复用同一原因文本；报告与测试检查只读项均有可执行分类，不以猜测扩大写入范围。
  - 为修改器增加统一新手任务模型和路由，把数据库六页及地图、剧情、字库、动画、存档、音乐、资源规划和最终验证串成明确工作流。
  - 通过正式打包入口重建单文件程序并同步 SHA-256；按用户要求使用 `git add -A` 收纳全部既有工作区内容，包括 10,424 个未跟踪实验/验证文件（约 74 MB）。
- 影响范围：
  - `src/dc_modifier/`、`src/fc_editor/`、`src/fc_rom_editor_core.py`
  - `tests/`、`tools/`
  - `docs/`、`output/app/`、`output/reports/`、`output/verification/`
- 验证：
  - M05/M12 及关联 UI 定向回归 59/59 通过；M08—M18 交付门禁修正当前 EXE 哈希后 2/2 通过，汇总为 11/11 模块机器门禁和总体交付通过。
  - 全仓 769 项首次运行中 768 项直接通过；唯一失败为验收清单仍登记上一版 EXE 哈希，更新构建证据与清单后由上述交付门禁定向复验关闭，未重新宣称一次未执行的 769/769 完整运行。
  - 正式 EXE `--self-test` 退出码为 0，SHA-256 为 `7E8BE4802BF639280FF28C66E9FA1707408BB49F75C796984037A1E0D61C6556`；推荐 ROM SHA-256 保持 `82C218275459D53C0F306F6BC036C4797316976E0FA7AD1D5A8247E338995B8E`；`git diff --check` 通过。

## 2026-09-23 22:08:57 +08:00 — Archive modifier acceptance and weapon UI experiments

- 分支：`shanghua`
- 基线提交：`01336b6102bb7fafcf36b2e09587b38c7066ab17`
- 改了什么：
  - 为当前正式修改器补齐本机构建的可见点击证据：打开推荐 ROM、通过 F1 新手向导直达人物头像与最终检查、导出三张头像 BMP，并执行 F7 完整检查。
  - 更新 M08—M18 当前构建证据、交付门禁、验收汇总及测试预期，使报告明确记录当前 EXE 已完成可见烟测，不再沿用“本轮控制接口不可用”的旧结论。
  - 保留工作区既有的武器跨 UI 研究记录，以及 V36—V42 ROM、逐帧截图、FCEUX/Mesen 脚本、存档和视频处理依赖等全部实验产物。
- 怎么改的：
  - 使用 Windows Computer Use 实际控制正式 EXE；对导出的 `[正面].bmp`、`[背面].bmp`、`[效果].bmp` 检查尺寸、颜色集合与 SHA-256，并写入结构化 `result.json`。
  - 将交付报告的可见烟测说明改为依据当前构建证据动态生成，同时调整门禁单元测试，使真假两种状态均由证据字段驱动。
  - 按本技能要求使用 `git add -A` 收纳全部既有工作区内容，包括约 1650 个未跟踪文件、1526 张 PNG、37 个 ROM 和本地 ffmpeg 二进制依赖。
- 影响范围：
  - `docs/M08-M18验收汇总.md`、`docs/开发记录.md`
  - `output/verification/`、`tools/vendor/fceux-2.6.6/sav/`
  - `tools/report_m08_m18_delivery_gate.py`、`tests/test_m08_m18_delivery_gate.py`
- 验证：
  - 人物 004 琉妮成功导出三张 32×32 BMP；正面与背景各 4 色，合成 7 色，预览及导出颜色正确。
  - 头像、新手向导和主界面 76 个实际功能用例通过；修正误写的测试模块名后，门禁与头像定向复验 12/12 通过。
  - 正式 EXE `--self-test` 退出码为 0；参考完成门禁为 18/18，安全字段分母 6885；M08—M18 总门禁为 11/11，`overall_delivery_complete=true`；`git diff --check` 通过。
