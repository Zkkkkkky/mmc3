# 提交记录

## 2026-09-10 08:38:12 +08:00 — 扩展资源池并完善修改器

- 分支：`expanded-modifier`
- 基线提交：`10a91dc88133cb998d81de0c95afd736f6baa63f`
- 改了什么：
  - 将原 CHR 旧位置副本和 Bank `$60` 释放为托管空间，使可分配资源池由 200 KiB 增至 464 KiB，同时保留旧基线兼容。
  - 完善修改器的资源导入、工程回放、保护区校验，以及机体、剧情、事件、音乐等编辑入口。
  - 全部页面改为旧版 SRW2 修改器式紧凑布局，并补充 13 张截图的图文使用说明和可重复截图工具。
  - 更新派生 ROM、IPS、单文件 EXE、映射表、容量/构建/验证报告与 Mesen 验证产物。
- 怎么改的：
  - 把双音频调度跳板写入原 Bank `$18` 的已验证空洞，清零旧 CHR 位置副本，以 `$40-$60/$65-$7D` 作为确定性分配区；`$61-$64/$7E-$7F` 保持专用或受保护。
  - 在 profile、工程模型、资源图和构建校验中登记资源 ID、对齐、地址、哈希及重叠保护，并将分配记录写入 `.dcmod`。
  - 隐藏现代外层导航和大标题，按菜单、顶部页签、左侧记录、右侧编辑及地图全景重新组织界面。
- 影响范围：
  - `tools/build_dc_expanded_dual_audio.py`
  - `tools/fc_editor/`
  - `tools/dc_modifier/`
  - `analysis/`
  - `FC模拟器/`
  - `build/`、`patches/`
  - `DC修改器使用说明.md`、`交接文档.md`
- 验证：
  - `.\.venv\Scripts\python.exe -m unittest discover -s tools -p "test_*.py"`：66 项通过。
  - `.\build_modifier_exe.ps1`：成功；EXE 自检退出码 0。
  - 最终 EXE 12 个功能页逐页视觉检查通过，13 张说明截图生成成功且链接有效。
  - `.\验证交接包.ps1`：2226 个文件全部通过。
