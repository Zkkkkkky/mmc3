# DC.nes 扩容 MMC3 双音频引擎交接文档

## 1. 最终状态

本交接包的正式基线是 `build/nsf/新DC.nes`，最终产物是 `build/DC_FamiStudio_增容双引擎_测试.nes`。

最终采用的不是“删除旧驱动并全量替换”，而是“保留原驱动 + 新增一套 FamiStudio 引擎”的双引擎方案，并通过扩展 PRG 保存全部新增内容。核心结果如下：

> 后续修改器交付以 `FC模拟器/DC_kuorong.nes` 为正式编辑基线；该文件为 Mapper 194、1,310,736 字节，SHA-256 为 `1DDD4F74B2D3ACEAA8A0BC4A6846BE8E6148C858C2D8EA5A1F6E0A04F0AD4A75`。`build/nsf/新DC.nes` 仍是只读源素材，禁止原地修改。修改器架构、事件逆向结果和操作方法分别见 `DC修改器设计与实现方案.md` 与 `DC修改器使用说明.md`。

- 原曲、原音效和旧音频驱动继续使用原始字节。
- Ash to Ash、Dark Knight、Dark Prison 使用同一套新增 FamiStudio 引擎。
- 新曲播放时，游戏的 56 个音效命令改由迁移后的 FamiStudio SFX 播放，避免两套引擎同时争用 APU。
- 三首新曲暂时只分配调试命令 `$9D/$9E/$9F`，没有绑定游戏角色、地图或剧情事件。
- 所有新增 ROM 数据都从源 ROM 的 EOF `$0C0010` 开始，没有占用原 ROM 内的空白字节。
- 源 ROM 保留不动，输出独立测试 ROM 和 IPS。

## 2. 原始音频数量的纠正

早期曾把 56 个六字节记录误认为 54/56 首曲目。后续通过入口、指针和运行时调用审计确认：

- 可达原背景音乐记录：20 首。
- 另有 1 个未分类记录，但不属于正常可达的 20 首 BGM 集合。
- 56 个六字节记录是音效/乐器配置，不是 56 首歌曲。
- 原音乐头和序列数据：14480 字节。
- 原驱动代码：2137 字节。
- 原音效表及数据：2086 字节。
- 原音频合计：18703 字节。

因此，“原曲约 4 KiB、加入一首却增加约 10 KiB”的主要原因不是原来有 54 首歌，而是独立 FamiStudio 版本同时携带了第二套播放引擎、入口、音色/包络、桥接代码和银行对齐空间。Dark Knight 曲目本体的最终 FamiStudio 数据为 4065 字节。

## 3. 方案演进和淘汰原因

### 3.1 Dark Knight 独立引擎版

最初 Dark Knight 自带独立 FamiStudio 播放器，曲目本体约 4065 字节，但连同引擎和桥接后明显增大。该版证明新曲可运行，但重复引擎造成容量浪费。

### 3.2 方案一：转换到原驱动格式

曾将 Dark Knight 转成 DC 原生两字节音符/时值流，让它共享旧驱动。该版本曲目负载为 5659 字节，比独立播放器方案节省约 321 字节，但听感明显下降，因此没有作为最终方案。

### 3.3 完全移除旧驱动

已完成过把 20 首原曲和 56 个音效全部迁到 FamiStudio 的技术验证。该方案可以运行，但会重写原曲和原音效数据，原曲听感存在变化，修改面与风险都更大。用户最终选择保留旧驱动，因此该方案只作为技术参考，报告保存在 `历史方案报告/`。

### 3.4 双引擎、使用原 ROM 空白 Bank

早期双引擎版把三首新曲和新引擎写入原文件的空白 Bank，文件仍为 786448 字节。功能能够运行，但它实际占用了原 ROM 现有空间，不符合“现在的空间完全不占用”的要求，因此被淘汰。

### 3.5 Mapper 199 扩容尝试

曾尝试把 Mapper 194 转成 Mapper 199，以获得更直接的扩展 Bank 访问。实际测试发现两者的 CHR 插槽/CHR-RAM 语义不兼容，画面会损坏，因此撤销 Mapper 199 转换。

### 3.6 最终方案：保持 Mapper 194、PRG 扩到 1 MiB

最终保持 Mapper 194 的寄存器和混合 CHR-ROM/CHR-RAM 行为，只将 PRG 从 512 KiB 扩到 1 MiB。这样既不动原正文，也能把新增音频放到原 EOF 之后。

## 4. ROM 身份与容量

| 项目 | 源 ROM | 最终测试 ROM |
|---|---:|---:|
| 文件大小 | 786448 字节 | 1310736 字节 |
| PRG | 524288 字节 | 1048576 字节 |
| CHR | 262144 字节 | 262144 字节 |
| Mapper | 194 | 194 |
| SHA-256 | `267CFA5E6273E0FE3557D5339B0E50172BCF25475283944C7E8372276FB3493C` | `223FDD6433C95B84D4566513FFC94C2B4121C3E4027A73FE841D13DB29BFF2E1` |

文件共增加 524288 字节。iNES 文件头只有偏移 `$04` 从 `$20` 改为 `$40`；源文件正文 `$000010-$0C000F` 在输出的相同文件偏移逐字节完全一致。

## 5. 最终 PRG/CHR 布局

| 区域 | 文件偏移/Bank | 用途 |
|---|---|---|
| 原 PRG | Bank `$00-$3F` | 完整保留，未修改 |
| 原 CHR 原位置副本 | Bank `$40-$5F` 对应的扩展 PRG 区 | 为保持原文件正文偏移而保留，不写入新增数据 |
| 复制的旧音频 Bank | Bank `$60`，从 `$0C0010` 开始 | 复制原 Bank `$18`，加入调度与同帧交接跳板 |
| Ash to Ash | Bank `$61`，文件偏移 `$0C2010` | 新曲数据 |
| Dark Knight | Bank `$62`，文件偏移 `$0C4010` | 新曲数据 |
| Dark Prison | Bank `$63`，文件偏移 `$0C6010` | 新曲数据 |
| 新引擎 | Bank `$64` | FamiStudio 引擎、56 个迁移 SFX、桥接器 |
| 预留扩展区 | Bank `$65-$7D` | 全零，供后续功能使用 |
| 新固定 Bank | Bank `$7E-$7F` | 原固定 Bank `$3E-$3F` 的副本；仅 Bank `$7F` 的两个音频 Bank 操作数改为 `$60` |
| 有效 CHR | `$100010-$14000F` | 源 CHR 的完整逐字节副本 |

注意：原 CHR 字节在旧文件位置依然保留，但扩容后的 iNES 布局把它们纳入了 PRG `$40-$5F`。真正供 PPU 使用的 CHR 是文件末尾的新副本。两个 CHR 副本均与源 CHR 相同。

## 6. 新音频数据容量

| 内容 | Bank | 已用字节 | Bank 内剩余 |
|---|---:|---:|---:|
| Ash to Ash | `$61` | 4672 | 3520 |
| Dark Knight | `$62` | 4065 | 4127 |
| Dark Prison | `$63` | 4834 | 3358 |
| FamiStudio 引擎代码 | `$64` | 2078 | — |
| 56 个 FamiStudio SFX | `$64` | 2984 | — |
| 双引擎桥接器 | `$64` | 310 | — |
| 普通跳板 | `$60` | 29 | — |
| 同帧切回旧驱动跳板 | `$60` | 24 | — |

新增音频有效数据合计 18996 字节。为了保持原文件正文偏移、保留 Mapper 194 行为并留出固定 Bank，ROM 采用 1 MiB PRG，而不是只按 18996 字节紧缩扩展。

## 7. 播放接口

### 7.1 游戏原接口

- 音频命令地址：`$004C`。
- 原背景音乐命令继续进入旧驱动。
- 原曲状态下的 `$00-$37` 音效继续进入旧驱动。
- `$FF` 表示无新命令/正常更新。

### 7.2 新曲调试接口

| 命令 | 曲目 | Bank | 游戏用途 |
|---:|---|---:|---|
| `$9D` | Ash to Ash | `$61` | 未绑定 |
| `$9E` | Dark Knight | `$62` | 未绑定 |
| `$9F` | Dark Prison | `$63` | 未绑定 |

新曲活动期间，`$00-$37` 会调用 FamiStudio SFX。收到其他负值旧音乐命令时，桥接器会清除新引擎状态、映射旧驱动固定 Bank、在同一个 NMI 内向旧驱动发送 `$FE` 复位，再重放待处理的旧音乐命令。

两套引擎不会在同一帧同时更新 APU。

## 8. RAM 布局和已修复故障

### 8.1 被淘汰的 RAM 区

早期扩容版使用了 `$0100-$0158`。短测能够通过，但长测数百帧后卡在 `$D05x`。原因是 `$0100` 属于 CPU 硬件栈页，并且游戏 NMI/PPU 更新路径会使用该页，FamiStudio 状态会破坏游戏栈和队列。

旧实验中的 `$031E-$037F` 也会被地图炮粒子系统使用；`$06B8-$06BD` 会被地图/剧情路径覆盖。最终版不再使用这些区域。

### 8.2 最终 RAM 分区

最终 FamiStudio 与桥接器只复用旧音频驱动拥有的 165 字节：

- 零页：`$0028-$005D`，不含 `$002C`。
- 第 4 页：`$0400-$046F`。
- FamiStudio 调用临时寄存器：`$0028-$002B`、`$002D-$002E`、`$0030-$0031`。
- FamiStudio 持久状态：`$002F`、`$0032-$004B`、`$0057-$005D`、`$0400-$0467`、`$046C-$046F`。
- 桥接魔数和当前新曲状态：`$0468-$046B`。
- `$004C-$0056` 同时是游戏音频接口和 FamiStudio 的 11 字节临时 APU 输出区；每次 `engine_update` 前全部压栈，返回后逆序恢复。

构建器会逐符号验证地址和长度，检查无重叠，并断言占用集合恰好等于旧音频驱动的 165 字节，任何越界都会中止构建。

## 9. 验证结果

### 9.1 静态和回放验证

- 专项测试：`tools/test_dc_expanded_dual_audio.py`，10/10 通过。
- 完整测试发现：108 项通过，10 项按环境条件跳过。
- Python 语法检查通过。
- IPS 应用于指定源 ROM 后，与最终测试 ROM 逐字节一致。
- 原文件正文保持为输出的完整前缀。
- 原 PRG、旧位置 CHR、新有效 CHR、固定 Bank 副本、三首曲目 Bank、新引擎 Bank 均有精确字节断言。

### 9.2 Mesen 0.9.9 运行时验证

- 自动检查 17/17 通过，运行 11309 帧，退出码 0。
- Ash to Ash、Dark Knight、Dark Prison 各持续约 3600 帧。
- FamiStudio 更新 10993 次。
- APU `$4000-$4013` 写入 101960 次。
- 56/56 个迁移音效入口全部调用。
- 显式切回旧曲 `$89` 成功，旧驱动在同一 NMI 内完成复位和命令处理。
- Bank `$60-$64/$7E-$7F` 均命中真实 1 MiB PRG 偏移，没有回绕到原 512 KiB。

三个 3600 帧耐久窗口只屏蔽了游戏标题流程自行发出的旧曲切换命令，使被测曲目保持活动；三首新曲切换、56 个正值音效命令和最后切回旧引擎均未屏蔽。

### 9.3 画面一致性

源 ROM 和最终测试 ROM 的第 675 帧截图大小均为 4736 字节，SHA-256 均为：

`B315051DA2C1CF8CD0AF743879F52CB5D95E54AF07EA5921600312A423D936A3`

两张 PNG 逐字节相同。

## 10. 模拟器兼容性

- 已验证：Mesen 0.9.9。
- 不兼容：FCEUX 2.6.6。其 Mapper 194 实现把可访问 PRG 固定为 512 KiB，Bank `$60-$7F` 会回绕。
- 需要使用能依据 iNES PRG 容量处理 Mapper 194 的模拟器、烧录卡或实现。
- 未完成真实卡带/烧录卡验证；如目标是实机，必须额外验证所用 Mapper 194 硬件是否支持 1 MiB PRG。

## 11. 交接包目录

```text
扩容MMC3/
├─ 交接文档.md
├─ 文件清单_SHA256.csv
├─ 验证交接包.ps1
├─ build/
│  ├─ nsf/                         源 ROM、三首原始 NSF、播放器
│  ├─ DC_FamiStudio_增容双引擎_测试.nes
│  ├─ DC_FamiStudio_增容双引擎_容量报告.json
│  ├─ DC_FamiStudio_增容双引擎_构建记录.md
│  └─ DC_FamiStudio_增容双引擎_验证记录.md
├─ patches/                        最终 IPS
├─ tools/                          构建器、专项测试、IPS 核心及 fc_editor 依赖
├─ analysis/
│  ├─ dc_dual_*.asm/.bin/.lst      双引擎、跳板和最终银行产物
│  ├─ three_song_*.asm             三首曲目 Bank 包装
│  ├─ *_famistudio_asm6.asm        三首曲目导出数据
│  ├─ dc_stock_sfx.asm             56 个迁移音效的最终数据
│  ├─ dc_stock_sfx_imports/        56 个音效的导入记录
│  ├─ dc_mesen_*.lua/.log          模拟器验证脚本和原始日志
│  ├─ mesen-*.png/.txt             源/输出画面证据
│  └─ FamiStudio-4.5.3-source/...  ASM6 和音高表构建依赖
├─ 验证工具/Mesen-0.9.9/          已用于运行时验证的模拟器及许可证
└─ 历史方案报告/                   被淘汰方案的容量与验证资料
```

交接包没有复制工作区中后来出现的“琉妮与白河愁战斗曲”等已绑定衍生 ROM，因为当前正式基线明确要求三首新曲暂不绑定用途，混入这些衍生版会造成基线歧义。

## 12. 重新构建

在 `扩容MMC3` 目录打开 PowerShell：

```powershell
python tools\build_dc_expanded_dual_audio.py
python tools\test_dc_expanded_dual_audio.py
python -m py_compile tools\build_dc_expanded_dual_audio.py tools\test_dc_expanded_dual_audio.py
```

构建器会先验证源 ROM 大小、iNES 头和固定 SHA-256；源 ROM 不匹配时会停止。输出会重新写入交接包自己的 `build/` 和 `patches/`，不会写回源 ROM。

已在 `扩容MMC3` 目录内实际执行上述专项构建和测试，10/10 通过，生成 ROM 与 IPS 的哈希均和正式产物一致。当前构建器对 Dark Prison 使用 `three_song_dark_prison_v9_data.asm` 和 `dark_prison_famistudio_asm6.asm`；目录中的 v10 文件仅作为先前数据版本参考，不参与正式构建。

已验证的 Mesen 0.9.9 运行文件也保存在 `验证工具/Mesen-0.9.9/`，其中保留原许可证。运行时 Lua 脚本和最终日志在 `analysis/`；没有把临时 Mesen 目录中的重复 ROM 或过时测试脚本混入交接包。

预期最终 ROM SHA-256：

`223FDD6433C95B84D4566513FFC94C2B4121C3E4027A73FE841D13DB29BFF2E1`

预期 IPS SHA-256：

`895A301DF94F7D59298EC7A97DAC78E0E32D6BC77CF6D5AF0DB091BB6B7D035B`

## 13. 后续绑定三首新曲

后续只需要让选定的游戏事件向 `$004C` 写入对应命令：

- Ash to Ash：`$9D`。
- Dark Knight：`$9E`。
- Dark Prison：`$9F`。

建议每次只绑定一个明确事件，并保留独立 IPS。绑定后至少重新验证：标题、存档载入、普通地图、角色战斗、地图炮/大范围特效、音效优先级、从新曲切回原曲、暂停/继续以及真实目标模拟器。

不要修改 `build/nsf/新DC.nes`；所有绑定应基于构建器生成的副本或通过新的派生构建脚本完成。

## 14. 尚未完成和风险边界

- 三首新曲的最终游戏用途尚未配置。
- 自动测试确认播放、循环、Bank、APU 写入、音效调用和引擎交接，但不能替代人工听感验收。
- FamiStudio SFX 是对原音效的迁移版本；虽然 56 个入口全部可运行，实际音色和优先级仍建议逐场景试听。
- FCEUX 2.6.6 不能用于本扩容版。
- 真实 Mapper 194 硬件的 1 MiB PRG 支持需要在最终烧录环境确认。
- ROM 文件可能受版权限制；对外分发时优先提供 IPS、报告和源码，不直接分发 ROM。

## 15. 正式交付文件

- 测试 ROM：`build/DC_FamiStudio_增容双引擎_测试.nes`
- IPS：`patches/DC_FamiStudio_增容双引擎_测试.ips`
- 容量报告：`build/DC_FamiStudio_增容双引擎_容量报告.json`
- 构建记录：`build/DC_FamiStudio_增容双引擎_构建记录.md`
- 验证记录：`build/DC_FamiStudio_增容双引擎_验证记录.md`
- 原始模拟器日志：`analysis/dc_mesen_dual_audio_final.log`
- 除哈希清单自身外的全文件哈希：`文件清单_SHA256.csv`
- 一键完整性验证：`验证交接包.ps1`
