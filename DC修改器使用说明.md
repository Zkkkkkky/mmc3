# 新DC篇完整修改器 2.1

本工具专用于 `FC模拟器/DC_kuorong.nes`。启动时会核对 Mapper 194、1.25 MiB 布局、关键音频银行和基准 SHA-256；所有 ROM 输出均为另存，不会覆盖基准文件。

## 启动

双击 `启动DC修改器.bat`，或直接运行 `FC模拟器/新DC篇完整修改器.exe`。源码方式：

```powershell
.\.venv\Scripts\python.exe tools\run_dc_modifier.py
```

## 推荐工作流

1. 打开 ROM，先保存一个 `.dcmod` 工程。
2. 从顶部五个工作区进入资源；数据库和剧情工作区还有第二级标签。每次已应用操作均可撤销/重做。
3. 按 `F7` 检查所有指针、容量、事件边界、音乐银行和受保护区域。
4. 使用“一键构建”输出新 `.nes`、`.ips`、工程副本和 JSON 报告。
5. 用 Mesen 实机验证新 ROM。FCEUX 2.6.6不能正确验证高位 Mapper 194 Bank。

## 功能页面

- **机体**：按真实名称搜索，修改已确认属性、名称引用、两个武器槽及 16 字节高级记录；复制共享记录前会显示影响范围。
- **机体导入与图像**：导出/导入 `.dcunit` 包；可携带 CHR 图块，支持 16×16 图块总览、PNG/CHR 导入、像素绘制、预览和还原。动画帧可作为连续图块范围一起打包。
- **武器**：按真实名称搜索，修改射程、命中、对空/陆/海攻击并显示原始记录和基准值。
- **剧情文本**：内置 2915 项新 DC 码表，Unicode/原始 Token 可逆编辑；未确认语义的控制参数明确显示为“原始字节”。
- **地图与部署**：绘制 100 张逻辑地形图；编辑 32 章敌军、客军和我方部署；新增「地图事件/商店」页，可编辑坐标、限定人物和事件号。
- **战场事件**：按章节、阶段和类型筛选。结构化修改敌军/客军增援、我方出击、人物/机体替换、临时友军、转敌、正式加入、自爆、撤退和移除；全部操作码显示已核对的中文语义，增援参数顺序为坐标、人物、机体、等级、AI/标志。
- **劝降条件**：以真实人物名修改 4 条已验证的「章节/劝说者/目标」匹配规则；关联成功脚本地址只读显示。
- **背景音乐**：修改或批量复制 200 个主动/被攻击音乐绑定；可替换 `$9D/$9E/$9F` 三个 8 KiB FamiStudio 槽，接受已组装 `.bin` 或不含外部引用的自包含 ASM6 导出。
- **资源占用、变更与验证**：显示 Bank 边界、逐字节改动、错误/警告和输出哈希。

## 重要边界

章节事件区已接近 8 KiB 上限，因此事件编辑强制保持原指令长度，不自动插入、删除或重排脚本。需要新事件时，可选择同长度的现有动作槽并套用模板。候选标志位、CHR 到具体机体动画的自动归属及逻辑地图块的地形语义仍不够可靠，工具会保留原值或明确标为高级功能，不会猜写。对外发布请优先提供 IPS，不要公开分发受版权保护的 ROM。

## 测试与打包

```powershell
.\.venv\Scripts\python.exe -m unittest discover -s tools -p "test_*.py"
.\build_modifier_exe.ps1
```

打包脚本会检查 EXE 依赖，并用 `--self-test` 实际创建主窗口、载入默认 ROM、核对 5 个工作区中的全部 12 个功能页后才生成校验值。EXE 旁的 `.sha256.txt` 用于核对独立版完整性。
